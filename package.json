{
  "name": "fibu-pdf-pipeline",
  "version": "1.0.0",
  "description": "PDF-Generator für FiBu-Fachbücher (Markdown → PDF mit Mermaid-Diagrammen)",
  "type": "module",
  "scripts": {
    "build": "node build-pdfs.mjs",
    "build:book1": "node build-pdfs.mjs --book=1",
    "build:book2": "node build-pdfs.mjs --book=2",
    "build:book3": "node build-pdfs.mjs --book=3",
    "build:book4": "node build-pdfs.mjs --book=4",
    "build:book5": "node build-pdfs.mjs --book=5"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "puppeteer": "^22.0.0"
  }
}
