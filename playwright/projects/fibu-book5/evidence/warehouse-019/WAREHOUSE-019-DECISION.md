# WAREHOUSE-019 Source Document Decision

Status: `labor-blocked`, `route-decision`, `no-posting`, `not-final`.

## Entscheidung

WAREHOUSE-016 bis WAREHOUSE-018 zeigen: Der Warehouse-Receipt-Pfad ist technisch erreichbar, aber es gibt keine konkrete auswählbare Source-Document-Zeile.

Deshalb wird der gleiche Dialog nicht erneut geklickt. Der nächste fachlich saubere Schritt ist WAREHOUSE-020: erst eine konkrete eingehende Quelle fuer FRA-ZL nachweisen oder erzeugen, dann Warehouse Receipt erneut versuchen.

## Belegt

- Frischer Warehouse Receipt Draft kann `Get Source Documents` oeffnen.
- `Source Documents - Inbound` wird sichtbar.
- `Use Filters to Get Src. Docs...` wird sichtbar und klickbar.
- Keine klare Source-Zeile ist sichtbar.
- Kein OK/Select, kein Source Confirm, kein Posting.

## Nicht belegt

- Kein geeigneter eingehender Quellbeleg.
- Keine Warehouse-Receipt-Zeilen aus Quelle.
- Kein Warehouse Receipt Posting.
- Kein Put-away.
- Keine Postenspur.
- Kein deutscher Finalnachweis.

## Naechster Schritt

WAREHOUSE-020: Source-Document-Readiness vor Receipt. Konkret: pruefen oder kontrolliert erzeugen, welcher Purchase-/Inbound-Beleg fuer FRA-ZL als Warehouse-Receipt-Quelle geeignet ist.
