# Posting- und Setup-Gates fuer FiBu Buch 5

Stand: 2026-06-17

Diese Datei ist die Freigabe-Leitplanke fuer autonome Laeufe in `MCP_1_20260210`. Sie verhindert versehentliche Doppelbuchungen, Setup-Aenderungen ohne dokumentierten Zweck, Company-Aktionen ohne Registry und das Umdeuten von CRONUS-USA-Laborbefunden in deutsche Finalnachweise.

Regel: Ein Gate ist gesperrt, solange der aktuelle Prompt oder diese Datei es nicht ausdruecklich fuer genau den naechsten Lauf freigibt. Seit `GOVERNANCE-005` gibt es zusaetzlich den Status `autonomous-allowed`: Er erlaubt keine freie Buchung, sondern nur den konkret beschriebenen Laborfall, wenn alle Vorbedingungen aus dieser Datei und `AUTOPILOT-STATE.json` frisch im UI nachgewiesen sind. Seit `GOVERNANCE-012` gibt es fuer Company-Aktionen den Status `autonomous-allowed-instance-scoped`: Company-Wechsel oder neue Labor-Companies sind innerhalb `MCP_1_20260210` erlaubt, wenn `COMPANY-REGISTRY.md/json` vor der Aktion aktualisiert ist und Ziel, Zweck, Risiken, Evidence-Plan und Rueckfalllogik dokumentiert sind. Alte Chat-Historie oder fruehere Bereitschaftsdateien zaehlen nicht als aktuelle Freigabe.

## Gate-Matrix

| Gate-ID | Status | Risiko | Freigabequelle | Erlaubte Aktion | Evidence-Pflicht | Rueckfallregel |
|---|---|---|---|---|---|---|
| `PAYMENTS-011-LAB-PAYMENT` | done-labor | Zahlung, OP-Ausgleich und Hauptbuchwirkung wurden erzeugt; Wiederholung wuerde die Referenz verwischen | Autopilot V2.2 / `GOVERNANCE-005`, ausgefuehrt in `PAYMENTS-011` | erledigt: genau eine kontrollierte RM-DEMO-Laborzahlung `PAY011-PS103297` fuer `D10000` / `PS-INV103297` nach frischem UI-Preflight; Rechnung zeigt `Remaining Amount = 0,00` und `Applied Entries = 1` | `evidence/payments-011/`, Screenshots `payments-011-*`, Customer Ledger Entries, Detailed Customer Ledger Entries mit `Payment Discount`/`Application`, G/L Entries mit `15110`/`18200`/`40910`; seit `PAYMENTS-013` Bank Account Ledger Entries ueber Page `372` mit `PAY011-PS103297`, `BANK-RM-01`, Betrag `67.673,60`, Entry No. `4995`; `PAYMENTS-014` synchronisiert Kapitel 20 mit diesem Pfad; Page `371` bleibt rejected; keine Bankabstimmung | nicht wiederholen; `PAYMENTS-014`, `TAX-002`, `GOVERNANCE-009`, `BOOK-O2C-FOUNDATION-DRIFT-SYNC` und `BOOK-REPORTING-UAT-K25-SYNC` sind erledigt; weiterer Payment-Schritt nur mit neuem Gate oder neuem Evidence-Zweck; naechster No-Approval-Schritt ist `GOVERNANCE-010-NEXT-NO-GATE-DECISION` |
| `REPORTING-011-ANALYSIS-VIEW-FIT` | rejected | Aendert Reporting-Setup und kann alte Negativbefunde ueberdecken | `GOVERNANCE-006`; verbraucht durch `REPORTING-011` | versucht: UI-first Analysis Views oeffnen und eigenen Laborfit `RM-PLCH` fuer `PRODUCTLINE`/`CHANNEL` vorbereiten; Ergebnis: Liste erreichbar, aber keine sichere editierbare Feldzuordnung, deshalb keine Anlage/Aenderung | `evidence/reporting-011/`, Screenshots `reporting-011-*`, JSON `REPORTING-011-result.json`; keine Buchung, keine Zahlung, keine Bankabstimmung | nicht wiederholen; neues Analysis-View-Setup nur mit neuem ausdruecklichem Feldmapping-/Setup-Gate |
| `REPORTING-013-ANALYSIS-VIEW-FIELDMAPPING-SETUP` | rejected | Reporting-Setup bleibt gesperrt; ungescopter `New/Neu`-Klick kann in falschen BC-Kontext fallen | `GOVERNANCE-007`; verbraucht durch `REPORTING-013` | erledigt: Analysis Views UI-first geoeffnet, REVENUE-Karte als Feldpositionsbeleg fuer `Code`, `Name`, `Dimension 1 Code`, `Dimension 2 Code` dokumentiert; `RM-PLCH` nicht angelegt/geaendert, weil `New/Neu` global mehrdeutig ist | `evidence/reporting-013/`, Screenshots `reporting-013-*`, JSON `REPORTING-013-result.json`; keine Buchung, keine Zahlung, keine Bankabstimmung, kein `RM-PLCH`-Setup | nicht wiederholen; jedes weitere Analysis-View-Setup braucht neues Gate plus stabil gescoptes New-/Kartenaktionsmuster oder manuelle UI-Fuehrung |
| `TAX-002-DE-VAT-FIT` | locked | Deutsche USt-/VAT-Einrichtung und Steuerposten koennen falsch behauptet werden | keine aktuelle Freigabe; `TAX-002` hat nur Freigabekriterien vorbereitet | VAT Business/Product Posting Groups, VAT Posting Setup, Preview und VAT Entries fuer Zielbeleg | Setup-Werte, Preview, VAT Entries, klare Labor-/DE-Trennung | ohne Freigabe nicht wiederholen: `TAX-002-DE-VAT-GATE-READINESS`, `GOVERNANCE-009`, `BOOK-O2C-FOUNDATION-DRIFT-SYNC` und `BOOK-REPORTING-UAT-K25-SYNC` sind erledigt; naechster No-Approval-Schritt ist `GOVERNANCE-010-NEXT-NO-GATE-DECISION`; praktische Freigabe nur fuer `TAX-002A-DE-VAT-SETUP-PREFLIGHT` oder `TAX-002-DE-VAT-FIT`; keine Setup-Aenderung |
| `COMPLIANCE-002-EINVOICE-SETUP` | locked | E-Rechnungs-/E-Documents-/Versandprofil-/Change-Log-Setup kann Compliance-Wahrheit falsch als erledigt erscheinen lassen | keine aktuelle Freigabe | E-Rechnungs-/Provider-/Peppol-/Versandprofil-/Change-Log-Setup pruefen oder aendern | Vorher/Nachher, UI-first Klickpfad, Setupwerte, Validierungs-/Versandstatus oder Auditnachweis, klare Labor-/DE-Trennung | `COMPLIANCE-001`/`COMPLIANCE-002` sind nur Readiness/Buch-Sync; ohne Freigabe keinen Compliance-Setup-Lauf starten |
| `SECURITY-002-USER-PERMISSION-SETUP` | locked | Benutzer, Permission Sets, Profile/Rollen, Security Groups, User Setup, Job Queue oder Change Log koennen Zugriff, SoD, Betrieb und Audit-Wahrheit veraendern | keine aktuelle Freigabe | Benutzer, Berechtigungen, Profile/Rollen, Security Groups, User Setup, Job Queue oder Change Log anlegen oder aendern | Vorher/Nachher, UI-first Klickpfad, Rechte-/SoD-Matrix, Review-/Auditnachweis, klare Labor-/DE-Trennung | `SECURITY-001` ist nur Readiness; ohne Freigabe nur Buch-Sync oder read-only Orientierung |
| `MIGRATION-002-CONFIGURATION-PACKAGES-IMPORT` | locked | Configuration Packages, Datenimport, Opening-Balance-Journale, neue Companies oder Cutover-Schritte koennen Setup, Stammdaten und Posten unwiderruflich veraendern | keine aktuelle Freigabe | Configuration Packages oeffnen/befuellen, Daten importieren, Opening-Balance-Journal vorbereiten/buchen oder Cutover-Schritte ausfuehren | UI-first Klickpfad, Tabellen-/Feldliste, Testimport-Grenze, Abstimmprotokoll, Salden-/Nebenbuchabgleich, Rueckfallregel und klare Labor-/DE-Trennung | `MIGRATION-001` ist nur Buch-/Readiness-Sync; ohne Freigabe keinen Import, keine neue Company und keine Opening-Balance-Buchung |
| `INTEGRATIONS-002-SETUP-OR-CONNECTOR` | locked | Extensions, AppSource-Apps, APIs/Web Services, Connector-Autorisierung, Power Platform, Power BI, Job Queues oder produktiver Datenaustausch koennen Datenfluss, Berechtigungen, Betrieb und UAT-Wahrheit veraendern | keine aktuelle Freigabe | genau einen Integrationskandidaten UI-first einrichten, testen oder pilotieren | Vorher/Nachher, UI-first Klickpfad, Extension-/Connector-Version, Auth ohne Secrets, Rollen/Berechtigungen, Testdaten, Fehlerfall, Rollback, Monitoring, Support Owner und klare Labor-/DE-Trennung | `INTEGRATIONS-001` ist nur Buch-/Readiness-Sync; ohne Freigabe keine Extension installieren, keine API/Web Services einrichten, keinen Connector autorisieren und kein Power-BI-/Power-Platform-Setup aendern |
| `OPERATIONS-002-JOB-QUEUE-OR-MONITORING-SETUP` | locked | Job Queues, Telemetrie, Admin Center, Monitoring-Connectoren und produktionsnahe Betriebsfunktionen koennen Automatisierung, Zugriff, Support- und Betriebswahrheit veraendern | keine aktuelle Freigabe | genau einen Betriebshebel UI-first pruefen, einrichten oder aendern | Vorher/Nachher, UI-first Klickpfad, Job-/Telemetry-/Admin-Kontext, Support Owner, Fehlerfall, Rueckfallregel und klare Labor-/DE-Trennung | `OPERATIONS-001` ist nur Buch-/Readiness-Sync; ohne Freigabe keine Job Queue anlegen oder starten, keine Telemetrie-/Admin-Aenderung, kein Monitoring-Connector und keine Produktivumgebung |
| `SOLUTIONARCHITECT-002-ARCHITECTURE-DECISION-OR-ADR` | locked | Architekturentscheidungen koennen Extension-, Customizing-, Integrations-, Security-, Migrations- oder Betriebsfolgen ausloesen und Laborbefunde falsch als Zielarchitektur festschreiben | keine aktuelle Freigabe | genau eine Architekturentscheidung oder einen ADR fachlich vorbereiten | Standardnachweis, Fit-Gap, Risiko/TCO, UAT-Fall, Owner, Rollback, Betriebsfolge, klare Labor-/DE-Trennung | `SOLUTIONARCHITECT-001` ist nur Buch-/Readiness-Sync; ohne Freigabe keine AL-/Extension-Entwicklung, keine AppSource-/API-/Connector-/Power-Platform-/Power-BI-Umsetzung und keine produktive Architekturentscheidung |
| `UAT-002-PRACTICAL-REGRESSION-OR-NEW-UAT` | locked | Ein praktischer UAT-Lauf kann je nach Fall Buchung, Zahlung, Setup-Aenderung, neue Company, Integration, Security-, Migration- oder Operationswirkung ausloesen | keine aktuelle Freigabe | genau einen UAT-Fall praktisch ausfuehren oder regressionsartig wiederholen | Testziel, Testdaten, erwarteter Nachweis, tatsaechliches Ergebnis, Abweichung, Screenshots/Evidence, Sign-off-Status und zusaetzlich das passende Prozess-/Setup-/Posting-Gate | `UAT-001` ist nur Buch-/Readiness-Sync; ohne Freigabe vorhandene Evidence einordnen, aber keinen neuen praktischen UAT-Lauf starten |
| `FIXEDASSETS-004-SETUP-OR-POSTING` | locked | Anlagenstamm, AfA-Buch, Anlagenbuchungsgruppen und Anlagenposten veraendern Setup und Ledger | keine pauschale Freigabe; `FIXEDASSETS-014` hat nur `HGB` erledigt | `MACHINES`, `FA-CNC-01`, `K30000` anlegen oder Anlagenzugang/AfA buchen; `HGB` nicht wiederholen ohne neuen Evidence-Zweck | Vorher/Nachher, Preview soweit moeglich, FA Ledger/G/L/Vendor Entries bei Buchung | `MACHINES`, `FA-CNC-01`, `K30000`, Zugang und AfA bleiben gesperrt; `HGB` ist Labor-Setup-Proof, kein deutscher Finalnachweis |
| `FIXEDASSETS-050-K30000-VENDOR-DEFAULTS-PAGEINSPECTION-OR-SETUP-GATE-DECISION` | done-decision | Aus `Personalisieren` koennte faelschlich ein Setup-Fit oder Kaufbeleg-Preflight abgeleitet werden | ausgefuehrt in `FIXEDASSETS-050` | erledigt: kein Default-/Setup-Fit und kein Kaufbeleg; naechster erlaubter praktischer Schritt ist read-only Page Inspection/Seitenpruefung auf der `K30000` Vendor Card Page `26` | `evidence/fixedassets-050/`; keine Screenshots, kein BC-Lauf, keine Kreditor-/Setup-Aenderung, keine Buchung | `FIXEDASSETS-051-K30000-VENDOR-PAGEINSPECTION-READONLY` ist erlaubt; weiter kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung, keine gespeicherte Personalisierung, kein API-Shortcut, kein Company-Wechsel |
| `FIXEDASSETS-051-K30000-VENDOR-PAGEINSPECTION-READONLY` | done-labor-read-only | Technische Seitenpruefung koennte faelschlich als sichtbares Anwenderbild oder Kaufbelegfreigabe gelesen werden | ausgefuehrt in `FIXEDASSETS-051` | erledigt: `K30000` Vendor Card Page `26` read-only geoeffnet; Page Inspection belegt `Vendor Card (26)`, `Vendor (23)` und Feldwerte `DOMESTIC`, leere Currency/VAT/Tax-Felder, `Nein`, `1M(8D)`, `BANK` | `evidence/fixedassets-051/`, Screenshots `fixedassets-051-*`; keine Kreditor-/Setup-Aenderung, keine gespeicherte Personalisierung, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-052-K30000-VENDOR-PURCHASE-INVOICE-PREFLIGHT-GATE-DECISION`; bis dahin kein Kaufbeleg, kein Zugang, keine AfA und keine Buchung |
| `FIXEDASSETS-052-K30000-VENDOR-PURCHASE-INVOICE-PREFLIGHT-GATE-DECISION` | done-decision | Technische Feldwerte koennten faelschlich als Buchungsfreigabe gelesen werden | ausgefuehrt in `FIXEDASSETS-052` | erledigt: der technische `K30000`-Nachweis reicht fuer genau einen Purchase-Invoice-Preflight ohne Buchung; der naechste Lauf darf Seite, Pflichtfelder, Draft-/Cleanup-Verhalten, `K30000`, `FA-CNC-01` und gefaehrliche Aktionen sichtbar pruefen | `evidence/fixedassets-052/`; kein BC-Lauf, keine Screenshots, keine Kreditor-/Setup-Aenderung, keine Buchung | `FIXEDASSETS-053-K30000-PURCHASE-INVOICE-PREFLIGHT-NO-POSTING` ist erlaubt; kein `Post`, kein Anlagenzugang, keine AfA, keine Buchung, Entwuerfe bereinigen, kein API-Shortcut, kein Company-Wechsel |
| `FIXEDASSETS-053-K30000-PURCHASE-INVOICE-PREFLIGHT-NO-POSTING` | done-labor-preflight | Ein sichtbarer neuer Purchase-Invoice-Kontext koennte faelschlich als Zielbeleg oder Buchungsreife gelesen werden | ausgefuehrt in `FIXEDASSETS-053` | erledigt: `Purchase Invoices` Page `9308` geoeffnet, `New/Neu` fuehrt zu `Purchase Invoice` Page `51`; Pflichtfelder `Vendor Name` und `Vendor Invoice No.`, Zeilenbereich `Type`/`No.` und `Post` als Gefahrengrenze sind sichtbar | `evidence/fixedassets-053/`, Screenshots `fixedassets-053-*`; keine K30000-Eingabe, keine FA-CNC-01-Zeile, keine Preview, keine Kreditor-/Setup-/Anlagen-Aenderung, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-054-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-GATE`; bis dahin kein `K30000`/`FA-CNC-01` in Einkaufsrechnung, keine Preview, kein `Post`, kein Zugang, keine AfA |
| `FIXEDASSETS-054-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-GATE` | done-decision | Zielwert-Eingabe in einem auto-save-nahen Belegkontext kann einen Draft erzeugen und faelschlich als Buchungsreife gelesen werden | ausgefuehrt in `FIXEDASSETS-054` | erledigt: genau ein enger Field-Mapping-Lauf ist freigegeben, um `K30000` im Kopf und `FA-CNC-01` in der Zeile sichtbar zu testen, weiterhin ohne Preview und ohne Posting | `evidence/fixedassets-054/`; kein BC-Lauf, keine Screenshots, keine Setup-/Stammdaten-Aenderung, keine Buchung | `FIXEDASSETS-055-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-NO-POSTING` ist erlaubt; Draft-Cleanup zwingend; kein `Preview Posting`, kein `Post`, kein Zugang, keine AfA, kein API-Shortcut, kein Company-Wechsel |
| `FIXEDASSETS-055-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-NO-POSTING` | done-rejected-cleaned-up | Unscharfer Zeilen-/Lookup-Fokus kann falsche Stammdatenkarten oeffnen und Drafts erzeugen | ausgefuehrt in `FIXEDASSETS-055` | erledigt/rejected: kein gueltiges K30000-/FA-CNC-01-Feldmapping; falscher `Vendor Card - V00040 - FA-CNC-01`-Kontext und Purchase-Invoice-Draft `107222` wurden ueber UI bereinigt | `evidence/fixedassets-055/`; rejected Screenshots `fixedassets-055-030-*`, `fixedassets-055-050-*`; Cleanup-Bilder `fixedassets-055-071-*`, `fixedassets-055-081-*`; keine Preview, keine Buchung | Kein Preview-/Post-Gate. Naechster Schritt ist `FIXEDASSETS-056-PURCHASE-INVOICE-FIELD-MAPPING-BLOCKER-DIAGNOSIS`: erst sicheren Zeilenkontext-Helper und Stop-Kriterien bauen; kein erneuter Field-Mapping-Lauf ohne neues Gate |
| `FIXEDASSETS-056-PURCHASE-INVOICE-FIELD-MAPPING-BLOCKER-DIAGNOSIS` | done-helper-evidence | Ein Code-Treffer im falschen Kontext koennte erneut als Anlagenzeile fehlinterpretiert werden | ausgefuehrt in `FIXEDASSETS-056` | erledigt: 055-Evidence wurde ohne neuen BC-Lauf klassifiziert; `Vendor registration dialog`, `Vendor Card`, `V00040` und `FA-CNC-01` ohne sichtbaren Zeilentyp `Fixed Asset` sind harte Stop-Kriterien | `evidence/fixedassets-056/`, Helper `playwright/core/bc/purchase-invoice-guards.ts`; keine Screenshots, kein BC-Lauf, keine Setup-/Stammdaten-Aenderung, keine Buchung | Kein Preview-/Post-Gate. Naechster Schritt ist `FIXEDASSETS-057-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-RETRY-GATE`: erst entscheiden, ob ein erneuter UI-first Field-Mapping-Lauf mit Guard erlaubt ist |
| `FIXEDASSETS-012-SCOPED-NEW-CARD-PREFLIGHT` | done-labor | Ein ungescopter oder unkontrollierter `New/Neu`-Klick kann unbeabsichtigt Datensaetze oder falsche Vorlagen oeffnen | `FIXEDASSETS-011-SETUP-GATE-DECISION`, ausgefuehrt in `FIXEDASSETS-012` | erledigt: Zielseiten geoeffnet, seitenbezogene `New/Neu`-/Kartenformulare cancel-safe inspiziert, Pflichtfelder/Feldlabels/Defaults/Templates/Abbruchwege dokumentiert | `evidence/fixedassets-012/`, Screenshots `fixedassets-012-*`; QA-Regel: Bilder zeigen leere Karten/Template-Dialog und nicht die Zielcodes `MACHINES`, `HGB`, `FA-CNC-01`, `K30000` | Nicht wiederholen ohne neues Form-Mapping-Delta; kein Speichern, kein Setup, keine Buchung; fuer Buchbilder muss der jeweils behauptete fachliche Zielzustand sichtbar sein |
| `FIXEDASSETS-014-HGB-DEPRECIATION-BOOK-FIT` | done-labor | Auch ein kleines AfA-Buch ist Setup und kann spaetere Anlagenlaeufe beeinflussen | `FIXEDASSETS-013-SETUP-FIT-DECISION`, ausgefuehrt in `FIXEDASSETS-014` | erledigt: `HGB` wurde UI-first in `RM-DEMO` angelegt, `Description = HGB depreciation book`; keine weiteren Zielobjekte | `evidence/fixedassets-014/`, Screenshots `fixedassets-014-010-*` und `fixedassets-014-020-*`; Nachher-Bild zeigt `HGB` und Beschreibung sichtbar | Nicht wiederholen ohne neues Evidence-Delta; kein `MACHINES`, kein `FA-CNC-01`, kein `K30000`, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-016-MACHINES-FA-POSTING-GROUP-FIT` | done-labor | Anlagenbuchungsgruppen steuern Sachkonten; falsche Konten wuerden Zugang, AfA, Abgang und Auswertungen verfaelschen | `FIXEDASSETS-015-MACHINES-ACCOUNT-MAPPING-DECISION`, ausgefuehrt in `FIXEDASSETS-016` | erledigt: `MACHINES` wurde UI-first/idempotent als CRONUS-USA-Laboralias der bestehenden Gruppe `EQUIPMENT` angelegt beziehungsweise nachgewiesen | `evidence/fixedassets-016/`, Screenshot `fixedassets-016-020-fa-posting-groups-after-machines.png` zeigt `MACHINES`, `12210` und `82000`; JSON-Ergebnis und Screenshot-Metadaten vorhanden | Nicht wiederholen ohne neues Evidence-Delta; kein `FA-CNC-01`, kein `K30000`, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine deutsche Finalbehauptung |
| `FIXEDASSETS-017-FA-CNC-01-SETUP-READINESS` | done-decision | Falsche Reihenfolge wuerde vom Setup direkt in Kreditor/Einkaufsrechnung/Zugang springen | ausgefuehrt in `FIXEDASSETS-017` | erledigt: naechste enge Schicht ist `FA-CNC-01` Karten-Preflight; `K30000`, Einkaufsrechnung, Zugang und AfA bleiben nachgelagert | `evidence/fixedassets-017/`; kein BC-Lauf, keine Setup-Aenderung, keine Buchung | Nicht wiederholen ohne neues Asset-Card-Readiness-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-018-FA-CNC-01-CARD-PREFLIGHT`; kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA |
| `FIXEDASSETS-018-FA-CNC-01-CARD-PREFLIGHT` | done-labor | Auch ein leerer Karten-Preflight kann falsch als fertiger Stammdatennachweis gelesen werden | ausgefuehrt in `FIXEDASSETS-018` | erledigt: `FA-CNC-01` ist gefiltert nicht sichtbar; die leere `Fixed Asset Card` zeigt Pflichtfelder und AfA-Bereich; nichts wurde gespeichert | `evidence/fixedassets-018/`, Screenshots `fixedassets-018-010-*` und `fixedassets-018-030-*`; Kartenbild ist Preflight, kein Zielstammdatenbild | Nicht wiederholen ohne neues Feld-/Visual-QA-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-019-FA-CNC-01-CARD-FIELD-MAPPING-DECISION`; kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-019-FA-CNC-01-CARD-FIELD-MAPPING-DECISION` | done-decision | Ein zu frueh gespeicherter Anlagenstammsatz koennte `HGB`/`MACHINES`-Setupbezug falsch oder unsichtbar lassen | ausgefuehrt in `FIXEDASSETS-019` | erledigt: Setup-Fit fuer `FA-CNC-01` noch nicht freigegeben; zuerst muessen `HGB` und `MACHINES` auf der Anlagenkarte sichtbar/setzbar gemappt werden | `evidence/fixedassets-019/`; kein BC-Lauf, keine Anlage, kein Setup, keine Buchung | Nicht wiederholen ohne neues Kartenfeldmapping-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-020-FA-CNC-01-CARD-MORE-FIELDS-MAPPING`; kein Speichern, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-020-FA-CNC-01-CARD-MORE-FIELDS-MAPPING` | done-labor | Feldsichtbarkeit kann falsch als gesetzter Zielwert gelesen werden | ausgefuehrt in `FIXEDASSETS-020` | erledigt: leere Anlagenkarte ohne Speichern geoeffnet; `Mehr anzeigen` fuer `General` und `Depreciation Book` zeigt `Depreciation Book Code` und `Posting Group`; Zielwerte wurden nicht gesetzt | `evidence/fixedassets-020/`, Screenshot `fixedassets-020-020-card-more-fields-mapping.png`; kein Speichern, keine Anlage, kein Setup, keine Buchung | Nicht wiederholen ohne neues Feld-/Visual-QA-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-021-FA-CNC-01-SETUP-FIT-DECISION`; kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-021-FA-CNC-01-SETUP-FIT-DECISION` | done-decision | Eine sichtbare Feldposition koennte faelschlich als Speicherfreigabe fuer den Anlagenstamm gelesen werden | ausgefuehrt in `FIXEDASSETS-021` | erledigt: `FA-CNC-01` wird noch nicht gespeichert; vor dem Speicherlauf muessen `HGB` und `MACHINES` als auswaehlbare/gesetzte Kartenwerte sowie Klasse/Unterklasse und AfA-Daten belegt werden | `evidence/fixedassets-021/`; kein BC-Lauf, keine Anlage, kein Setup, keine Buchung | Nicht wiederholen ohne neues Lookup-/Werte-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-022-FA-CNC-01-LOOKUP-VALUE-PREFLIGHT`; kein Speichern, kein neues Setup, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-022-FA-CNC-01-LOOKUP-VALUE-PREFLIGHT` | done-labor-partial | Ein sichtbares Feld oder ein falsch geoeffneter Dialog kann faelschlich als Lookup-Wertnachweis gelesen werden | ausgefuehrt in `FIXEDASSETS-022` | erledigt: leere Anlagenkarte no-save geoeffnet und nachlaufend bestaetigt, dass `FA-CNC-01` nicht gespeichert wurde; `HGB`, `MACHINES`, Klasse und Unterklasse sind nicht als richtige Karten-Lookupwerte sichtbar belegt | `evidence/fixedassets-022/`, Screenshot `fixedassets-022-020-empty-card-context.png`; alte falsche Lookupbilder entfernt; kein Speichern, keine Anlage, kein Setup, keine Buchung | Nicht wiederholen ohne neues technisches Diagnose-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-023-FA-CNC-01-CARD-TECHNICAL-DIAGNOSIS`; kein Speichern, kein neues Setup, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-023-FA-CNC-01-CARD-TECHNICAL-DIAGNOSIS` | done-labor-partial | Technischer Page-Nachweis koennte faelschlich als Speicherfreigabe gelesen werden | ausgefuehrt in `FIXEDASSETS-023` | erledigt: Page Inspection zeigt `Fixed Asset Card (5600, Document)` und Source Table `Fixed Asset (5600)`; das Kartenbild zeigt relevante Felder, aber der Locator muss auf die Vordergrundkarte scopen | `evidence/fixedassets-023/`, Screenshots `fixedassets-023-020-*` und `fixedassets-023-040-*`; kein Speichern, keine Anlage, kein Setup, keine Buchung | Nicht wiederholen ohne neues Active-Card-Locator- oder manuelles Personalisieren/Page-Inspection-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-024-FA-CNC-01-ACTIVE-CARD-CONTROL-HELPER-OR-MANUAL-DIAGNOSIS`; kein Speichern, kein neues Setup, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-024-FA-CNC-01-ACTIVE-CARD-CONTROL-DIAGNOSIS` | done-labor-partial | Aktive Kartencontrols koennen faelschlich als gesetzte Zielwerte oder Speicherfreigabe gelesen werden | ausgefuehrt in `FIXEDASSETS-024` | erledigt, aber nur partiell: Active-Card-Control-Helper mappt `FA Class Code`, `FA Subclass Code` und AfA-Datumsfelder; `Depreciation Book Code` und `Posting Group` sind in der aktuellen Diagnose `caption-not-visible`; `FA-CNC-01` bleibt nicht gespeichert | `evidence/fixedassets-024/`, Screenshot `fixedassets-024-020-active-card-control-diagnosis.png`, Helper `playwright/core/bc/cards.ts`; kein Speichern, keine Anlage, kein Setup, keine Buchung | Nicht wiederholen ohne Werte-/Lookup- oder Save-Gate-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-025-FA-CNC-01-VALUE-LOOKUP-OR-SAVE-GATE-DECISION`; kein Speichern, kein neues Setup, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-025-FA-CNC-01-VALUE-LOOKUP-OR-SAVE-GATE-DECISION` | done-decision | Ein partieller Control-Nachweis koennte faelschlich als Save-Gate gelesen werden | ausgefuehrt in `FIXEDASSETS-025` | erledigt: kein Werte-/Lookup-Preflight und kein Save-Gate, weil `FIXEDASSETS-024` nur 4/6 aktive Controls belegt und `Depreciation Book Code`/`Posting Group` fehlen | `evidence/fixedassets-025/`; kein BC-Lauf, kein Speichern, keine Anlage, kein Setup, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-026-FA-CNC-01-DEPRECIATION-BOOK-CONTROL-RECOVERY`; weiter kein Speichern, keine Werteingabe, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-026-FA-CNC-01-DEPRECIATION-BOOK-CONTROL-RECOVERY` | done-labor | Ein Kartenbild oder Helper-Klick kann faelschlich als Zielwert- oder Save-Gate gelesen werden | ausgefuehrt in `FIXEDASSETS-026` | erledigt: `Fixed Asset Card` wurde no-save geoeffnet; echte `Mehr anzeigen`-Buttons fuer `General` und `Depreciation Book` wurden geklickt; aktive Controls fuer `Depreciation Book Code` und `Posting Group` sind wiedergefunden | `evidence/fixedassets-026/`, Screenshot `fixedassets-026-030-depreciation-book-controls-recovery.png`, 6/6 Active-Card-Control-Diagnose; kein Speichern, keine Werte, keine Anlage, kein Setup, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-027-FA-CNC-01-VALUE-LOOKUP-PREFLIGHT-DECISION`; weiterhin kein Speichern, keine Werteingabe ausser explizitem no-save Lookup-/Wertpfad, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-027-FA-CNC-01-VALUE-LOOKUP-PREFLIGHT-DECISION` | done-labor-cleanup | Lookupwerte koennen faelschlich als no-save gelesen werden; New Card kann eine Auto-Number-Karte speichern | ausgefuehrt in `FIXEDASSETS-027` | erledigt: Lookup-/Wertpfade zeigen `TANGIBLE`, `EQUIPMENT`, `HGB` und `MACHINES`; `FA-CNC-01` wurde nicht gespeichert; temporaerer Auto-Number-Datensatz `FA000110` wurde erkannt und per UI geloescht | `evidence/fixedassets-027/`, Screenshots `fixedassets-027-*`; Cleanup-Nachweis `100-auto-number-draft-cleanup-coordinate-result.json`; kein Setup, kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung | Nicht wiederholen ohne cleanup-aware Auto-Number-Pattern oder neues Evidence-Ziel; naechster No-Approval-Schritt ist `FIXEDASSETS-028-FA-CNC-01-AUTO-NUMBER-SAVE-GATE-DECISION` |
| `FIXEDASSETS-028-FA-CNC-01-AUTO-NUMBER-SAVE-GATE-DECISION` | done-decision | Nach Auto-Number-Falle koennte ein Folge-Agent zu viel auf einmal speichern oder direkt in Einkauf/Zugang springen | ausgefuehrt in `FIXEDASSETS-028` | erledigt: naechster praktischer Lauf darf nur `FA-CNC-01` als Zielanlage speichern, mit Stop-Kriterien fuer Auto-Number, Klasse/Unterklasse, `HGB`, `MACHINES` und AfA-Pflichtfelder | `evidence/fixedassets-028/`; kein BC-Lauf, kein Setup, keine Anlage, kein `K30000`, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-029-FA-CNC-01-CONTROLLED-ASSET-SAVE`; bei Nummern-/Wert-/Cleanup-Abweichung abbrechen und bereinigen; weiterhin kein `K30000`, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-030-FA-CNC-01-CORRECTION-GATE-DECISION` | done-decision | Eine vorhandene, aber leere Zielanlage koennte faelschlich als buchungsreifer Stammsatz gelesen oder durch einen neuen Zielcode umgangen werden | ausgefuehrt in `FIXEDASSETS-030` | erledigt: `FA-CNC-01` bleibt Buch-/Labor-Zielcode; naechster praktischer Lauf darf nur diese bestehende Karte UI-first korrigieren, wenn Safety-Checks keine Acquisition-/Posting-/Book-Value-Wirkung zeigen | `evidence/fixedassets-030/`; kein BC-Lauf, keine Setup-Aenderung, keine Stammdatenkorrektur, kein `K30000`, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-031-FA-CNC-01-EXISTING-CARD-CORRECTION`; erlaubt sind nur Kartenwerte `CNC Maschine FRA`, `TANGIBLE`, `EQUIPMENT`, `HGB`, `MACHINES`, `8 Jahre` plus belegbare AfA-Datumslogik; bei Buchwert/Posten/Acquired stoppen |
| `FIXEDASSETS-031-FA-CNC-01-EXISTING-CARD-CORRECTION` | done-labor-partial | Teilweise korrigierte Anlagenkarte koennte faelschlich als buchungsreif gelesen werden | ausgefuehrt in `FIXEDASSETS-031` | erledigt: bestehende `FA-CNC-01`-Karte direkt ueber Page `5600` geoeffnet, Safety-Check ohne Anlagenposten/Blockbuchwert, `HGB` und `MACHINES` sichtbar gesetzt; keine Buchung | `evidence/fixedassets-031/`, Screenshots `fixedassets-031-*`; Nachherbild zeigt `FA-CNC-01`, `HGB`, `MACHINES`, `Book Value = 0,00`, aber nicht Beschreibung/Klasse/Unterklasse/AfA-Jahre als fit | Naechster No-Approval-Schritt ist `FIXEDASSETS-032-FA-CNC-01-CORRECTION-BLOCKER-DIAGNOSIS`; keine Einkaufsrechnung, kein `K30000`, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-032-FA-CNC-01-CORRECTION-BLOCKER-DIAGNOSIS` | done-labor-partial | Teilfit mit AfA-Daten koennte faelschlich als buchungsreifer Anlagenstamm gelesen werden | ausgefuehrt in `FIXEDASSETS-032` | erledigt: bestehende `FA-CNC-01`-Karte direkt ueber Page `5600` geoeffnet, Safety-Check ohne Anlagenposten, `HGB`, `MACHINES`, AfA-Start `01.01.2026`, `8,00` Jahre und Enddatum `31.12.2033` sichtbar; keine Buchung | `evidence/fixedassets-032/`, Screenshots `fixedassets-032-*`; finales Bild zeigt die AfA-Daten, aber Beschreibung, `FA Class Code` und `FA Subclass Code` bleiben leer/nicht fit | Naechster No-Approval-Schritt ist `FIXEDASSETS-033-FA-CNC-01-FIELD-EDITABILITY-HELPER-OR-MANUAL-PATH`; keine Einkaufsrechnung, kein `K30000`, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-033-FA-CNC-01-FIELD-EDITABILITY-HELPER-OR-MANUAL-PATH` | done-labor-partial | Ein fast vollstaendiger Karten-Teilfit koennte faelschlich als buchungsreifer Anlagenstamm gelesen werden | ausgefuehrt in `FIXEDASSETS-033` | erledigt: bestehende `FA-CNC-01`-Karte direkt ueber Page `5600` in breiter Layoutansicht geoeffnet, Safety-Check ohne Anlagenposten, Stift-/Edit-Top-Icon geklickt, `Description = CNC Maschine FRA`, `FA Class Code = TANGIBLE`, `HGB`, `MACHINES`, AfA-Start `01.01.2026`, `8,00` Jahre und Enddatum `31.12.2033` sichtbar; keine Buchung | `evidence/fixedassets-033/`, Screenshots `fixedassets-033-*`; finales Bild zeigt die korrigierten Kartenwerte, aber `FA Subclass Code` bleibt leer/nicht fit | Naechster No-Approval-Schritt ist `FIXEDASSETS-034-FA-CNC-01-SUBCLASS-FIELD-DIAGNOSIS`; keine Einkaufsrechnung, kein `K30000`, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-034-FA-CNC-01-SUBCLASS-FIELD-DIAGNOSIS` | done-labor | Der geloeste Subclass-Blocker koennte faelschlich als Freigabe fuer Anschaffung oder AfA gelesen werden | ausgefuehrt in `FIXEDASSETS-034` | erledigt: bestehende `FA-CNC-01`-Karte direkt ueber Page `5600` in breiter Layoutansicht geoeffnet, Safety-Check ohne Anlagenposten, `FA Subclass Code = EQUIPMENT` sichtbar und im idempotenten Rerun persistent; keine Buchung | `evidence/fixedassets-034/`, Screenshots `fixedassets-034-*`; 034 zeigt Subclass/AfA-Zeilen, 033+034 zusammen bilden den Labor-Stammdatenfit | Naechster No-Approval-Schritt ist `FIXEDASSETS-035-K30000-VENDOR-READINESS-DECISION`; kein `K30000` ohne Readiness-Entscheid, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-035-K30000-VENDOR-READINESS-DECISION` | done-decision | Ein fertiger Anlagenstamm koennte faelschlich als Freigabe fuer Kreditoranlage, Einkaufsrechnung oder Anlagenzugang gelesen werden | ausgefuehrt in `FIXEDASSETS-035` | erledigt: `K30000` bleibt nicht nachgewiesen; naechster Schritt darf nur `Vendors/Kreditoren` read-only pruefen und den Zielkreditor sichtbar suchen/filtern | `evidence/fixedassets-035/`; kein BC-Lauf, keine Kreditorenanlage, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-036-K30000-VENDOR-PREFLIGHT-READONLY`; erlaubt ist nur read-only Vendor-Preflight in breiter Layoutansicht, keine Speicherung, kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-036-K30000-VENDOR-PREFLIGHT-READONLY` | done-labor-read-only | Ein leerer oder schlecht sichtbarer Vendor-Screenshot koennte faelschlich als Zielkreditor- oder Kartenbeweis gelesen werden | ausgefuehrt in `FIXEDASSETS-036` | erledigt: `Vendors` Page `27` wurde mit Filter `No. = K30000` geoeffnet; Filterbereich und leere Liste sind sichtbar, `K30000` ist nicht als Kreditorenzeile sichtbar; keine Speicherung, keine Anlage, keine Buchung | `evidence/fixedassets-036/`, Screenshot `fixedassets-036-010-vendors-k30000-readonly.png`; Bild ist negativer Filter-/Listen-Kandidat, kein Kreditorenkartenbild | Gate-Entscheidung wurde in `FIXEDASSETS-037` erledigt; nicht wiederholen ohne neues Vendor-Evidence-Delta |
| `FIXEDASSETS-037-K30000-VENDOR-SETUP-GATE-DECISION` | done-decision | Ein negativer Vendor-Listenbefund koennte faelschlich als Freigabe fuer Einkaufsrechnung oder Anlagenzugang gelesen werden | ausgefuehrt in `FIXEDASSETS-037` | erledigt: genau ein naechster UI-first Kreditor-Setup-Fit fuer `K30000` / `Zollspedition Nord GmbH` ist erlaubt; danach Karten-Defaults lesen | `evidence/fixedassets-037/`; kein BC-Lauf, keine Kreditoranlage, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Folgeschritt `FIXEDASSETS-038-K30000-VENDOR-SETUP-FIT` ist erledigt; aktuell nur `FIXEDASSETS-039-K30000-VENDOR-CARD-DEFAULTS-READONLY`, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-038-K30000-VENDOR-SETUP-FIT` | done-labor | Vendor-Templates und Nummernserien koennen Auto-Number-Drafts erzeugen; Wiederholung koennte Zielkreditor oder Cleanup verwischen | ausgefuehrt in `FIXEDASSETS-038` | erledigt: `K30000` / `Zollspedition Nord GmbH` UI-first als Labor-Kreditor sichtbar; `V00030` wurde auf `K30000` umgesetzt, `V00020` per UI geloescht | `evidence/fixedassets-038/`, Screenshots `fixedassets-038-*`; keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Nicht wiederholen ohne neues Evidence-Delta; naechster No-Approval-Schritt ist `FIXEDASSETS-039-K30000-VENDOR-CARD-DEFAULTS-READONLY`; weiter keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-039-K30000-VENDOR-CARD-DEFAULTS-READONLY` | done-labor-partial | Sichtbare Zahlungsdefaults koennen faelschlich als Buchungsfreigabe gelesen werden | ausgefuehrt in `FIXEDASSETS-039` | erledigt: K30000-Kreditorenkarte read-only geoeffnet; `Payment Terms Code = 1M(8D)` und `Payment Method Code = BANK` sichtbar; Posting-/Currency-/Tax-Defaults nicht sichtbar | `evidence/fixedassets-039/`, Screenshots `fixedassets-039-*`; keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Folgeschritt `FIXEDASSETS-040` ist erledigt; weiter keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung |
| `FIXEDASSETS-040-K30000-VENDOR-DEFAULTS-DECISION` | done-decision | Ein Teilnachweis koennte faelschlich als Kaufbeleg-Gate gelesen werden | ausgefuehrt in `FIXEDASSETS-040` | erledigt: Kaufbeleg bleibt gesperrt; fehlende Vendor Posting Group, Gen. Bus. Posting Group, Currency Code und Tax/VAT-Defaults brauchen read-only UI-Feld-Diagnose | `evidence/fixedassets-040/`; kein BC-Lauf, keine Kreditor-Aenderung, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-041-K30000-VENDOR-DEFAULTS-FIELD-DIAGNOSIS-READONLY`; weiter kein Kaufbeleg und keine Buchung |
| `FIXEDASSETS-041-K30000-VENDOR-DEFAULTS-FIELD-DIAGNOSIS-READONLY` | done-labor-partial | Ein Screenshot mit sichtbarem Kreditor und Zahlungswerten koennte faelschlich als vollstaendiger Default-/Buchungsgruppenbeweis gelesen werden | ausgefuehrt in `FIXEDASSETS-041` | erledigt: K30000 Vendor Card read-only geoeffnet; `1M(8D)` und `BANK` sichtbar; Posting-/Currency-/Tax-Defaults im Bild nicht sichtbar; FastTab-Expansion nicht ausreichend bewiesen | `evidence/fixedassets-041/`, Screenshots `fixedassets-041-*`; keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-Aenderung | Naechster No-Approval-Schritt ist `FIXEDASSETS-042-K30000-VENDOR-DEFAULTS-VISIBILITY-DECISION`; weiter kein Kaufbeleg und keine Buchung |
| `FIXEDASSETS-042-K30000-VENDOR-DEFAULTS-VISIBILITY-DECISION` | done-decision | Aus einem Sichtbarkeitsproblem koennte faelschlich ein Setup-Fehler oder Kaufbeleg-Gate gemacht werden | ausgefuehrt in `FIXEDASSETS-042` | erledigt: kein Kaufbeleg und kein Setup-Fit; naechster Schritt ist ein read-only FastTab-Sichtbarkeitsnachweis mit Zielcaption-/Zielwert-Nachbedingung | `evidence/fixedassets-042/`; kein BC-Lauf, keine Kreditor-Aenderung, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung | Naechster No-Approval-Schritt ist `FIXEDASSETS-043-K30000-VENDOR-INVOICING-FASTTAB-VISIBILITY-READONLY`; weiter kein Kaufbeleg und keine Buchung |
| `FIXEDASSETS-043-K30000-VENDOR-INVOICING-FASTTAB-VISIBILITY-READONLY` | done-labor-partial | Sichtbare Tax-/Payment-Felder koennen faelschlich als Kaufbeleg- oder Buchungsgruppenfreigabe gelesen werden | ausgefuehrt in `FIXEDASSETS-043` | erledigt: K30000 Vendor Card read-only geoeffnet; `Invoicing` per kleinem `aria-expanded=false`-Control geoeffnet; `Tax Liable`, `Tax Area Code`, `1M(8D)` und `BANK` sichtbar; `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group` nicht sichtbar | `evidence/fixedassets-043/`, Screenshots `fixedassets-043-*`; keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-/Setup-Aenderung | Naechster No-Approval-Schritt ist `FIXEDASSETS-044-K30000-VENDOR-PURCHASE-INVOICE-GATE-DECISION`; weiter kein Kaufbeleg und keine Buchung |
| `FIXEDASSETS-044-K30000-VENDOR-PURCHASE-INVOICE-GATE-DECISION` | done-decision | Ein partieller FastTab-/Tax-Screenshot koennte faelschlich als Kaufbeleg-Preflight-Freigabe gelesen werden | ausgefuehrt in `FIXEDASSETS-044` | erledigt: Kaufbeleg-Preflight bleibt gesperrt; vor Einkaufsrechnung muessen die fehlenden Defaults per Personalisieren/Page Inspection oder spaeterem Setup-/Default-Gate geklaert werden | `evidence/fixedassets-044/`; kein BC-Lauf, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-/Setup-Aenderung | Naechster No-Approval-Schritt ist `FIXEDASSETS-045-K30000-VENDOR-PERSONALIZE-PAGEINSPECTION-DIAGNOSIS-READONLY`; weiter kein Kaufbeleg und keine Buchung |
| `FIXEDASSETS-045-K30000-VENDOR-PERSONALIZE-PAGEINSPECTION-DIAGNOSIS-READONLY` | done-labor-read-only | Diagnosewerkzeuge koennen faelschlich als Feld-/Setup-Fit gelesen werden | ausgefuehrt in `FIXEDASSETS-045` | erledigt: K30000 Vendor Card read-only geoeffnet; `Personalisieren` ist im Einstellungen-Pane sichtbar; keine Personalisierung gespeichert; Page Inspection Shortcut nicht stabil verfuegbar; kritische Defaults bleiben nicht sichtbar | `evidence/fixedassets-045/`, Screenshots `fixedassets-045-*`; keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-/Setup-Aenderung | Naechster No-Approval-Schritt ist `FIXEDASSETS-046-K30000-VENDOR-DEFAULTS-GATE-DECISION`; weiter kein Kaufbeleg und keine Buchung |
| `FIXEDASSETS-046-K30000-VENDOR-DEFAULTS-GATE-DECISION` | done-decision | Ein sichtbarer Personalisieren-Einstieg koennte faelschlich als Freigabe fuer geratenen Default-Fit oder Kaufbeleg gelesen werden | ausgefuehrt in `FIXEDASSETS-046` | erledigt: kein Setup-/Default-Fit freigegeben; fehlende Vendor Posting Group, Gen. Bus. Posting Group, Currency Code und VAT Bus. Posting Group muessen erst read-only sichtbar oder als nicht sichtbar belegt werden | `evidence/fixedassets-046/`; kein BC-Lauf, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-/Setup-Aenderung | Naechster No-Approval-Schritt ist `FIXEDASSETS-047-K30000-VENDOR-DEFAULTS-VISIBILITY-VALUE-DISCOVERY-READONLY`; weiter kein Kaufbeleg, keine Buchung und kein Default-Fit |
| `FIXEDASSETS-047-K30000-VENDOR-DEFAULTS-VISIBILITY-VALUE-DISCOVERY-READONLY` | done-labor-read-only | Tax-/Payment-Teilwerte koennen faelschlich als Kaufbeleg-Readiness gelesen werden | ausgefuehrt in `FIXEDASSETS-047` | erledigt: K30000 Vendor Card read-only geoeffnet; `Tax Area Code`, `Tax Liable`, `1M(8D)` und `BANK` sichtbar; kritische Einkaufsdefaults bleiben nicht sichtbar | `evidence/fixedassets-047/`, Screenshots `fixedassets-047-*`; keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-/Setup-Aenderung | Folgeschritt `FIXEDASSETS-048` ist erledigt; weiter kein Kaufbeleg, keine Buchung und kein Default-Fit |
| `FIXEDASSETS-048-K30000-VENDOR-DEFAULTS-MANUAL-PERSONALIZE-OR-SETUP-GATE-DECISION` | done-decision | Ein fehlendes Feld koennte zu geratenem Setup-Fit statt Sichtbarkeitsdiagnose fuehren | ausgefuehrt in `FIXEDASSETS-048` | erledigt: kein Default-/Setup-Fit freigegeben; naechster Schritt ist nur Personalisieren-Feldverfuegbarkeit read-only | `evidence/fixedassets-048/`; kein BC-Lauf, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-/Setup-Aenderung | Naechster No-Approval-Schritt ist `FIXEDASSETS-049-K30000-VENDOR-PERSONALIZE-FIELD-AVAILABILITY-READONLY`; weiter kein Kaufbeleg und keine Buchung |
| `FIXEDASSETS-049-K30000-VENDOR-PERSONALIZE-FIELD-AVAILABILITY-READONLY` | done-labor-read-only | Geoeffneter Personalisieren-Modus koennte faelschlich als Feld- oder Default-Beweis gelesen werden | ausgefuehrt in `FIXEDASSETS-049` | erledigt: `Personalisieren` oeffnet `Wird personalisiert: Vendor Card` fuer K30000, aber der Feldlisten-/`Add field`-Pfad ist nicht stabil sichtbar und keine kritische Default-Caption erscheint | `evidence/fixedassets-049/`, Screenshots `fixedassets-049-*`; keine gespeicherte Personalisierung, keine Einkaufsrechnung, kein Zugang, keine AfA, keine Buchung, keine Kreditor-/Setup-Aenderung | Naechster No-Approval-Schritt ist `FIXEDASSETS-050-K30000-VENDOR-DEFAULTS-PAGEINSPECTION-OR-SETUP-GATE-DECISION`; weiter kein Kaufbeleg, keine Buchung und kein Default-Fit |
| `O2C-NEW-LAB-POSTING` | autonomous-allowed | Ein neuer O2C-Beleg erzeugt neue Nebenbuch-, Sach-, Wert- und Artikelposten | Autopilot V2.2 / `GOVERNANCE-005` | nur einen fachlich neuen, dokumentierten O2C-Laborfall, nicht `UAT-O2C-001` wiederholen | neuer Zweck, Testdaten, Preview Posting, Post-Dialog, gebuchter Beleg, Postenspur, Cleanup-/No-Doppelbuchungsregel | ohne neuen Evidence-Zweck `PS-INV103297` read-only nutzen |
| `P2P-NEW-LAB-POSTING` | autonomous-allowed | Ein neuer P2P-Beleg erzeugt neue Kreditoren-, Sach-, Wert- und Artikelposten | Autopilot V2.2 / `GOVERNANCE-005` | nur einen fachlich neuen, dokumentierten P2P-Laborfall, nicht `108219` wiederholen | neuer Zweck, Testdaten, Preview Posting, Post-Dialog, gebuchter Beleg, Postenspur, Steuer-/Kontengrenze | ohne neuen Evidence-Zweck `108219` read-only nutzen |
| `INVENTORY-JOURNAL-LAB-POSTING` | autonomous-allowed | Item-Journal-Buchungen veraendern Bestand, Wertposten und Sachposten | Autopilot V2.2 / `GOVERNANCE-005` | nur einen neuen dokumentierten Lager-/Inventory-Laborfall nach Journal-Check und Zielbestandsplan, nicht `INV008-899959` wiederholen | Zielwerte, Dimension, Journal Check, Buchungsdialog, Item/Value/G/L Entries, Inventory Valuation | ohne neuen Evidence-Zweck `INV008-899959` read-only nutzen |
| `FIXEDASSETS-LAB-POSTING` | autonomous-allowed | Anlagenbuchungen veraendern Anlagen- und Sachposten | Autopilot V2.2 / `GOVERNANCE-005` | nur nach eigener Readiness: Zielanlage, AfA-Buch, FA Posting Group, Kreditor/Journalpfad und Preview/Postenspur muessen vorher UI-first tragen | Setup-Readiness, Preview soweit verfuegbar, FA Ledger Entries, G/L Entries, Vendor Entries bei Einkauf | `FA-CNC-01`, `HGB` und `MACHINES` sind als Laborstammdaten sichtbar; `K30000` darf erst im naechsten Schritt als Kreditor-Stammdatum gefittet werden. Keinen Anlagenprozess buchen, bis Kreditorenkarte, Einkaufsrechnung/Preview und eigenes Posting-Gate belegt sind |
| `SERVICE-LAB-POSTING` | autonomous-allowed | Servicebuchungen koennen Artikel-, Ressourcen-, Service- und Rechnungsfolgen erzeugen | Autopilot V2.2 / `GOVERNANCE-005` | nur nach UI-first Setup-Readiness fuer Serviceartikel, Ersatzteil, Ressource, Lager und Serviceauftrag | Zielobjekte, Preview/Pruefung, Service-/Item-/Resource-/G/L-/Invoice-Spur | solange Zielobjekte fehlen, nur Readiness oder Setup-Fit mit eigenem Nachweis |
| `PROJECTS-LAB-POSTING` | autonomous-allowed | Projektbuchungen koennen Projektposten, WIP und Rechnungsfolgen erzeugen | Autopilot V2.2 / `GOVERNANCE-005` | nur nach UI-first Setup-Readiness fuer Projekt, Aufgaben, Ressource, Material, Lager und Fakturapfad | Projektkarte, Planzeilen/Journal, Pruefung/Preview soweit verfuegbar, Job Ledger Entries, WIP/G/L/Rechnung | solange Zielobjekte fehlen, nur Readiness oder Setup-Fit mit eigenem Nachweis |
| `MANUFACTURING-LAB-POSTING` | autonomous-allowed | Fertigungs-/Montagebuchungen koennen Verbrauch, Output, Kapazitaet, Wert- und Sachposten erzeugen | Autopilot V2.2 / `GOVERNANCE-005` | nur nach UI-first Setup-Readiness fuer BOM/Routing oder Assembly-Struktur, Komponenten, Auftrag, Verbrauch/Output und Kostenpfad | BOM/Routing/Order, Preflight, Consumption/Output, Item/Value/Capacity/G/L Entries | solange BOM/Routing/Assembly-Struktur fehlt, keine Manufacturing-Buchung |
| `WAREHOUSE-001-ACTIVATION` | locked | Lagerort-/Bin-/Warehouse-Setup veraendert spaetere Lagerprozesse | keine aktuelle Freigabe | Bins, Warehouse Receipts, Put-aways, Picks oder Location-Aktivierung einrichten | Setup-Screenshots, Prozessnachweis, Rueckwirkung auf `FRA-ZL` | `WAREHOUSE-001` Readiness und `WAREHOUSE-002` Buch-Sync sind erledigt; ohne Freigabe andere read-only Vorbereitung |
| `MANUFACTURING-001-POSTING` | locked | Verbrauch, Output, Kapazitaet und Kostenposten entstehen | keine aktuelle Freigabe | kontrollierte Fertigungs-/Montagebuchung | Item/Value/Capacity/G/L Entries, Kostenwirkung | `MANUFACTURING-001` Readiness und `MANUFACTURING-002` Buch-Sync sind erledigt; ohne Freigabe anderer read-only Block |
| `SERVICE-001-POSTING` | locked | Serviceauftrag kann Artikel-, Ressourcen- und Rechnungsfolgen erzeugen | keine aktuelle Freigabe | kontrollierte Servicebuchung | Servicebeleg, Item/Resource/G/L Entries, Rechnungsspur | `SERVICE-001` Readiness und `SERVICE-002` Buch-Sync sind erledigt; ohne Freigabe kein Service-Setup und keine Servicebuchung |
| `PROJECTS-001-POSTING` | locked | Projektposten, WIP oder Faktura koennen entstehen | keine aktuelle Freigabe | kontrollierte Projektbuchung oder Faktura | Job Ledger Entries, WIP/G/L, Rechnungsspur | `PROJECTS-001` Readiness ist erledigt; ohne Freigabe nur Buch-Sync oder read-only Folgearbeit |
| `DROPSHIPPING-001-PROCESS` | locked | Verkaufsauftrag, Einkaufsbestellung, Belegverknuepfung und spaetere Verkaufs-/Einkaufsbuchung koennen Posten erzeugen und Margen-/Steuerbefunde verwischen | keine aktuelle Freigabe | Debitor `D11000`, Kreditor `K20000`, Artikel `SP-PUMP-01`, Purchasing Code/Drop Shipment, Auftrag `DS-24001`, Bestellung und spaetere Preview/Buchung nur mit eigenem Gate | Vorher/Nachher-Stammdaten, Auftrag, Bestellung, Preview, Postenspur, Lager-Negativnachweis, Buchwirkung | `DROPSHIPPING-001` Readiness ist erledigt; ohne Freigabe nur Buch-Sync oder weitere read-only Vorbereitung |
| `INTERCOMPANY-001-PROCESS` | locked | Company-Wechsel, IC-Partner, IC Inbox/Outbox, EU-/Auslandsteuer und IC-Belege koennen mehrere Companies und Abstimmungslogik veraendern | keine aktuelle Freigabe | IC-Partner-/Mehr-Company-/Steuer-Setup, Beleg `IC-7001`, IC Outbox senden, IC Inbox annehmen, Partnerbeleg/Preview/Buchung nur mit eigenem Gate | Zielcompanies, IC Partner, Debitoren, Steuer-/Waehrungsfit, IC Inbox/Outbox, Partnerbeleg, Postenspur, IC-Abstimmung | `INTERCOMPANY-001` Readiness ist erledigt; ohne Freigabe nur Buch-Sync oder andere read-only Vorbereitung |
| `NEW-COMPANY-001` | autonomous-allowed-instance-scoped | Neue Company erzeugt Umgebungsdrift, Setup-Streuung und Handover-Risiko; ausserhalb der Instanz waere es kritisch | `GOVERNANCE-012-COMPANY-AUTONOMY-REGISTRY` und V5-Instanzgrenze | innerhalb `MCP_1_20260210` Company-Liste read-only pruefen, Company wechseln oder neue Labor-Company anlegen, wenn Registry-Eintrag, Zweck, Risiko, Evidence-Plan und Rueckfalllogik vorher dokumentiert sind | Company Registry, Company-Context-Screenshot, Vorher/Nachher, Quelle/Vorlage, Buchkapitel, Setup-Startzustand, Handover | sofort stoppen, wenn eine andere Instanz/Produktivumgebung sichtbar wird; keine Company-Aktion ohne Registry-Eintrag |
| `O2C-REPOST-001` | locked | zweite O2C-Buchung wuerde `PS-INV103297` als Referenz verwischen | keine aktuelle Freigabe | zweiter O2C-Laborbeleg nur mit neuem Zweck | neue Belegnummer, Preview, Postenspur, warum Wiederholung noetig war | ohne Freigabe `PS-INV103297` read-only nutzen |
| `P2P-REPOST-001` | locked | zweite P2P-Buchung wuerde `108219` als Referenz verwischen | keine aktuelle Freigabe | zweiter P2P-Laborbeleg nur mit neuem Zweck | neue Belegnummer, Preview, Postenspur, warum Wiederholung noetig war | ohne Freigabe `108219` read-only nutzen |
| `INVENTORY-REPOST-INV008` | locked | zweite Inventory-Buchung wuerde Zielbestand und Valuation veraendern | keine aktuelle Freigabe | zweite Inventory-/Target-Stock-Buchung nur mit neuem Zweck | neue Dokumentnummer, Item/Value/G/L Entries, Inventory Valuation | ohne Freigabe `INV008-899959` read-only nutzen |

## Statusregeln

- `locked`: keine Setup-Aenderung oder Buchung.
- `autonomous-allowed`: autonomer Lauf darf nur den exakt beschriebenen Laborfall ausfuehren, nachdem die frischen UI-Vorbedingungen im selben Lauf belegt wurden; bei jeder Abweichung abbrechen und Evidence schreiben.
- `autonomous-allowed-instance-scoped`: autonomer Lauf darf die dokumentierte Company-Aktion nur innerhalb `MCP_1_20260210` ausfuehren; Registry-Eintrag und Evidence-Plan sind Pflicht vor dem Klick.
- `approved-for-next-run`: Freigabe gilt nur fuer genau einen folgenden Lauf und muss danach entfernt oder auf `done-labor`/`locked` gesetzt werden.
- `done-labor`: kontrollierte Laboraktion wurde ausgefuehrt und mit Evidence belegt; Wiederholung bleibt gesperrt.
- `rejected`: der Pfad wurde versucht und als nicht tragfaehig dokumentiert.

## Aktuelle Konsequenz

`FIXEDASSETS-048-K30000-VENDOR-DEFAULTS-MANUAL-PERSONALIZE-OR-SETUP-GATE-DECISION` ist erledigt. Ein Default-/Setup-Fit fuer `K30000` wird nicht freigegeben, weil `FIXEDASSETS-047` die kritischen Felder weiter nicht sichtbar belegt. Naechster No-Approval-Schritt ist `FIXEDASSETS-049-K30000-VENDOR-PERSONALIZE-FIELD-AVAILABILITY-READONLY`: `Personalisieren` nur diagnostisch nutzen, Feldverfuegbarkeit pruefen und keine Kreditor-/Setupdaten aendern. Weiter kein Kaufbeleg, kein Anlagenzugang, keine AfA und keine Buchung.

`FIXEDASSETS-047-K30000-VENDOR-DEFAULTS-VISIBILITY-VALUE-DISCOVERY-READONLY` ist erledigt. Der read-only UI-Lauf bestaetigt die Teilwerte `Tax Liable`, `Tax Area Code`, `Payment Terms Code = 1M(8D)` und `Payment Method Code = BANK`, aber `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group` sind weiterhin nicht sichtbar belegt. Das gibt keine Kaufbeleg-, Anlagenzugangs-, AfA- oder Buchungsfreigabe und keine Setup-Aenderungsfreigabe. Naechster No-Approval-Schritt ist `FIXEDASSETS-048-K30000-VENDOR-DEFAULTS-MANUAL-PERSONALIZE-OR-SETUP-GATE-DECISION`: ohne BC-Aenderung entscheiden, ob ein manueller Personalisieren-Sichtbarkeitspfad oder ein enger UI-first Default-/Setup-Fit als naechster Schritt erlaubt wird.

`FIXEDASSETS-044-K30000-VENDOR-PURCHASE-INVOICE-GATE-DECISION` ist erledigt. Der 043-Teilnachweis reicht nicht fuer einen Kaufbeleg-Preflight, weil `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group` nicht sichtbar belegt sind. Naechster No-Approval-Schritt ist `FIXEDASSETS-045-K30000-VENDOR-PERSONALIZE-PAGEINSPECTION-DIAGNOSIS-READONLY`: fehlende Defaults read-only als UI-Verfuegbarkeits- oder technische Page-/Feldfrage klaeren. Einkaufsrechnung, Anlagenzugang, AfA, Kreditor-/Setup-Aenderung und Anlagenbuchung bleiben gesperrt.

`FIXEDASSETS-042-K30000-VENDOR-DEFAULTS-VISIBILITY-DECISION` ist erledigt. Die fehlenden K30000-Defaults werden weiterhin als Sichtbarkeits-/FastTab-Diagnose behandelt, nicht als bewiesener Setup-Fehler. Naechster No-Approval-Schritt ist `FIXEDASSETS-043-K30000-VENDOR-INVOICING-FASTTAB-VISIBILITY-READONLY`: read-only die K30000-Kreditorenkarte oeffnen und FastTabs nur mit sichtbarer Zielcaption oder sichtbarem Zielwert als aufgeklappt akzeptieren. Einkaufsrechnung, Anlagenzugang, AfA, Kreditor-/Setup-Aenderung und Anlagenbuchung bleiben gesperrt.

`FIXEDASSETS-041-K30000-VENDOR-DEFAULTS-FIELD-DIAGNOSIS-READONLY` ist erledigt, aber nur partiell. `K30000` ist als RM-DEMO-Labor-Kreditor sichtbar, und `Payment Terms Code = 1M(8D)` sowie `Payment Method Code = BANK` sind sichtbar. Das gibt keine Buchungsfreigabe, weil `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und Tax/VAT-Defaults im Bild weiter nicht sichtbar sind und die FastTab-Expansion nicht ausreichend bewiesen ist. Der Folgeschritt `FIXEDASSETS-042` ist inzwischen erledigt; weiter kein Kaufbeleg und keine Buchung.

`FIXEDASSETS-038-K30000-VENDOR-SETUP-FIT` ist erledigt: `K30000` / `Zollspedition Nord GmbH` ist als RM-DEMO-Labor-Kreditor sichtbar, Auto-Number-Draft `V00030` wurde zum Zielcode umgesetzt und `V00020` wurde per UI bereinigt. Das gibt keine Buchungsfreigabe. Naechster No-Approval-Schritt ist `FIXEDASSETS-039-K30000-VENDOR-CARD-DEFAULTS-READONLY`; Einkaufsrechnung, Anlagenzugang, AfA und Anlagenbuchung bleiben gesperrt.

`GOVERNANCE-012-COMPANY-AUTONOMY-REGISTRY` ersetzt die alte pauschale Sperre "keine neue Company" fuer die Instanz `MCP_1_20260210` durch eine kontrollierte Registry-Regel. Company-Liste read-only pruefen, Company wechseln oder eine neue Labor-/Buchcompany anlegen ist innerhalb dieser Instanz erlaubt, wenn vorher Registry-Eintrag, Zweck, Risiko, Evidence-Plan und Rueckfalllogik dokumentiert sind. Ausserhalb `MCP_1_20260210` bleibt alles hart gesperrt. `GOVERNANCE-013-COMPANY-LIST-READONLY` ist erledigt: sichtbar sind `CRONUS USA, Inc.`, `My Company` und `RM-DEMO`; `RM-PROD`, `RM-SALES`, `RM-SERVICE`, `RM-SHARED` und `RM-AT` sind planned-only/nicht sichtbar. Es wurde keine Company gewechselt, angelegt oder geaendert.

`FIXEDASSETS-034-FA-CNC-01-SUBCLASS-FIELD-DIAGNOSIS` ist erledigt. `FA Subclass Code = EQUIPMENT` ist auf der bestehenden `FA-CNC-01`-Karte sichtbar und persistent belegt. Zusammen mit `FIXEDASSETS-033` ist `FA-CNC-01` als CRONUS-USA-Labor-Stammdatum gefittet: Beschreibung, Klasse, Unterklasse, `HGB`, `MACHINES`, `Book Value = 0,00`, AfA-Start `01.01.2026`, `8,00` Jahre und AfA-Ende `31.12.2033` sind belegt. Das oeffnet keine Buchungsfreigabe: `K30000`, Einkaufsrechnung, Zugang, AfA und jede Anlagenbuchung bleiben gesperrt. Der naechste praktische Schritt ist `FIXEDASSETS-035-K30000-VENDOR-READINESS-DECISION`; ein neuer Anlagenzielcode ist nicht noetig.

`FIXEDASSETS-008` hat Kapitel 21 mit der Readiness-Kette `FIXEDASSETS-005` bis `FIXEDASSETS-007` abgeglichen. Ohne ausdrueckliche Freigabe bleibt Fixed Assets danach fuer Setup und Buchung gesperrt. `FIXEDASSETS-006` hat vorhandene CRONUS-Konten in `FA Posting Groups` gelesen; `FIXEDASSETS-007` hat `Depreciation Books` und `FA Classes` gelesen. Diese Schritte sollen nicht wiederholt werden, solange kein neuer Hebel vorliegt.

`WAREHOUSE-001` hat `FRA-ZL` und Warehouse-Einstiegspfade read-only geprueft; `WAREHOUSE-002` hat Kapitel 13 mit diesem Befund synchronisiert. Ohne ausdrueckliche Freigabe bleibt Warehouse-Aktivierung gesperrt: keine Bins anlegen, keine Lagerortfelder aktivieren, keine Warehouse Receipts/Put-aways/Picks/Shipments erzeugen oder buchen.

`MANUFACTURING-001` hat Manufacturing-/Assembly-Einstiege und Zielartikel read-only geprueft. Sichtbar sind Planning Worksheet, Production BOMs, Routings, Released Production Orders, Consumption Journal und Output Journal als Tell-Me-Einstiege sowie `RM-M100` und `RAW-STEEL` als Artikel. Nicht sichtbar sind `Assembly Orders` als stabiler Suchtreffer sowie die geplanten Artikel `COMP-CTRL` und `KIT-MAINT`; sichtbare BOM-/Routing-Marker auf den Artikelkarten wurden nicht nachgewiesen. `MANUFACTURING-002` hat Kapitel 14 mit diesem Laborstand synchronisiert und `INV008-899959` ausdruecklich als Trainingsbestand statt Manufacturing-Output markiert. Ohne ausdrueckliche Freigabe bleibt jede Fertigungs-/Montageeinrichtung und jede Buchung gesperrt. Manufacturing ist ohne Gate vorerst abgeschlossen.

`SERVICE-001` hat Service-Einstiege und Zielobjekte read-only geprueft. Tell-Me zeigt `Service Orders`, `Service Items`, `Resources`, `Service Management Setup`, `Service Contracts` und `Service Ledger Entries`; `D10000` ist sichtbar. Die konkreten Zielobjekte `RM-M100-SN1001`, `SP-PUMP-01`, `RES-TECH` und `VAN-SERV` sind im gefilterten Laborlauf nicht als Nummern sichtbar. `SERVICE-002` hat Kapitel 15 mit diesem Laborstand synchronisiert: sichtbare Service-Seiten sind nur Readiness, die Schrittfolge `SERV-4001` bleibt Zielpfad. Ohne ausdrueckliche Freigabe bleibt Service-Setup und jede Servicebuchung gesperrt: keinen Serviceartikel anlegen, kein Ersatzteil anlegen oder verbrauchen, keine Ressource anlegen oder erfassen, keinen Serviceauftrag `SERV-4001`, keine Servicerechnung.

`PROJECTS-001` hat Project-/Job-Einstiege und Zielobjekte read-only geprueft. Tell-Me zeigt `Projects`, `Project Planning Lines`, `Project Journals`, `Project Ledger Entries`, `Project Statistics` und `Project WIP`; `D10000` ist sichtbar. Die konkreten Zielobjekte `PROJ-5001`, `RES-TECH`, `SP-SENSOR-02` und `PROJ-LAG` sind im gefilterten Laborlauf nicht als Nummern sichtbar. `PROJECTS-002` hat Kapitel 16 damit synchronisiert. Ohne ausdrueckliche Freigabe bleibt Project-Setup und jede Projektbuchung gesperrt: kein Projekt anlegen, keine Projektaufgaben oder Planzeilen anlegen, kein Projektjournal, keine WIP-Berechnung, keine Meilensteinrechnung.

`DROPSHIPPING-001` hat Kapitel 17 read-only ohne Shopify-Scope geprueft. Tell-Me zeigt `Sales Orders`, `Purchase Orders`, `Requisition Worksheets` und `Purchasing Codes` als Einstiegspfade; `Drop Shipments` wurde in diesem Lauf nicht als stabiler Tell-Me-Treffer erreicht. Die konkreten Zielobjekte `D11000`, `K20000` und `SP-PUMP-01` sind in gefilterten Listen nicht sichtbar. `DROPSHIPPING-002` hat diese Grenze in Kapitel 17 und der Projektwahrheit synchronisiert. Ohne ausdrueckliche Freigabe bleibt Dropshipping/Sonderverkauf fuer Setup und Prozess gesperrt: keinen Debitor/Kreditor/Artikel anlegen, keinen Auftrag `DS-24001`, keine Einkaufsbestellung, keine Requisition-Worksheet-Aktion, keine Preview, keine Verkaufs-/Einkaufsbuchung. Dropshipping-Setup braucht ein eigenes Gate; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`INTERCOMPANY-001` hat Kapitel 18 read-only geprueft. Tell-Me zeigt `Intercompany Setup`, `IC Partners`, `IC Inbox Transactions`, `IC Outbox Transactions` und `VAT Entries` als Einstiegspfade; `Currencies` wurde in diesem Lauf nicht als stabiler Tell-Me-Treffer erreicht. Die Zieldebitoren `D20000`, `D30000` und `D90000` sind in gefilterten Customer-Listen nicht sichtbar. `INTERCOMPANY-002` hat Kapitel 18 damit synchronisiert: `IC-7001` ist Zielpfad, nicht Labor-Endstand. Ohne ausdrueckliche Freigabe bleibt Intercompany/Ausland fuer Setup und Prozess gesperrt: keine Company anlegen oder wechseln, keine IC-Partner anlegen, keine EU-/IC-/VAT-Setup-Aenderung, kein Beleg `IC-7001`, keine IC Outbox senden, keine IC Inbox annehmen, keine Preview und keine Buchung. `TRAINING-001`, `MB800-001`, `PAGESINDEX-001`, `ARTIFACTS-001`, `REPORTING-013` und `REPORTING-014` sind inzwischen erledigt; ohne neues Gate ist der naechste sichere Schritt `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`COMPLIANCE-001` hat Kapitel 22 read-only geprueft. Tell-Me-/Role-Center-Kontexte fuer `E-Rechnungen`, `VAT Entries`, `VAT Posting Setup`, `Document Sending Profiles`, `Change Log Entries` und `Change Log Setup` sind sichtbar/kontextuell sichtbar. `COMPLIANCE-002` hat Kapitel 22 damit synchronisiert: Das ist Readiness-Evidence, kein E-Rechnungsversand, keine Validierung, kein Peppol-/Providerstatus, kein Archivnachweis, keine Change-Log-Aktivierung und kein deutscher VAT19-/GoBD-Finalnachweis. Ohne ausdrueckliche Freigabe bleibt Compliance-/E-Rechnungs-Setup gesperrt: keine VAT-Aenderung, keine E-Documents-/Provider-/Versandprofil-Einrichtung, keine Change-Log-Aktivierung, kein Versand, keine Validierung und keine Buchung. `TRAINING-001`, `MB800-001`, `PAGESINDEX-001`, `ARTIFACTS-001`, `REPORTING-013` und `REPORTING-014` sind inzwischen erledigt; ohne neues Gate ist der naechste sichere Schritt `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`SECURITY-001` hat Kapitel-27-/Admin-Kontexte read-only geprueft. `Permission Sets`, `Profiles Roles`, `Security Groups`, `User Setup`, `Job Queue Entries` und `Change Log Entries` sind sichtbar beziehungsweise kontextuell sichtbar; `Users` war in diesem Tell-Me-Lauf nicht stabil als Zielkontext sichtbar. `SECURITY-002` hat Kapitel 27 mit diesem Befund synchronisiert. Das ist Navigations- und Readiness-Evidence, kein Rechtekonzept, kein SoD-Nachweis und keine Audit-/Betriebsfreigabe. Ohne ausdrueckliche Freigabe bleibt jedes Benutzer-, Berechtigungs-, Profil-/Rollen-, Security-Group-, User-Setup-, Job-Queue- oder Change-Log-Setup gesperrt. `TRAINING-001`, `MB800-001`, `PAGESINDEX-001` und `ARTIFACTS-001` sind inzwischen erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`MIGRATION-001` hat Kapitel 28 ohne BC-Lauf synchronisiert. Migration, Opening Balances und Cutover sind jetzt getrennte Prueffelder; `INV008-899959` bleibt Trainings-/Opening-Balance-Laborlogik und kein produktiver Migrationsnachweis. Ohne ausdrueckliche Freigabe bleibt Migration fuer praktische Ausfuehrung gesperrt: kein Configuration Package befuellen, kein Import, keine neue Company, keine Opening-Balance-Buchung und kein Cutover-Schritt. `TRAINING-001`, `MB800-001`, `PAGESINDEX-001` und `ARTIFACTS-001` sind inzwischen erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`INTEGRATIONS-001` hat Kapitel 29 ohne BC-Lauf synchronisiert. Integrationen, Extensions, AppSource, APIs/Web Services, Power Platform und Power BI sind jetzt als Zielbild und Gate-Thema getrennt. Ohne ausdrueckliche Freigabe bleibt praktische Integration gesperrt: keine Extension installieren, keine AppSource-App pilotieren, keine API/Web Services einrichten, keinen Connector autorisieren, kein Power-Platform-/Power-BI-Setup aendern, keine Job Queue fuer Integration anlegen/starten und keinen produktiven Datenaustausch ausfuehren. `TRAINING-001`, `MB800-001`, `PAGESINDEX-001` und `ARTIFACTS-001` sind inzwischen erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`OPERATIONS-001` hat Kapitel 30 ohne BC-Lauf synchronisiert. Betrieb, Monitoring und Hypercare sind jetzt als Zielbild und Gate-Thema getrennt. Ohne ausdrueckliche Freigabe bleibt praktische Operations-Aenderung gesperrt: keine Job Queue anlegen, starten oder umplanen, keine Job-Queue-Benachrichtigung einrichten, keine Telemetrie-/Application-Insights- oder Admin-Center-Aenderung, keinen Monitoring-Connector einrichten und keine Produktivumgebung anfassen. `TRAINING-001`, `MB800-001`, `PAGESINDEX-001` und `ARTIFACTS-001` sind inzwischen erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`SOLUTIONARCHITECT-001` hat Kapitel 31 ohne BC-Lauf synchronisiert. Solution-Architect-Denken ist jetzt als Entscheidungsrahmen aus Standardnachweis, Fit-Gap, UAT, Risiko, Owner, Rollback und Betriebsfolge markiert. Ohne ausdrueckliche Freigabe bleibt jede praktische Architekturumsetzung gesperrt: keine AL-/Extension-Entwicklung, keine AppSource-Installation, keine API-/Connector-/Power-Platform-/Power-BI-Einrichtung, keine produktive Architekturentscheidung, kein ADR mit Umsetzungswirkung, keine Setup-Aenderung und keine Buchung. `TRAINING-001`, `MB800-001`, `PAGESINDEX-001` und `ARTIFACTS-001` sind inzwischen erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`UAT-001` hat Kapitel 32 ohne BC-Lauf synchronisiert. Die Master-UAT-Tabelle ist jetzt als Zielbibliothek gegen vorhandene Labor-Evidence eingeordnet: O2C, P2P und Inventory liefern Laborbausteine; Payments, Reporting und mehrere Folgeprozesse bleiben Readiness, Teilbefund oder Gate-Folgearbeit. Ohne ausdrueckliche Freigabe bleibt praktische UAT-Ausfuehrung gesperrt, sobald sie Buchung, Zahlung, Setup, neue Company, Integration, Security, Migration oder Operationswirkung ausloesen koennte.

`TRAINING-001` hat Kapitel 33 ohne BC-Lauf synchronisiert. Uebungen und Loesungen sind jetzt als Trainingsbibliothek gegen vorhandene Evidence eingeordnet: O2C, P2P und Inventory liefern Labormuster; Payments und Reporting bleiben Readiness beziehungsweise Teil-/Negativbefund. Ohne ausdrueckliche Freigabe bleibt jede praktische Uebungsausfuehrung gesperrt, sobald sie Buchung, Zahlung, Setup, neue Company, Integration, Security, Migration, Operationswirkung oder einen neuen UAT-Lauf ausloesen koennte. `PAYMENTS-012` ist erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`MB800-001` hat Kapitel 34 ohne BC-Lauf synchronisiert. Die MB-800-Kompetenzmatrix ist jetzt als Lern-/Readiness-Mapping gegen Microsoft Learn und vorhandene Evidence eingeordnet, nicht als Zertifizierungsnachweis, nicht als deutsche Final-Evidence und nicht als vollstaendige praktische Kompetenzfreigabe. Ohne ausdrueckliche Freigabe bleibt jede praktische Pruefungssimulation oder Uebungsausfuehrung gesperrt, sobald sie Buchung, Zahlung, Setup, neue Company, Integration, Security, Migration oder Operationswirkung ausloesen koennte. `PAYMENTS-012` ist erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`LEARNPATH-001` hat Kapitel 35 ohne BC-Lauf synchronisiert. Das Microsoft-Learn-Lernpfad-Mapping ist jetzt als Lernlandkarte gegen offizielle Microsoft-Learn-Quellen, den MB-800-Study-Guide und vorhandene Evidence eingeordnet, nicht als Abschluss von Learn-Modulen, nicht als Zertifizierungsnachweis, nicht als deutsche Final-Evidence und nicht als praktische Prozessfreigabe. Ohne ausdrueckliche Freigabe bleibt jede praktische Pruefungssimulation, Uebungsausfuehrung oder Setup-/Buchungswirkung gesperrt. `PAYMENTS-012` ist erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`EXAMTRAINING-001` hat Kapitel 36 ohne BC-Lauf synchronisiert. Das MB-800-Pruefungstraining ist jetzt als Pruefungsfallen-Katalog gegen Kapitel 34/35, den Microsoft-Learn-Study-Guide und vorhandene Evidence eingeordnet, nicht als Pruefungssimulation, nicht als bestandene Pruefung, nicht als Zertifizierungsnachweis, nicht als deutsche Final-Evidence und nicht als praktische Prozessfreigabe. Ohne ausdrueckliche Freigabe bleibt jede praktische Pruefungssimulation, Uebungsausfuehrung oder Setup-/Buchungswirkung gesperrt. `PAYMENTS-012` ist erledigt; naechster sicherer Entscheidungsblock ist `GOVERNANCE-008-NEXT-READINESS-DECISION`.

`ARTIFACTS-001` hat Kapitel 39 ohne BC-Lauf synchronisiert. Projektartefakte, Templates, Handover-Dateien und Repo-QA sind jetzt als Kontroll- und Uebergabeschicht eingeordnet, nicht als praktischer Prozess-, Screenshot-, Buchungs- oder deutscher Finalnachweis. Ohne ausdrueckliche Freigabe bleibt jede praktische Ausfuehrung gesperrt, sobald sie Buchung, Zahlung, Setup, neue Company, Integration, Security, Migration oder Operationswirkung ausloesen koennte.

`SOURCES-001` hat Kapitel 40 ohne BC-Lauf synchronisiert. Quellen sind jetzt als Standardreferenz, amtliche Steuer-/Compliance-Quelle, optionale Vendor-Dokumentation, gestrichener Scope-Hinweis oder konkrete Projekt-Evidence eingeordnet. Quellen ersetzen keine RM-DEMO-Klickpfade, keine Screenshots, keine Postenspur und keinen deutschen Finalnachweis. `Q10` Shopify bleibt nur Scope-Abgrenzung und reaktiviert keinen Shopify-Test.

`PAYMENTS-011` hat die erste autonome Laborzahlung tatsaechlich ausgefuehrt: `PAY011-PS103297` wurde in `RM-DEMO` UI-first fuer `D10000` / `PS-INV103297` gebucht. Der offene Rechnungsrest ist im Debitorenposten mit `Remaining Amount = 0,00` und `Applied Entries = 1` sichtbar; detaillierte Debitorenposten zeigen `Initial Entry`, `Payment Discount` und `Application`; Sachposten zeigen `15110`, `18200` und `40910`. `PAYMENTS-013` hat danach den belastbaren Bank-Account-Ledger-Nachweis ueber Page `372` erbracht: sichtbar sind `PAY011-PS103297`, `BANK-RM-01`, Betrag `67.673,60`, Entry No. `4995` und Related G/L Entries; der alte Page-371-Pfad bleibt rejected. `PAYMENTS-014` hat Kapitel 20, Coverage, Findings und Evidence-Pack damit synchronisiert. Es wurde keine Bankabstimmung ausgefuehrt. `PAYMENTS-011-LAB-PAYMENT` steht deshalb auf `done-labor` und darf nicht wiederholt werden. `PAYMENTS-012`, `PAYMENTS-013` und `PAYMENTS-014` sind erledigt.

`GOVERNANCE-006` hat den ersten Analysis-View-Hebel freigegeben; `REPORTING-011` hat ihn als rejected geschlossen und `REPORTING-012` hat den Blocker synchronisiert. `GOVERNANCE-007` gab danach genau einen Feldmapping-/Setup-Lauf frei. `REPORTING-013` hat dieses Gate verbraucht: `Analysis Views` ist in `RM-DEMO` erreichbar, die bestehende `REVENUE`-Karte belegt Feldpositionen fuer `Code`, `Name`, `Dimension 1 Code` und `Dimension 2 Code`, aber `RM-PLCH` wurde nicht angelegt oder geaendert, weil `New/Neu` in der BC-Shell global mehrdeutig ist und ein ungescopter Klick in den Role-Center-Kontext fallen kann. `REPORTING-014` hat diese Wahrheit synchronisiert. `GOVERNANCE-008` waehlt danach ohne BC-Lauf den naechsten sicheren No-Approval-Schritt: `TAX-002-DE-VAT-GATE-READINESS`. `TAX-002` hat diese Gate-Readiness jetzt ohne BC-Lauf erledigt: Freigabekriterien, Stop-Kriterien und der Vorschlag `TAX-002A`/`TAX-002B`/`TAX-002C` sind unter `evidence/tax-002/` dokumentiert. `GOVERNANCE-009` synchronisiert danach die Autopilot-V2.2-Policy mit `AUTOPILOT-STATE.json`. `BOOK-O2C-FOUNDATION-DRIFT-SYNC` und `BOOK-REPORTING-UAT-K25-SYNC` haben danach Kapitel 10/25 ohne BC-Lauf synchronisiert: `UAT-K25-001` ist Ziel-UAT, aktueller RM-DEMO-Anker bleibt `PS-INV103297` plus Artikelposten `792`, und Financial-Reports-Summenwirkung bleibt offen. Status `rejected` fuer Analysis-View-Setup, `gate-readiness-done` fuer VAT19, `policy-synced` fuer autonome Laborfaelle, `book-synced` fuer Reporting-UAT; kein Setup-Fit, keine Buchung, keine Zahlung, keine Bankabstimmung. `O2C-REPOST-001`, `P2P-REPOST-001`, `INVENTORY-REPOST-INV008` und neue Payment-Buchungen bleiben fuer die vorhandenen Referenzbelege gesperrt; neue Laborfaelle brauchen einen neuen fachlichen Evidence-Zweck. Neue Company, Environment-Wechsel, praktischer `TAX-002-DE-VAT-FIT`, Security, Integration, Migration, Warehouse-Aktivierung, Analysis-View-Setup ohne neues Gate und unvorbereitete Modulbuchungen bleiben hart gesperrt. Ohne Freigabe ist der naechste sichere Schritt `GOVERNANCE-010-NEXT-NO-GATE-DECISION`.
