# Fix or Workaround

## Sofort-Workaround

Keine Buchung ausfuehren. Den Auftrag offen lassen und Setup-Entscheidung einholen.

## Dauerhafte Loesung

Fachlich korrektes `Inventory Account` fuer `FRA-ZL` + `RESALE` bestimmen und in einer freigegebenen Sandbox/Testumgebung eintragen. Danach Preview Posting erneut pruefen.

## Was man nicht tun darf

- Kein beliebiges Sachkonto eintragen, nur damit die Fehlermeldung verschwindet.
- Nicht direkt buchen, wenn Preview Posting Fehler zeigt.
- Den Fehler nicht als Debitor-/Artikelproblem behandeln, solange die Meldung eindeutig Setup nennt.
