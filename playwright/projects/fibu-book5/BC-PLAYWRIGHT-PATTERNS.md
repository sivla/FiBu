# Business-Central-Playwright-Patterns

Stand: 29.06.2026

Diese Datei sammelt wiederverwendbare Muster aus belegten Laeufen. Sie ist keine allgemeine Playwright-Dokumentation, sondern Projektwissen fuer Business Central. Alte Muster aus `MCP_1_20260210` / `RM-DEMO` bleiben historische Laborreferenz. Neue aktive Zieltests laufen fuer die Universaarl-Welt in `playthru` / `UNIVERSAARL-DE` und duerfen alte RM-/CRONUS-Evidence nur als Musterquelle verwenden.

## Universaarl Pattern Baseline

- Neue aktive Tests muessen ihre Zielwelt explizit setzen: `targetWorld = Universaarl`, `instance = playthru`, `company = UNIVERSAARL-DE` sobald die Company existiert.
- Legacy-Tests mit `RM-DEMO`, `MCP_1_20260210`, `CRONUS`, `Rhein-Main`, `K30000`, `FA-CNC-01`, `BANK-RM-01` oder aehnlichen Laborcodes werden nicht als aktive Universaarl-Zieltests ausgefuehrt. Sie bleiben Archiv oder Patternquelle, bis ein Test bewusst portiert wurde.
- Portierte Universaarl-Tests nennen die verwendeten Capabilities im Result, zum Beispiel `bc_navigation_control`, `bc_page_context_guard`, `bc_scoped_action_click`, `bc_dialog_gate`, `screenshot_truth_gate`, `bc_card_field_diagnostics`, `journal_line_control_snapshot`, `evidence_pack_writer`.
- Der Konsolidierungsstand steht in `UNIVERSAARL-PLAYWRIGHT-PATTERN-CONSOLIDATION.md` und `.agent/state/playwright_pattern_consolidation.json`.

## Grundprinzipien

- Das Projekt ist UI-first. Wenn ein Schritt im Buch als Klickanleitung erscheinen soll, muss er ueber die UI nachvollziehbar sein.
- Tests orientieren sich am sichtbaren Anwenderverhalten: Rollen, Labels, Texte, Dialogtitel, sichtbare Codes, Betraege, Konten, Dimensionen, Filter und Status.
- API-/Direktdatenwege sind nur Diagnose, historischer Laborfit oder technische Hilfsevidence. Sie ersetzen keinen Buch-Klickpfad.
- Jeder Screenshot braucht ein sichtbares Lernziel. Ein Bild ist nur brauchbar, wenn der behauptete Code, Betrag, Status, Button, Dialog, Filter, Fehler, Reportwert oder Postentyp wirklich sichtbar ist.
- Ein technisch gruener Playwright-Test ist kein fachlicher Erfolg, wenn das Result JSON `blocked` meldet oder der Screenshot den Zielwert nicht zeigt. TARGET-026M war das Gegenbeispiel: Der Test lief durch, aber `4400` und `5400` blieben sichtbar auf `Bilanz` statt `GuV`. TARGET-026M-SAFE-WRITE zeigt die Korrektur: Erfolg zaehlt erst, wenn der Zielwert nach Karten-Reopen und Listen-Reopen sichtbar ist.
- `TARGET-026N` verschaerft den Screenshot-Truth-Gate: `compactPageText()` kann Kontenplan-Fragmente aus Frames liefern, waehrend der sichtbare Screenshot noch den Role Center zeigt. Ein Case darf dann nicht als `observed` gelten. Bei Kontenplan-, Setup- und Buchbildern muss das Bild selbst die behauptete Page und die relevanten Felder/Spalten zeigen; versteckter oder stale Frame-Text ist nur Diagnose.
- `TARGET-026O` belegt den Recovery-Pfad: Wenn die direkte Page-URL wieder im Role Center landet, den sichtbaren Navigationslink `Kontenplan` in der BC-Oberflaeche verwenden und danach das sichtbare Business-Central-Iframe fotografieren. Ein Iframe darf nur als Screenshot-Ziel gelten, wenn es echte Kontozeilen enthaelt, nicht nur den Menue-/Navigationsbegriff `Kontenplan`.
- `TARGET-027` uebertraegt diese Regel auf Setupseiten: Direct Page ID plus Tell-Me/Search-Fallback ist noch kein Seitennachweis, wenn das sichtbare Bild weiter Role Center zeigt. Fuer VAT Business/Product Posting Groups und VAT Posting Setup darf ein Result erst `observed` sein, wenn Screenshot und fokussierter Text die eigentliche VAT-Seite zeigen. Role-Center-Text, Suchoverlay oder Top-Navigation sind Blocker, keine Setup-Evidence.
- Nach Keyboard- oder Dropdown-Versuchen in Business-Central-Listen darf ein Zeilen-Locator nicht blind weiterverwendet werden. Vor einem Karten-Fallback muss die Zielzeile erneut ueber Nummer und Name gefunden werden; sonst kann die Karte oder der aktive Editor auf eine vorherige Zeile zeigen.
- CRONUS-USA-Labor, gemischte Sprache und deutsche Zielbilder werden immer getrennt.
- Bei Fehlern zuerst Fehlerklasse bestimmen: Oberflaeche, Page/Tabelle, Berechtigung, Stammdaten, Prozessstatus, Posting Setup, Extension, Daten/Filter, Integration oder Performance. Details stehen in `BC-BUGFIXING-PLAYBOOK.md`.

## Tell-Me

- Tell-Me niemals mit blindem `Enter` bedienen, wenn mehrere Treffer moeglich sind.
- Erst Trefferkandidaten sammeln, fachlichen Treffer anklicken und danach Zielseitenkontext pruefen.
- Exakte Treffer koennen in BC als Ergebniszeile mit Zusatztext erscheinen, zum Beispiel `Customers Listen`. Helper muessen deshalb die Tell-Me-Ergebniszeile und nicht den gleichnamigen Hintergrund-Link treffen.
- `searchFor()` muss die sichtbare Tell-Me-Textbox gezielt befuellen. `GOVERNANCE-013` zeigte, dass reines Tastatur-Tippen bei offenem Suchdialog leer bleiben kann.
- `TARGET-027C2` bestaetigt diese Falle fuer `playthru / UNIVERSAARL-DE`: Der Tell-Me-Dialog kann sichtbar geoeffnet sein, waehrend das Suchfeld im Screenshot leer bleibt. Ein Suchlauf darf dann nicht als "keine Treffer" oder "Seite nicht vorhanden" interpretiert werden; zuerst muss die sichtbare Textbox/Fokusroute recovered werden. `searchFor()` nutzt deshalb zusaetzlich sichtbare Dialog-/Input-Fallbacks. Trotzdem zaehlt erst ein Screenshot mit gefuelltem Suchfeld oder Zielseite als neue Routing-Evidence.
- `TARGET-027C3` recovered die sichtbare Tell-Me-Eingabe: Der Screenshot muss den Suchbegriff im Feld und den exakten Treffer zeigen, bevor ein Ergebnis-Klick versucht wird. Helper duerfen nach `fill()` nicht sofort returnen, sondern muessen den Feldwert pruefen oder einen engen sichtbaren Fallback nutzen.
- Bei Search-Overlay-Screenshots darf der Test vor Capture oder Trefferklick nicht pauschal `Escape` druecken. `TARGET-027C2` zeigte, dass dadurch der sichtbare Trefferzustand zerstoert wird und der folgende Klick faelschlich als `no-click` endet.
- `TARGET-027C4` recovered den Page-470-Trefferklick: Tell-Me-Kandidaten koennen sehr breite Container mit demselben Text enthalten. Der Helper muss deshalb Umlaut-/Mojibake-Text normalisieren und den kleinsten sichtbaren Trefferbereich klicken, der den exakten Seitennamen enthaelt. Ein Klick auf den grossen Overlay-/Body-Container ist zu unscharf und darf nicht als Zielseitenroute gelten.
- `TARGET-027D/D2/D3` verschaerft die Page-472-Regel fuer `MwSt.-Buchungsmatrix Einr.`: Ein gruener Playwright-Test ist kein Setup-Erfolg, wenn der Screenshot eine verschobene oder unvollstaendige Matrixzeile zeigt. Blindes Tabben nach `Neu` kann Werte in falsche Felder schreiben; in diesem Lauf blieb `19` als Beschreibung sichtbar, waehrend `MwSt. %` weiter nicht als `19` bewiesen war und die Konten `3806`/`1406` nicht sichtbar zugeordnet wurden. Diese Route darf nicht wiederholt werden. Der naechste Schritt braucht Page Inspection, Field-/Control-Mapping oder eine nachweisbare Karten-/Detailroute, bevor Page 472 erneut geschrieben wird.
- Fuer Page-472-Recovery gilt: Ein Bild mit `INLAND` + `VAT19` reicht nur als Nachweis einer partiellen Zeile. Als vollstaendige USt-Matrix zaehlt erst ein Reopen-Screenshot, der in derselben Zeile `INLAND`, `VAT19`, `MwSt. % = 19`, `Normale MwSt.`, `Umsatzsteuerkonto = 3806` und `Vorsteuerkonto = 1406` sichtbar oder strukturiert feldgebunden beweist. Vorher bleiben Stammdaten, Preview Posting und Posting gesperrt.
- `TARGET-027D4` zeigt den besseren Diagnosepfad fuer Page 472: `Ctrl+Alt+F1` oeffnet die Seitenueberpruefung mit `VAT Posting Setup (472, List)` und Tabelle `VAT Posting Setup (325)`. Fuer die ausgewaehlte Zeile sind `VAT % = 0`, `Sales VAT Account = (Leer)` und `Purchase VAT Account = (Leer)` sichtbar. Das ist genau die technische Wahrheit, die vor einem weiteren Schreibversuch noetig war. Der folgende Korrektur-/Cleanup-Case darf sich auf diese Feldnamen stuetzen, darf aber weiter keine Preview oder Buchung ausloesen.

- `TARGET-027D5` verschaerft die Page-472-Editorregel: Ein Zellklick zaehlt nur dann als Schreibroute, wenn danach ein echtes aktives Editor-Control im richtigen Zellbereich sichtbar/fokussiert ist. Fuer Beschreibung und `MwSt. %` wurde ein Editor erkannt, fuer `Umsatzsteuerkonto`/`Vorsteuerkonto` aber nicht. Deshalb wurden `3806`/`1406` nicht getippt und die Zeile blieb nach Reopen unvollstaendig. Diese Cell-Edit-Route ist fuer Steuerkonto-Felder gesperrt; der naechste sinnvolle Weg ist Row Action, Personalisieren, Tooltip/Page Inspection oder eine andere sichtbar neue UI-Route.
- Bei `Companies` ist der belegte Treffer `Companies Listen`; der nackte Text `Companies` ist zu breit und kann im Suchdialog oder Hintergrundkontext landen.
- Nach `RM-DE-LAB-CREATE-002` gilt fuer Company-Anlage: Page `357`/`Companies` plus `Neu` in der Listenzeile ist kein Speichernachweis. Wenn BC `Nicht gespeichert` zeigt und der Zeilenfehler auf `Create New Company` / Assisted Setup verweist, darf der naechste Lauf nicht weiter die Listenzeile editieren, sondern muss genau diesen UI-Pfad scoped pruefen.
- Sichtbarer Suchtreffer beweist nur Navigation, nicht Prozessfaehigkeit.
- `FIXEDASSETS-130` verschaerft die Tell-Me-Regel: Suchoverlay-Text wie `Nach <Begriff> suchen` oder `keine Vorschlaege verfuegbar` darf niemals als geoeffnete Page/Wizard zaehlen. Ein Treffer ist erst dann ein Seitenbeweis, wenn das Suchoverlay geschlossen ist und echte Page-Felder, Seitentitel oder fachliche Controls sichtbar sind. Screenshots mit Tell-Me-Overlay sind Navigation-/Blocker-Evidence und muessen als `rejected/do-not-use-as-page-proof` markiert werden.
- Bei gemischter deutscher/englischer UI beide Begriffe als Suchhilfe dokumentieren, aber die Buchsprache deutsch halten.
- Wenn Microsoft Learn oder Supportseiten im Browser aufpoppen, gilt das nicht als BC-Nachweis; Business-Central-Seiten muessen im BC-Kontext bleiben.
- `TARGET-007` ergaenzt fuer BC-Splitbuttons: Der visuelle Pfeil neben einem Button wie `Neu` kann in der Mandanten-Aktionsleiste sichtbar sein, ohne als stabiler Button mit `aria-label`/`title` in der DOM-Kandidatenliste aufzutauchen. In diesem Fall ist ein enger Geometrie-Fallback nur erlaubt, wenn Page, Toolbar, Zielbutton und Screenshot-Nachweis zusammen dokumentiert werden. Geometrie allein ist kein Beweis; das Retake-Bild muss zeigen, dass wirklich das gewuenschte Dropdown oder der gewuenschte UI-Zustand sichtbar ist.
- Fuer jeden Buch-/Clickguide-Screenshot gilt ab `TARGET-007`: Nach dem Test muss Screenshot-QA stattfinden. Das Bild wird visuell geprueft und die Evidence nennt, welche Page, welche Company, welche Controls, welche sichtbaren Eintraege und welche Grenzen tatsaechlich im Bild erkennbar sind. Ein gruener Playwright-Test ersetzt diese Bildpruefung nicht.
- `TARGET-008` verschaerft die Regel: DOM-/Texttreffer wie `Neues Unternehmen erstellen` zaehlen erst, wenn der Screenshot denselben Zielzustand zeigt. Wenn das Bild danach `Neu - Mandanten` mit leerer Listenzeile zeigt, wurde die direkte `Neu`-Listenanlage geoeffnet, nicht der gefuehrte `Neues Unternehmen erstellen`-Wizard. Result JSON und Buchtext muessen diese sichtbare Wahrheit uebernehmen; ein Test darf den falschen Routen-Namen nicht schoenrechnen.
- `TARGET-026J`/`TARGET-026K`/`TARGET-026L` zeigen fuer den Kontenplan: `Liste bearbeiten` plus `Neu` kann in einen Zustand fuehren, in dem keine editierbare Zielzeile sichtbar ist. Der robuste Fallback ist ein Route-Reset zur Kontenplanliste und danach `Neu` ohne aktiven Edit-List-Zustand. Erfolg zaehlt erst nach `Nr.`, `Name`, `GuV/Bilanz`, `Kontoart` und Reopen-Proof. Die Route ist fuer Bilanzkonten wie `1800`, `1406`, `3300` und `3806` belastbar belegt. Fuer GuV-Konten darf sie nicht blind wiederverwendet werden, weil `4400` und `5400` eine eigene GuV/Bilanz-Feldroute brauchen.

## Page-ID

- Direkte Page-ID ist fuer Labor- und Regressionseinstiege erlaubt, aber didaktisch nicht automatisch ein Buch-Klickpfad.
- Page-ID beweist den geoeffneten Kontext, nicht die fachliche Vollstaendigkeit.
- Neue Helper sollen `openBcPageById()` nutzen und danach erwarteten Seitentext pruefen.
- Fuer Buchbilder nach Moeglichkeit Tell-Me- oder Menuepfad separat nachziehen.
- Nach `FIXEDASSETS-287` gilt: Bei direkten Page-ID-Aufrufen kann das Hauptdokument nur Shell-Text wie `Dynamics 365 Business Central`, Umgebung und Benutzer enthalten, waehrend der fachliche Seiteninhalt im `runinframe`-Frame liegt. Evidence-Scanner muessen deshalb alle Frames durchsuchen und den fachlich relevanten Frame protokollieren. Ein Shell-only-Scan ist ein technischer Blocker, kein negativer BC-Fachbefund.
- Nach `P2P-024`/`P2P-026` gilt fuer stateful BC-Listen: Eine direkte Page-ID-URL ohne sichtbaren `filter`/`bookmark` im angeforderten Ziel beweist noch keinen neutralen Listenstart. Business Central kann den alten Listen-/Bookmark-Kontext im Frame wiederherstellen; bei `Purchase Invoices` blieb `107229` nach scoped `Neu` und sogar beim spaeteren read-only Page-9308-Aufruf sichtbar. Fresh-Draft-Routen muessen deshalb vor Zielwerteingabe beweisen, dass kein verbotener Altbeleg im Vordergrundkontext sichtbar ist und entweder eine echte neue Karten-/Belegnummer oder ein expliziter Bookmark-/Filter-Reset erreicht wurde.
- Nach `P2P-027` gilt fuer Routenentscheidungen: Wenn eine BC-Route wiederholt von unsichtbarem Navigations-/Bookmark-State dominiert wird, ist ein Helper auf Vorrat nicht automatisch der beste naechste Schritt. Fuer den aktuellen RM-DEMO-P2P-Zweig ist `Purchase Invoices` verworfen; die naechste Route ist `Purchase Journal`, weil `P2P-019` konkrete Journal-Check-Regeln liefert: negativer Vendor-Zeilenbetrag, leere Gen.-Posting-Gruppen auf der Vendor-Zeile und balancierter Beleg vor Preview.

## Listen vs. Karten

- Eine Liste beweist, dass der Zielkontext erreichbar ist.
- Eine Karte beweist nur dann einen Zielstammsatz, wenn der Zielcode und relevante Felder sichtbar sind.
- Ein leeres Formular ist Formular-Preflight, kein Stammdaten-Nachweis.
- Zielobjekt-Nachweise brauchen konkrete sichtbare Werte, zum Beispiel `D10000`, `RM-M100`, `FA-CNC-01`, `K30000`.
- Negative Listenbefunde brauchen ebenfalls sichtbare Wahrheit: aktiver Filter/Suchwert plus leere Liste. `FIXEDASSETS-036` ist das Muster: `Vendors` Page `27` zeigt `No. = K30000` im Filterkontext und `(In dieser Ansicht kann nichts angezeigt werden)`. Erst dadurch ist das Bild als fehlender-Kreditor-Nachweis brauchbar; ein leeres Grid ohne Filterwert ist rejected.
- `FIXEDASSETS-018` ist das aktuelle Anlagen-Beispiel: Die gefilterte Liste beweist, dass `FA-CNC-01` fehlt; die leere `Fixed Asset Card` beweist Kartenfelder/Pflichtfelder, aber nicht den Zielstammsatz. `FIXEDASSETS-019` haelt fest: Vor dem Speichern eines Stammsatzes muessen die relevanten Setup-Referenzfelder sichtbar/setzbar gemappt sein, hier `HGB` und `MACHINES`. `FIXEDASSETS-020` zeigt danach: kartennahe `Mehr anzeigen`-Kontrollen koennen `Depreciation Book Code` und `Posting Group` sichtbar machen; das ist Feldpfad-Evidence, aber noch kein Wertebeweis fuer `FA-CNC-01`, `HGB` oder `MACHINES`. `FIXEDASSETS-021` ergaenzt die Speicherregel: Sichtbarer Feldpfad ist keine Speicherfreigabe. `FIXEDASSETS-022` ergaenzt die Screenshot-QA-Regel: Lookup-Evidence zaehlt nur, wenn der richtige fachliche Dialog und der behauptete Code sichtbar sind. `FIXEDASSETS-023` ergaenzt die Locator-Regel: Page Inspection kann `Fixed Asset Card (5600)` und `Fixed Asset (5600)` technisch bestaetigen, waehrend ungescopte DOM-Suchen weiter Labels der Hintergrundliste finden. `FIXEDASSETS-024` war nur partiell, weil `Depreciation Book Code` und `Posting Group` nicht als aktive Controls gefunden wurden. `FIXEDASSETS-026` recovered diese Controls no-save durch Vordergrundkarten-Pruefung und echte kleine FastTab-/`Mehr anzeigen`-Buttons; damit ist Control-Erreichbarkeit belegt, aber noch kein Wertebeweis und kein Save-Gate. `FIXEDASSETS-027` belegt Lookupwerte fuer Klasse/Unterklasse, `HGB` und `MACHINES`, zeigt aber auch die Auto-Number-Falle: Der New-Card-Preflight erzeugte temporaer `FA000110`, der danach per UI geloescht und per Filter leer nachgewiesen wurde. `FIXEDASSETS-029-EXISTING` zeigt die Gegenprobe: Ein vorhandener Listencode `FA-CNC-01` ist noch kein Zielstammsatz, wenn die geoeffnete Karte Beschreibung, Klasse/Unterklasse, AfA-Buch, Posting Group und AfA-Daten leer zeigt. `FIXEDASSETS-031` ergaenzt das bessere Korrekturmuster: Wenn Kartenwerte zaehlen, die bestehende Karte direkt ueber Page `5600` plus Filter oeffnen und danach sichtbare Zielwerte pruefen; ein Listenbild oder weiches `FA Class Code`-Matching reicht nicht. `FIXEDASSETS-032` zeigt die naechste Grenze: Ein Bild kann `HGB`, `MACHINES`, AfA-Start, Nutzungsdauer und AfA-Ende korrekt zeigen, waehrend Beschreibung, Klasse und Unterklasse weiter leer bleiben. Das ist Teilfit und Debugging-Evidence, aber keine Buchungsfreigabe. Ein Nummernserien-Dialog oder Listentreffer darf nicht als FA-Class/Subclass-/Stammdatenbeweis verwendet werden. Ein spaeteres Buchbild fuer die Anlage muss Zielcode, Beschreibung, Klasse/Unterklasse und relevante Setupwerte sichtbar zeigen.
- `FIXEDASSETS-033` ergaenzt: Ein fast vollstaendiger Karten-Screenshot ist erst dann buchungsnah, wenn auch der letzte fachliche Pflichtwert sichtbar ist. `Description = CNC Maschine FRA` und `FA Class Code = TANGIBLE` sind jetzt Kartenwert-Evidence; der leere `FA Subclass Code` bleibt ein blocker fuer Kreditor, Einkauf, Zugang und AfA.

## Vendor-/Kartenanlage mit Auto-Number-Drafts

- BC-Vorlagen und Nummernserien koennen schon beim `New/Neu`- oder Template-Schritt einen Entwurf erzeugen. `FIXEDASSETS-038` ist das Muster: `V00030` wurde erst durch echte UI-Eingabe und sichtbaren Nachfilter zu `K30000`; `V00020` wurde als leerer versehentlicher Draft per UI geloescht.
- `TARGET-016` ergaenzt fuer Universaarl-Nummernserien: Ein sichtbarer Nummernserienkopf (`U-CUST`, `U-VEND`, `U-ITEM`, `U-SO`, `U-SINV`, `U-PO`, `U-PINV`) ist noch keine vollstaendige Nummernserie, solange `Startnr.`/`Endnr.` in `Nr.-Serienzeilen` nicht sichtbar sind. Die Lines-Seite startet mit `Startdatum`, danach kommt `Startnr.`; blinde Tab-Routen duerfen nicht als Wertebeweis gelten. Splitbutton-/Command-Bar-Klicks koennen einen `force`-Fallback brauchen, wenn der sichtbare Button-Span Pointer-Events abfaengt.
- `TARGET-016B` verschaerft das Nummernserienmuster: Auf der Nummernserienseite koennen Hauptliste, `Nr.-Serienzeilen`-Modal und rechte FactBox gleichzeitig Labels wie `Startnr.` und `Endnr.` enthalten. Helper muessen deshalb frame-scoped und modal-scoped arbeiten, bevor sie eine Zelle als editierbar behandeln. Checkbox-Spalten wie `Standardnr.`, `Manuelle Anz.`, `Chronologisch`, `Luecken in Nummern zulassen` und `Offen` gehoeren zur fachlichen Setup-Wahrheit; sie duerfen nicht uebergangen werden, auch wenn der aktuelle Lauf sie nicht aendert. Ein Klick unter einen Spaltenkopf kann nur Header/Tooltip/Fokus markieren und ist kein Wertebeweis. Erfolg zaehlt erst, wenn der Zielwert nach Blur oder Reload sichtbar in der richtigen Lines-Zeile steht.
- `TARGET-036D2E` verschaerft dieses Muster fuer sichtbare Checkboxspalten auf Page 456: Eine sichtbare Checkbox in der Zeile `U-VEND` ist noch keine sichere Schreibroute, wenn DOM-Checkboxen nicht eindeutig an Zeile und Feld gebunden sind. Auch ein Screenshot-QA-Koordinatenfallback zaehlt nur, wenn danach der Reopen-Proof genau `U-VEND` mit aktivem `Manuelle Anz. / Manual Nos.` zeigt. Im Lauf blieb `Manuelle Anz.` nach Reopen inaktiv; deshalb ist Vendor-Anlage weiter gesperrt. Ein gruener Testlauf ohne Reopen-Wert ist Blocker-Evidence, kein Setup-Erfolg.
- `TARGET-036D2F` ergaenzt die Auth-Blocker-Regel fuer Live-Tests: Wenn Business Central vor dem Shell-Kontext auf einer Token-/Auth-Fehlerseite landet, ist das kein BC-Setup-Befund. Der Test muss ein kompaktes `blocked`-Result schreiben, keine rohen Auth-/Tenant-Screenshots committen und den naechsten Schritt auf Auth-Refresh plus denselben engen Case setzen. Erst nach gueltigem `storageState` darf die gefilterte `U-VEND`-Einzelzeilenroute erneut geprueft werden.
- `TARGET-036D2H` schliesst die aktuelle U-VEND-Checkboxhypothese: Page Inspection kann Page `No. Series (456, List)`, Tabelle `No. Series (308)` und die Felder `Default Nos. (3, Boolean)` / `Manual Nos. (4, Boolean)` sauber zeigen. Fuer `U-VEND` steht `Manual Nos.` aber auf `Nein`. Das ist ein starker technischer Blocker, kein Schreibpfad. Keine weitere unscoped Checkbox-, Koordinaten- oder Gridroute wiederholen; Vendor-Anlage bleibt gesperrt, bis eine wirklich neue Nummernserienroute existiert.
- Die Auth-Blocker-Erkennung ist in `playwright/core/bc-helpers.ts` zentralisiert. `waitForBcReady()` prueft vor dem langen Shell-/Suchbutton-Wait auf Token-/Login-Fehlerseiten und bricht mit einer klaren Auth-Refresh-Meldung ab. Neue BC-Tests sollen diesen Helper nutzen, statt eigene 120s-Suchbutton-Timeouts als Setup-Blocker zu interpretieren.
- `auth.setup.ts` darf den Storage State nicht speichern, nur weil irgendwo `Business Central` im Fehlerseitentext steht. Gueltiger Login-State braucht echte Shell-Signale wie `Suchen`, `Tell me`, `Rollencenter`, `Meine Einstellungen` oder `My Settings` und darf Token-/Login-Fehlerseiten nicht akzeptieren.
- `npm run auth:bc:check` ist ein lokaler Schnellcheck vor Live-Tests: Er prueft nur Existenz, JSON-Form, Alter und Cookie-Zaehler von `playwright/.auth/bc-user.json` sowie eine lokale `bc-user.meta.json`, die nur nach echter Shell-Pruefung durch `auth.setup.ts` geschrieben wird. Der Check gibt keine Token, Cookie-Werte, Tenant-IDs oder Nutzerdaten aus. Ein gruener Auth-State-Check ersetzt nicht die Shell-Pruefung im Live-Test, verhindert aber vermeidbare lange Runs mit fehlender oder offensichtlich unbrauchbarer Auth-Datei.
- Bei BC-Listen muss Screenshot-QA nach jedem Checkbox-/Zellversuch pruefen, ob der Zeilenfokus noch auf dem Zielcode liegt. `TARGET-036D2E` zeigte, dass eine falsche oder zu grobe Zellroute row focus auf eine andere Nummernserie bewegen kann, ohne eine akzeptierte Setup-Aenderung zu erzeugen. Helper muessen solche Row-Drift als Stop-Kriterium behandeln.
- Fuer Kartenfelder mit Seiteneffekten keine direkte DOM-Wertsetzung verwenden. Stattdessen sichtbare Inputs mit Playwright fuellen, Tab/Blur ausloesen und BC-Dialoge gezielt bestaetigen.
- Ein Screenshot zaehlt nur, wenn er den Zielwert oder den bewusst dokumentierten Fehler sichtbar zeigt. Template-Dialoge sind Kontext, aber kein Zielstammdatenbeweis.
- Nach `FIXEDASSETS-046` gilt fuer Kreditoren-Defaults: Ein sichtbarer Einstellungen-/`Personalisieren`-Einstieg ist nur Diagnose-Evidence. Er beweist keine ausgeblendeten Feldwerte und erlaubt keinen geratenen Setup-/Default-Fit. Der naechste Test muss die Feldcaption, den Lookup-Wert oder den Nicht-Sichtbar-Befund selbst sichtbar machen.
- Nach `FIXEDASSETS-047` gilt zusaetzlich: Tax-/Payment-Teilwerte auf derselben Vendor Card sind kein Ersatz fuer kritische Einkaufsdefaults. Wenn `Tax Area Code`, `Tax Liable`, `1M(8D)` und `BANK` sichtbar sind, aber `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group` fehlen, bleibt der Kaufbeleg gesperrt. Screenshots duerfen nur fuer die jeweils lesbaren Captions/Werte freigegeben werden.
- Nach `FIXEDASSETS-048` gilt fuer `Personalisieren`: Feldverfuegbarkeit darf read-only/diagnostisch geprueft werden, bevor ein Setup-Fit erwogen wird. Eine gespeicherte nutzerspezifische Personalisierung ist aber kein Standard-BC-Bild und darf nicht still als Buchscreen verwendet werden; der Lauf muss stoppen, wenn Speichern noetig waere, um die Felder sichtbar zu machen.
- Nach `FIXEDASSETS-049` gilt: Geoeffneter Personalisieren-Modus ist nur dann Feldverfuegbarkeitsbeweis, wenn die Feldliste oder die konkrete Feldcaption sichtbar ist. `Wird personalisiert: Vendor Card` plus K30000-Kontext beweist Debugging-Kontext, aber nicht `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` oder `VAT Bus. Posting Group`. Wenn der `Add field`-/Feldlisten-Pfad nicht sichtbar geoeffnet wird, folgt ein Page-Inspection-/manuelle-Diagnose- oder Setup-Gate-Entscheid, kein Kaufbeleg.
- Nach `FIXEDASSETS-051` gilt fuer Page Inspection: `Ctrl+Alt+F1` kann in diesem Browserkontext stabil die Seitenpruefung oeffnen. Page/Table-Kontext (`Vendor Card (26)`, `Vendor (23)`) ist Screenshot-tauglich als Technikbild; konkrete Feldwerte muessen aber separat aus sichtbaren Page-Inspection-Feldbloecken gesichert werden. Ein Page-Inspection-Screenshot, der die Codes nicht zeigt, bleibt Kontextbild. Werte wie `DOMESTIC`, `1M(8D)` oder `BANK` zaehlen erst, wenn sie in strukturierter Evidence oder im Bild selbst lesbar sind.

## Kartencontrol-Diagnose

- `P2P-004` ergaenzt fuer Einkaufsbestellungen: Kopf-Felder koennen sichtbar und fachlich richtig sein, ohne hilfreiche `aria-label`-/`title`-Signale zu liefern. Ein geometrischer Fallback auf ein Top-Header-Input ist nur als enger Labor-Hebel erlaubt, wenn vorher der Listenkontext und die geoeffnete Karte bewiesen wurden und danach der Zielwert im Seitentext/Screenshot sichtbar ist. Die Evidence muss x/y-Kandidaten und den sichtbaren Nachherwert dokumentieren; Geometrie allein ist kein allgemeiner Feldbeweis.
- Nach `FIXEDASSETS-234` gilt fuer Setup-Fit-Reviews: Ein breiter Edit-/Top-Action-Kandidat entwertet einen Feldfit nicht automatisch, wenn das Ziel-Control selbst eindeutig per Vorher/Nachher-Wert belegt ist. Fuer kuenftige Setup-Fits zuerst pruefen, ob das Ziel-Control bereits editierbar ist; wenn ja, keinen zusaetzlichen breiten Edit-Action-Klick ausfuehren. Der belastbare Nachweis ist der feldlokale Zielwert, nicht ein allgemeiner Action-Bar-Kandidat.
- Neuer Helper-Baustein: `playwright/core/bc/cards.ts` mit `collectActiveCardControlDiagnostics()`. Er bewertet sichtbare Feldcaptions, verwirft Grid-/Columnheader-Kandidaten und sucht editierbare Controls in derselben sichtbaren Kartenzeile.
- Der Helper ist Diagnose, keine Speicherfreigabe. Er beantwortet: "Treffe ich die Vordergrundkarte?" Nicht: "Ist der fachliche Zielwert bereits gesetzt?"
- Nach `FIXEDASSETS-026` ist das Muster fuer leere Karten: zuerst Kartenkontext beweisen, dann aktive Controls vollstaendig mappen, danach separat Werte/Lookups pruefen. Eine erfolgreiche Control-Recovery ist keine Speicherfreigabe und kein Nachweis fuer `HGB`, `MACHINES` oder `FA-CNC-01`.
- Nach `FIXEDASSETS-027` ist klar: New-Card-Lookup-Preflight ist nicht automatisch no-save. Tests muessen neben der Zielnummer auch automatisch erzeugte Nummern erkennen, Cleanup-Evidence sichern oder vor dem Lauf ein ausdrueckliches Save-Gate einholen.
- Nach `FIXEDASSETS-028` gilt fuer Anlagen: Der naechste Lauf ist kein weiterer Preflight, sondern ein kontrollierter Zielstammdaten-Save. Das Testziel muss enger sein als der Gesamtprozess: erst `FA-CNC-01` speichern und visuell pruefen, danach spaeter separat Kreditor, Einkauf, Zugang, AfA und Postenspur.
- Nach `FIXEDASSETS-029` gilt zusaetzlich: Ein sichtbarer Zielcode in der Liste ist ein Stop-Kriterium, aber kein fachlicher Stammdatenbeweis. Kartenfelder duerfen nicht ueber breite Ancestor-/Parent-Texte gefuellt werden, weil Business-Central-Cards viele Feldcaptions im gleichen DOM-Bereich enthalten. Fuer Werteingabe muss die Caption mit einem editierbaren Control derselben sichtbaren Kartenzeile verknuepft werden. Wenn ein Zielcode bereits existiert, wird nicht ueberschrieben; der naechste Lauf ist read-only Detailklassifizierung.
- Nach `FIXEDASSETS-029-EXISTING` gilt fuer bestehende Stammdaten: erst Karte oeffnen, FastTabs aufklappen, sichtbare Feldwerte extrahieren und gegen den fachlichen Zielzustand bewerten. `No.` allein ist kein Setup-Fit. Wenn Business-Central-Listenzellen nicht als normale Links klickbar sind, ist ein frame-aware Fokus-/Enter-/Doppelklick-Fallback erlaubt, aber nur mit Nachbedingung: die Zielkarte und der Zielcode muessen danach sichtbar sein.
- Nach `FIXEDASSETS-031` gilt fuer bestehende Fixed-Asset-Karten: direkter Page-5600-Aufruf mit Record-Filter ist ein stabiler Labor-/Regressionseinstieg. Screenshot-Kontext muss `Fixed Asset Card` und die behaupteten Werte enthalten. Ein Teilfit darf nicht als vollstaendig markiert werden; bei fehlenden sichtbaren Zielwerten folgt eine Blocker-Diagnose statt Buchung.
- Nach `FIXEDASSETS-032` gilt fuer bestehende Fixed-Asset-Karten: Felddiagnose muss drei Dinge trennen: sichtbare Caption, sichtbarer Wert und tatsaechlich editierbares Control. Ein erfolgreich gefuelltes AfA-Datum beweist nicht, dass Beschreibung, Klasse oder Unterklasse ebenfalls editierbar sind. Wenn Felder leer und nicht editierbar bleiben, ist der naechste Schritt Helper-/Personalisieren-/Page-Inspection-Diagnose, nicht Einkauf oder Posting.
- Nach `FIXEDASSETS-033` gilt fuer bestehende Fixed-Asset-Karten: Top-Icons koennen bei `y=14` liegen und werden von Helfern uebersehen, wenn sie erst ab Inhaltsbereich suchen. `clickBcTopIconAction()` sucht deshalb gezielt in der oberen BC-Leiste nach `title`/`aria-label` und prueft optional eine Nachbedingung. Breite Layoutansicht verschiebt Kartenfelder deutlich nach links; zeilenbasierte Feldfinder duerfen nicht mit starren `x>230`-Grenzen arbeiten.
- Nach `FIXEDASSETS-170` gilt fuer Setupkarten zusaetzlich: Ein sichtbares Kartenfeld ist nicht automatisch editierbar. Wenn ein gezielter Wertklick in der Kartenansicht nach Reopen keinen Wert liefert, nicht blind Koordinaten wiederholen. Zuerst ueber `clickBcTopIconAction()` das topnahe Edit-/Stift-Symbol im passenden Kartenkontext aktivieren, danach genau das Ziel-Feld setzen und die Karte neu oeffnen. Erfolg zaehlt erst, wenn der Zielwert nach Reopen im selben Feld sichtbar ist.
- Lookup-Erfolg zaehlt erst, wenn der Zielwert danach sichtbar im Kartenfeld steht. Bei `FA Subclass Code` hat `FIXEDASSETS-033` gezeigt: direkte Texteingabe kann von BC geleert werden, und ein nicht geoeffneter oder nicht bestaetigter Full-List-Lookup ist nur Blocker-Evidence, kein Wertebeweis.
- Nach `FIXEDASSETS-034` gilt fuer idempotente Setup-/Stammdatenlaeufe: Wenn der Zielwert bereits sichtbar fit ist, darf der Test nicht kuenstlich erneut klicken. Er soll den `already-fit`-Zustand als Erfolg mit Screenshot, JSON und klarer Grenze dokumentieren. Zusammengesetzte Stammdatenbeweise muessen offen sagen, welche Werte auf welchem Bild sichtbar sind.
- Nach `FIXEDASSETS-139` gilt fuer Karten-Comboboxen: Ein sichtbares Kartenfeld wie `Posting Group` darf nicht wie ein normales Textinput behandelt werden, wenn BC es als Kombinations-/Lookup-Feld rendert. Erfolg braucht eine feldspezifische Route: Ziel-FastTab und Caption beweisen, Combobox/Lookup fuer genau dieses Feld oeffnen, Option sichtbar erfassen, Zielwert auswaehlen, Karte neu oeffnen und den gespeicherten Wert im Bild/JSON beweisen. `FA-CNC-01` bleibt bis dahin bei `Posting Group = EQUIPMENT`; `MACHINES` darf nicht als zugewiesener Kartenwert behauptet werden.
- Nach `FIXEDASSETS-140` gilt zusaetzlich: Klicks auf Kartenwerte koennen verwandte Karten oeffnen. Bei `FA-CNC-01` oeffnete der vermeintliche Combobox-Pfad die `Depreciation Book Card` fuer `HGB`. Solche Related-Record-Navigation ist ein Stop-Kriterium, kein Fallback-Erfolg. Der naechste Locator muss nach jedem Klick pruefen, ob der Vordergrund weiter `Fixed Asset Card` ist und ob eine echte `Posting Group`-Option wie `MACHINES` sichtbar wurde.
- Nach `FIXEDASSETS-141` gilt fuer Ergebnisleser: Nach einer falschen Related-Card-Navigation duerfen Feldzeilen nicht aus dem neuen Vordergrundkontext zurueck auf die urspruengliche Karte projiziert werden. `Description = HGB depreciation book` ist in diesem Fall Kontextleck der `Depreciation Book Card`, kein Wert von `FA-CNC-01`. Ein reparierter Helper muss vor dem Zeilenlesen den erwarteten Vordergrundtitel, Zielrecord und Ziel-FastTab validieren.
- Nach `FIXEDASSETS-142` ist dieser Guard im FA-140-Test lokal eingebaut: `Depreciation Book Card` / `HGB depreciation book` wird nicht mehr als `Fixed Asset Card`-Kontext akzeptiert, Fill-/Dropdown-Versuche pruefen den Vordergrund erneut und falsche Related-Card-Navigation wird als `blocked-wrong-related-card-route` ausgegeben. Der Guard ist noch kein `MACHINES`-Fit; er macht nur den naechsten Live-Retry sicherer.
- Nach `FIXEDASSETS-143` gilt: Ein Guarded-Retry, der denselben Related-Card-Blocker reproduziert, ist kein Anlass fuer den gleichen UI-Pfad in einer weiteren Schleife. `Alt+ArrowDown`, Right-Edge-Klick und Typed-Value-Versuch fuer dieses Feld bleiben fuer `FA-CNC-01` blockiert, bis eine lokale Routenentscheidung einen wirklich anderen UI-Mechanismus benennt, zum Beispiel Page Inspection/Personalisieren als Diagnose oder einen klaren AssistEdit-/Lookup-Hebel. Der Screenshot beweist den falschen Vordergrund `Depreciation Book Card HGB`, nicht `Posting Group = MACHINES`.
- Nach `FIXEDASSETS-144` gilt als Autopilot-Regel: Wenn zwei praktische Laeufe denselben Related-Card-Blocker liefern, wird der Pfad als rejected gefuehrt und nur ein anderer Mechanismus darf naechster Live-Schritt sein. Fuer `FA-CNC-01` ist dieser andere Mechanismus eine no-save Diagnose ueber Page Inspection/Personalisieren; dabei darf nichts ausgewaehlt, gespeichert oder als Setup-Fit behauptet werden.
- Nach `FIXEDASSETS-145` gilt fuer technische Diagnosebilder: Page Inspection kann `FA Posting Group (29, Code[20])` beweisen und Personalisieren kann zeigen, dass `Posting Group` auf der `Fixed Asset Card` sichtbar ist. Beides ist aber kein Schreibhebel und kein `MACHINES`-Fit. Ein naechster Write-Case darf erst nach lokaler Review starten und muss einen anderen, explizit benannten UI-Mechanismus haben.
- Nach `FIXEDASSETS-147` gilt fuer no-save Affordance-Probes: Ein sichtbares Combobox-Feld plus feldnaher Auswahlbutton (`Wählen Sie einen Wert für Posting Group`) ist ein besserer Hebel als der alte Related-Card-Pfad. Es beweist aber nur Bedienbarkeit, nicht den gespeicherten Zielwert. Vor einem spaeteren Auswahlversuch muss ein Review-Gate festlegen: keine Related-Card-Navigation akzeptieren, `MACHINES` nur ueber den feldnahen Button auswaehlen, danach Karte neu oeffnen und `Posting Group = MACHINES` sichtbar beweisen.
- Nach `FIXEDASSETS-036` gilt fuer BC-Listen: Filter-/Such-/Listenkontext kann in einem `runinframe`-Frame liegen. DOM-Pruefungen, Filterpane-Klicks und sichtbare Signale muessen frame-aware laufen; main-document-only Scans koennen den eigentlichen Business-Central-Listenbereich verpassen.

## Action Bar

- Aktionen muessen an den Seitenkontext gebunden werden. `first()`/`last()` ist nur akzeptabel, wenn danach der Zielzustand geprueft wird.
- Neuer Helper-Baustein: `playwright/core/bc/actions.ts` mit `clickBcAction()` und `isBcActionVisible()`. Er sucht sichtbare `button`/`menuitem`/`link`-Aktionen ueber Page und Frames, kann optional Seitentext als Kontextanker verlangen und nach dem Klick einen erwarteten Zieltext pruefen.
- `New/Neu` ist global mehrdeutig. Kein Setup oder Stammdaten-Fit mit ungescoptem `New/Neu`.
- `FIXEDASSETS-060` ergaenzt fuer Purchase Invoices: Ein gescopter `Neu`-Klick reicht erst dann als aktiver Belegkontext, wenn danach ein singularer `Purchase Invoice`-Titel, Pflichtfelder wie `Vendor Name`/`Vendor Invoice No.` und Lines-/Gridspalten wie `Type`, `No.` und `Description` sichtbar sind. Dieser Kontextnachweis erlaubt noch keine Zielwerte, Preview oder Buchung.
- `FIXEDASSETS-062` ergaenzt fuer Purchase-Invoice-Zeilen: Kontextnachweis allein reicht nicht. Nach jeder Zeilentyp-Aenderung muss der sichtbare Wert `Fixed Asset`/`Anlage` in derselben Zeile bestaetigt werden, bevor `FA-CNC-01` in `No.` eingegeben wird. Wenn `FA-CNC-01` eine `Vendor Card` oeffnet oder die Zeile weiter `Type = Item` zeigt, ist der Screenshot `rejected/debugging`; der Lauf muss Drafts und versehentliche Stammdaten ueber die UI bereinigen.
- `FIXEDASSETS-063` macht daraus ein wiederverwendbares Gate: Der naechste UI-Lauf darf den Anlagen-Zielcode noch nicht eingeben, sondern muss zuerst den Zeilentyp `Fixed Asset` sichtbar/stabil in einer Einkaufsrechnungszeile beweisen. Ein Klick auf den Defaultwert `Item` ist kein Zeilentypwechsel. Erfolg braucht sichtbaren Zeilenwert, nicht nur Tastatureingabe oder Seitentext.
- `FIXEDASSETS-064` bestaetigt: Blindes `Control+A` + Tippen von `Fixed Asset` nach Klick auf `Item` ist kein robuster BC-Zeilentypwechsel. Der Lauf erzeugte einen Vendor-Registrierungsdialog fuer `Fixed Asset`, waehrend die Zeile weiter `Type = Item` zeigte. Der naechste Helper muss die echte Dropdown-/Lookup-Bedienung des Type-Felds finden und danach per sichtbarem Feldwert pruefen.
- `FIXEDASSETS-115` ergaenzt fuer Dropdown-Evidence: Ein Klick auf den sichtbaren `Item`-Button kann den Datensatz-/Kontextmenue-Pfad treffen statt die Werteliste zu oeffnen. Option-Discovery darf deshalb nur echte Optionswerte zaehlen, nicht Feldheader, Menueeintraege oder sonstigen Seitentext. Ein Screenshot ist `rejected/do-not-use`, wenn er nicht die behaupteten Optionen zeigt; in FA-115 blieb nach Bereinigung nur `Item` sichtbar und `Fixed Asset`/`Anlage` unbewiesen.
- `FIXEDASSETS-117` ergaenzt fuer BC-Line-Grids: Die breitere `TD role=gridcell` fuer `Type` ist nicht automatisch der Dropdown. Direkter Klick kann weiter das Kontextmenue `Details anzeigen` oeffnen. `Alt+ArrowDown` kann den kleinen `Menue fuer Type oeffnen`-Button fokussieren; das ist ein Routen-Signal, aber noch kein Optionslisten-Beweis. Erst sichtbare Werte wie `Fixed Asset`/`Anlage` in einer echten Liste oder als Zellwert zaehlen als Fit.
- `FIXEDASSETS-119` ergaenzt die Grenze dieses Signals: Auch ein praeziser Klick auf den kleinen `Type`-Menuebutton beweist keine Werteliste, wenn danach nur Grid-/Headerfokus sichtbar ist und die bereinigten Optionen bei `Item` bleiben. `Alt+ArrowDown` kann auf `Groesse aendern` landen; dieser Fokus darf nicht als Dropdown-Erfolg gelten. Den Menuebutton-Pfad nicht weiter wiederholen, bevor ein lokaler Review eine neue Strategie freigibt.
- `FIXEDASSETS-121` ergaenzt fuer Page Inspection und Personalisieren: `Ctrl+Alt+F1` kann nach Fokus der Einkaufsrechnungs-`Type`-Zelle technische Hilfe-/Page-Kontexte liefern, hier `Purchase Invoice (51)` / `Purchase Header (38)` plus `Type`-Hilfetext mit `fixed asset`-Hinweis. Das ist kein Wertelistenbeweis und kein `Purchase Line`-Source-Table-Beweis. Personalisieren darf nicht direkt danach als Feldangebot gewertet werden, wenn BC meldet, dass Personalisieren wegen aktiver Seitenpruefung nicht gestartet werden kann. Fuer einen Personalize-Retry muss Page Inspection zuerst nachweislich geschlossen sein.
- `REPORTING-013` ist der belegte Rejected Path: ungescopter `New/Neu` kann in den Role-Center-Kontext fallen.
- `FIXEDASSETS-012` zeigt nur leere Karten/Template-Dialoge; das ist kein Zielcode-Beweis.
- `FIXEDASSETS-016` zeigt das Gegenmuster: Auf der `FA Posting Group Card` ist `New/Neu` als titelbasierter Icon-Button sichtbar (`Erstellen Sie einen neuen Eintrag.`), nicht zwingend als Textlabel. Der Helper muss solche titelbasierten Aktionskandidaten bewerten und danach den Zielzustand sichtbar pruefen.
- `FIXEDASSETS-020` zeigt ein zweites Scoping-Muster: `Mehr anzeigen` nicht als jedes `aria-expanded=false` klicken. Nur explizite, kartennahe `Mehr anzeigen`-/`Show more`-Labels mit Feldkontext sind akzeptabel; globale Navigation, Agentenleisten oder Shell-Controls gehoeren nicht zum Kartenfeldmapping.
- Strictness-Migration nach `FIXEDASSETS-023`: `New/Neu` auf der Anlagenliste kann mit `clickBcAction()` genutzt werden, wenn `scopeText` den Listen-/Seitenkontext bindet und `expectedAfterClick` den Kartenkontext beweist. Der lokale Titel-Icon-Fallback bleibt nur Ausnahme; im migrierten Lauf wurde er nicht mehr gebraucht.
- `FIXEDASSETS-026` zeigt die strengere Nachbedingung: Ein geklickter `New/Neu`-Kandidat reicht nicht, wenn der erwartete Vordergrundkarten-Zustand nicht sichtbar erreicht wird. Der Fallback muss danach `Fixed Asset Card` und die erwarteten Feldbereiche beweisen. Fuer `Mehr anzeigen` duerfen keine grossen Kartencontainer als Button akzeptiert werden; nur kleine, sichtbare FastTab-nahe Controls mit passendem `aria-label`/`title` zaehlen.
- `FIXEDASSETS-027` zeigt die Auto-Number-Nachbedingung: Sobald eine neue Stammdatenkarte eine Nummer zieht oder `Gespeichert` zeigt, muss der Lauf das als Datensatzwirkung behandeln. Danach braucht es bewussten Cleanup oder ein Save-Gate; eine reine Zielnummern-Negativpruefung reicht nicht.
- `FIXEDASSETS-033` zeigt den Top-Icon-Pfad: Edit/Stift und breite Layoutansicht sind nicht zwingend normale Textbuttons im Inhaltsbereich. Fuer solche BC-Top-Actions ist ein titel-/aria-basierter Icon-Helfer robuster als ein lokaler Content-Button-Scan.
- `FIXEDASSETS-128` verschaerft die Action-Klassifizierung: Nicht jedes Toolbar-Element mit `edit` im Tooltip ist ein Edit-/Stift-Modus. `Copy Fixed Asset...` enthaelt `edit` in der Beschreibung, ist aber fachlich eine Kopieraktion und muss fuer Edit-Probes ausgeschlossen werden. Fuer sichere Edit-/Mode-Probes erst harte Ausschluesse wie `Copy`, `New`, `Delete`, `Share`, `Acquire`, `Post` und `Preview` anwenden, dann titel-/aria-basierte Kandidaten wie `Aenderungen auf der Seite vornehmen` pruefen und nach dem Klick zwingend validieren: kein gefaehrlicher Dialog, keine Feldwert-Aenderung, Zielaktion unveraendert oder bewusst dokumentiert.

## Post / Preview

- Nicht den Hauptteil von `Post...` klicken, wenn `Preview Posting` gemeint ist.
- `Preview Posting` ist der bevorzugte sichere Preflight vor Buchungen, wenn verfuegbar.
- Ein normaler Buchungsdialog ist die letzte Sicherheitsgrenze. Vor `OK` muss Screenshot/Evidence klaeren, welche Option gewaehlt wird.
- Belegte Optionen: O2C `Ship and Invoice`, P2P `Receive and Invoice`.
- Jede echte Buchung braucht Belegnummer, Postenspur und No-Repeat-Lock.

## Journale

- Journalzeile sichtbar ausfuellen reicht nicht.
- Nach Eingabe lokalisierte Zahlenformate pruefen, zum Beispiel `-68.000,00`.
- `Journal Check` oder gleichwertiger Preflight muss vor Buchung gelesen werden.
- FactBox sichtbar lassen, wenn sie selbst der Nachweis ist, etwa `Journal Check`.
- Bei Item Journals Kostenfelder nicht mit `Applies-to Entry` verwechseln.

## Tabellen / Grid

- Nach `FIXEDASSETS-172` gilt fuer BC-Journalgrids zusaetzlich: Sichtbare Spalten `Amount`, `Bal. Account Type` und `Bal. Account No.` sind kein Schreibnachweis. Ein Live-Lauf darf `Bal. Account No. = 82000` erst eingeben, wenn ein Helper eine eindeutig editierbare Zielzelle findet, diese Zelle an die Zielzeile bindet und danach den sichtbaren Wert in derselben Journalzone nachweist. Wenn `0` editierbare Kandidaten gefunden werden, ist das ein Lines/Subform-Helper-Blocker, kein Setup-Erfolg.
- Nach `FIXEDASSETS-174` gibt es dafuer den lokalen Helper `playwright/core/bc/journal-grid-candidates.ts`. Er trennt sichtbare Header, isolierte Zielzeile, verbotene Signale, editierbare Controls und bereits sichtbare Zielwerte. Der Helper ist ein Gate, keine Klick-Automatik: `single-editable-candidate` erlaubt erst einen spaeteren eng bewachten Live-Schritt; `blocked-missing-row-anchor` oder `blocked-no-editable-candidate` stoppt Werteingabe.
- Nach `FIXEDASSETS-175` gilt fuer Zielwerte in Journalgrids: `already-visible` darf nur gelten, wenn der Wert in der Zielzeile und Zielspalte verankert ist. Ein globaler Seitentext- oder Gridtreffer wie `82000` irgendwo im Snapshot ist kein Gegenkonto-Beweis fuer `G05001 / FA-CNC-01 / HGB`.
- Nach `FIXEDASSETS-176` prueft der Helper diese Regel lokal: `expectedValueVisible` wird nur aus row-anchored Kandidaten abgeleitet. Ein fremdes `82000` in einer anderen Zeile bleibt negativ; ein `82000` in der Zielzeile/Zielspalte kann als `already-visible` zaehlen. Der naechste praktische Schritt ist read-only Live-Probe, nicht direkte Werteingabe.
- Nach `FIXEDASSETS-113` gilt fuer BC-Journalgrids: Header-Sichtbarkeit und koordinatenbasierter Zellfokus sind keine Schreibreife. `FIXEDASSETS-110` sah `Amount` und `Bal. Account No.` als Header, fand aber keine konkreten Controls. `FIXEDASSETS-112` fokussierte read-only und verlor danach Page-/Zielzeilen-Signale. Dieser Pfad ist fuer Werteingabe blockiert, bis ein echter feld-/zeilenbasierter Control-Nachweis existiert.
- Nach `FIXEDASSETS-157` gilt zusaetzlich fuer BC-Journalgrids: Ein Screenshot kann die sichtbare Grid-Zeile beweisen, waehrend `innerText`/DOM-Auswertung nur Shell-Text liefert. In solchen Faellen die Evidence trennen: visuelle Screenshot-QA fuer Buch-/Lernwert, technischer Extractor-Blocker fuer Playwright-Lernen. Ein sichtbares Grid-Bild mit `G05001` / `FA-CNC-01` ist noch keine Werteingabe- oder Postingfreigabe, wenn Betrag, Gegenkonto und Ownership-Entscheidung fehlen.
- Nach `P2P-002` gilt fuer Payment-Journal-Grids: Control-Indizes duerfen nicht von einem anderen Journaltyp uebernommen werden. Das Payment Journal zeigte zusaetzliche Spalten wie `Document Date`, `External Document No.`, `Recipient Bank Account`, `Currency Code` und `Payment Method Code`; dadurch schrieb der erste Füllplan Betrag, Vendor und Bankwerte in falsche Spalten. Vor Werteingabe in Journal-/Subform-Zeilen muss der Test die sichtbare Header-Reihenfolge oder einen feldgebundenen Helper nutzen und danach die Zielwerte in derselben sichtbaren Zeile pruefen.
- Nach `FIXEDASSETS-055` gilt fuer Belegzeilen: Zielcode-Sichtbarkeit allein reicht nicht. `FA-CNC-01` in einer `Vendor Card` ist kein Anlagenzeilennachweis. Ein Belegzeilen-Helper muss nach jedem Klick/Tab pruefen, dass der aktive Kontext weiterhin Purchase Invoice plus Lines/Grid ist; `Vendor Card`, Vendor-Registrierungsdialoge oder Stammdaten-Popups sind harte Stop-/Rejected-Kriterien. Entstandene Drafts muessen sofort per UI bereinigt und als Cleanup-Evidence dokumentiert werden.
- Nach `FIXEDASSETS-056` gibt es dafuer den wiederverwendbaren Guard `classifyPurchaseInvoiceFieldMappingText()` in `playwright/core/bc/purchase-invoice-guards.ts`. Fuer Anlagen-Einkaufsrechnungszeilen ist Erfolg nur `Purchase Invoice` + Lines/Grid + Vendor `K30000` + Zeilentyp `Fixed Asset` + `FA-CNC-01` im Zeilenkontext. `Create a new vendor card`, `Vendor Card`, `V00040` oder `FA-CNC-01` ohne sichtbaren `Fixed Asset`-Zeilentyp stoppen den Lauf.
- Nach `FIXEDASSETS-057` darf ein neuer Purchase-Invoice-Field-Mapping-Retry nur als neuer Testfall laufen, nicht als Wiederholung von 055. Der Guard ist nach jeder Kopf-/Zeilenaktion Pflicht; Preview, Post, Zugang und AfA bleiben bis zu separaten Gates tabu.
- Nach `FIXEDASSETS-058` gilt zusaetzlich: Ein erfolgreich geklickter `Neu`-Kandidat auf `Purchase Invoices` beweist noch keinen aktiven `Purchase Invoice`-Card-/Zeilenkontext. Wenn der Guard nach `Neu` keinen stabilen Beleg-/Lines-Kontext erkennt, darf kein Zielwert eingegeben werden. Der naechste Helper muss den Vordergrund-Card- oder Grid-Kontext explizit beweisen, bevor `K30000`, `Vendor Invoice No.` oder `FA-CNC-01` gesetzt werden.
- Nach `FIXEDASSETS-059` unterscheidet der Guard `blocked-list-or-inline-row-context`: Sichtbare `Purchase Invoices`-Listen-/Inline-Signale nach `Neu` sind ein Stop-Kriterium, solange nicht zugleich aktive `Purchase Invoice`-Card, Pflichtfelder und Lines/Grid als Vordergrundkontext bewiesen sind. Der naechste Lauf darf deshalb nur Card-/Lines-Kontext pruefen und noch keine Zielwerte setzen.
- Nach `FIXEDASSETS-061` ist der naechste Purchase-Invoice-Zielwerte-Lauf nur als enger Field-Mapping-Preflight freigegeben. Der Guard muss nach jeder Aktion laufen. Erfolg zaehlt erst, wenn `K30000`, ein sichtbarer `Vendor Invoice No.`-Wert, `Type = Fixed Asset` und `FA-CNC-01` im selben Purchase-Invoice-/Lines-Vordergrundkontext sichtbar sind. Jede Vendor Card, jeder Vendor-Registrierungsdialog, `FA-CNC-01` ohne sichtbaren Zeilentyp `Fixed Asset`, Preview-/Post-Dialog oder nicht bereinigbarer Draft stoppt den Lauf.

- Breiter Viewport und breite Layoutansicht sind erlaubt, wenn dadurch der fachliche Zielbereich sichtbar wird.
- Horizontalen Grid-Scroll gezielt am BC-Container nutzen und visuell pruefen, ob die Zielspalten sichtbar sind.
- Tabellenbilder muessen den Zielwert zeigen, nicht nur irgendeine Liste.
- Geometrie- und Koordinatenklicks sind nur begruendete Fallbacks; danach muss ein sichtbarer fachlicher Zustand nachgewiesen werden.
- Bei bestehenden Listendatensaetzen reicht der Listentreffer nicht. Fuer Buch-/Setup-Reife immer die Karte oder Detailseite oeffnen und die Werte pruefen, die der Screenshot behauptet.
- `FIXEDASSETS-039` zeigt die Grenze bei Karten-Defaults: Eine Vendor Card mit `K30000` und Name ist ein Stammdatenbeweis, aber kein vollstaendiger Buchungsdefault-Beweis. Wenn Payment Terms/Payment Method sichtbar sind, Posting Groups, Currency oder Tax/VAT aber nicht, muss der Lauf als partieller Read-only-Nachweis enden und eine Diagnose mit `Mehr anzeigen`, Personalisieren oder Page Inspection vorbereiten.
- `FIXEDASSETS-041` verschaerft die FastTab-Regel: Ein registrierter Klick auf einen FastTab-Header oder Wertstreifen zaehlt nicht als aufgeklappt, solange danach nicht konkrete Zielcaptions oder Zielwerte sichtbar sind. `Payments 1M(8D) BANK` ist ein sichtbarer Zahlungswert, aber kein Beweis fuer `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` oder Tax/VAT. Fuer Folgelaeufe braucht es einen gezielten Chevron-/aria-expanded-Helfer oder eine bewusste Diagnose mit Personalisieren/Page Inspection.
- `FIXEDASSETS-042` entscheidet die Reihenfolge fuer solche Faelle: zuerst FastTab-Chevron/`aria-expanded` mit harter Nachbedingung, danach erst Personalisieren als UI-Verfuegbarkeitsdiagnose, danach Page Inspection als technischer Page/Table/Field-Nachweis. Ein Setup-Fit ist erst ein spaeteres Gate, wenn die UI-/Sichtbarkeitsfrage geklaert ist.
- `FIXEDASSETS-043` belegt das bessere FastTab-Muster praktisch: Breiten Viewport nutzen, FactBox ausblenden, echte kleine `aria-expanded=false`-FastTab-Controls gegen kompakte Caption-Elemente suchen und keine Body-/Topbar-Elemente als FastTab akzeptieren. Der Screenshot zaehlt nur fuer sichtbare Ziele: `Tax Liable` und `Tax Area Code` sind belegt; `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group` bleiben offen.
- `FIXEDASSETS-045` ergaenzt das Settings-/Personalisieren-Muster: Der Einstellungen-Pane darf als read-only Diagnoseeinstieg fotografiert werden, wenn `Personalisieren` sichtbar ist. Das beweist nur den Einstieg, nicht die Verfuegbarkeit oder Werte versteckter Felder. Nach dem Screenshot muss der Pane gezielt geschlossen werden; sonst kann BC in einen Resize-/Side-Pane-Fokus wechseln und Folgeschritte lesen nur noch Shell-Text.

## Personalisieren und Einstellungen

- Personalisieren ist ein UI-Diagnosewerkzeug: Es zeigt, welche Felder, Spalten oder Aktionen auf einer Page verfuegbar, aber aktuell ausgeblendet sind.
- Wenn ein erwartetes Feld, eine Spalte oder eine Aktion in einer Klickanleitung fehlt, zuerst pruefen, ob sie ueber `Personalisieren`, Ansichten, Seiteneinstellungen oder Roll-/Profilanpassung sichtbar gemacht werden kann.
- Personalisierung ist nutzer-, rollen- und profilabhaengig. Screenshots muessen deshalb markieren, ob sie Standardansicht, Nutzer-Personalisierung oder Profilanpassung zeigen.
- Personalisieren beweist keine Tabellen-, Posting- oder Steuerlogik. Es beweist nur, dass ein UI-Element fuer diese Page verfuegbar gemacht werden kann.
- Personalisierungsmodus nur als Debug-/Lernbild fotografieren. Fuer Buchbilder danach in die normale Ansicht zurueckkehren und erneut pruefen, ob das fachliche Ziel sichtbar ist.

## Page Inspection / Seitenpruefung

- `Ctrl+Alt+F1` oeffnet laut Microsoft die Page Inspection. Alternativ: `?` / Help & Support / `Inspect pages and data`.
- Page Inspection ist ein technisches Diagnosewerkzeug: Page Name, Page ID, Page Type, Source Table, Table ID, Felder, Filter und Extensions helfen zu klaeren, auf welcher BC-Page ein Klickpfad wirklich steht.
- Fuer Klickanleitungen Page Inspection nutzen, wenn Seitenname, Page-ID, Tabellenbezug, Feldherkunft oder Extension-Einfluss unklar sind.
- Page Inspection ist besonders nuetzlich fuer Bugfixing: falsche Page, falscher ListPart, andere Tabelle, Extension-Feld oder versteckter Filter lassen sich schneller erkennen.
- Page-Inspection-Screenshots sind Debug-/Evidence-Bilder, aber keine finalen Prozessbilder. Fuer Buchbilder danach wieder die normale Anwendersicht zeigen.
- `FIXEDASSETS-023` zeigt den praktischen Nutzen: Page Inspection bestaetigt die Vordergrundpage `Fixed Asset Card (5600)`, obwohl die Hintergrundliste noch DOM-Treffer liefern kann. Deshalb ist Page Inspection ein Diagnoseanker, aber kein Ersatz fuer einen sauber gescopten Karten-Locator.
- Manche Browser, Remote-Desktop- oder Windows-Konfigurationen koennen `Ctrl+Alt+F1` abfangen. Dann den Help-&-Support-Pfad oder die BC-Suche nach `Page Inspection` / `Seitenpruefung` pruefen.

## FactBox und breite Layouts

- FactBox einklappen, wenn sie Tabellenfelder verdeckt.
- FactBox sichtbar lassen, wenn sie selbst der Nachweis ist.
- Breite Layoutansicht ist fuer Ledger, Setup-Listen, Analysis Views, Journal Check und Screenshot-QA oft besser als Standardansicht.
- Vor einem Buchbild pruefen: Sieht der Leser das konkrete, was er lernen soll?

## Teaching Tips

- Teaching Tips duerfen gezielt geschlossen werden, wenn sie das Lernziel verdecken.
- Wenn ein Teaching Tip selbst Lernwert hat, separat als UI-Fundstelle dokumentieren.
- Wegklicken ist kein Fehler, muss aber im Workaround-/Pattern-Wissen stehen, wenn es wiederkehrend ist.

## Dialoge

- Dialoge sind Sicherheitsgrenzen: Posting, Apply Entries, Templates, Confirm, Delete, OK/Cancel.
- Neuer Helper-Baustein: `playwright/core/bc/dialogs.ts` mit `expectBcDialog()`, `isBcDialogVisible()` und `clickBcDialogButton()`. Vor einem Buttonklick wird der erwartete Dialogtext als Sicherheitsanker gelesen.
- Vor `OK` oder `Ja/Yes` muessen Dialogtitel, Option und fachliches Ziel klar sein.
- Bei Setup-/Stammdaten-Preflight ist `Cancel/Abbrechen` der Standardabschluss.
- Template-Dialoge wie bei Vendor Cards sind eigener Nachweis: sie zeigen, dass Anlage nicht einfach "New und fertig" ist.
- FA-188 ergaenzt fuer Journal-Preview-Pfade: Ein Klick auf den groben `Post`-Aktionspfad kann den normalen Buchungsdialog oeffnen, ohne dass `Preview Posting` ausgewaehlt wurde. Das ist ein Stop-/Rejected-Path, kein Preview-Nachweis. Vor einem weiteren Live-Retry braucht es eine gescopte Action-Inventory-Route oder einen eindeutigeren Preview-Posting-Selector; ein geoeffneter Posting-Dialog darf nicht mit `OK`/`Yes` bestaetigt werden.
- FA-189 entscheidet daraus: Der naechste Schritt ist read-only Action Inventory. Erst wenn `Preview Posting` als eigener sichtbarer/gescopter Kandidat belegt ist, darf ein neuer Preview-only Live-Versuch geplant werden. Der normale `Post`-Dialog bleibt ein Stop-Kriterium.
- FA-190 zeigt fuer `Fixed Asset G/L Journals`: Eine read-only Frame-Aktionsinventur fand keinen direkten `Preview Posting`-Kandidaten. Sichtbare Kandidaten wie `Post`, `Verwandte Aktionen fuer Post`, `Insert FA Bal. Account`, `Reconcile` und deaktiviertes `Apply Entries...` sind nur Aktionslandschaft, keine Klickfreigabe. Ein weiterer Live-Schritt braucht erst Review und einen separaten No-Post-Menueinventur-Case oder einen anderen Preflight-Pfad.
- FA-191 Review-Regel: Wenn eine Page nur einen Post-Splitbutton zeigt, darf der naechste Live-Lauf hoechstens den Dropdown-/Splitbutton inventarisieren. Danach sofort Labels sichern und abbrechen; keine Menuepunkte klicken, keinen Haupt-`Post`-Button klicken, keine `OK`/`Yes`-Bestaetigung.
- FA-192 zeigt: Der Splitbutton `Verwandte Aktionen fuer Post` kann ein Menue mit `Post`, `Preview Posting`, `Post and Print` und `Test Report...` oeffnen. Das ist ein Menueinventar, keine Ausfuehrung. Dialog-/Posting-Erkennung darf dabei nicht aus dem gesamten Seitentext auf `Post`/`Invoice` triggern, sondern nur echte Dialogcontainer oder eindeutige Confirm-Texte werten.
- FA-193 entscheidet: Ein einzelner `button[role=menuitem]` mit Text/Aria-Label `Preview Posting` darf genau einen separaten Preview-only-Liveversuch freigeben. Der Test muss vorher erneut den Splitbutton oeffnen, exakt einen Preview-Kandidaten pruefen, nur diesen Kandidaten klicken und bei jedem Posting-Dialog ohne `OK`/`Yes` stoppen. `Post`, `Post and Print`, `Insert FA Bal. Account` und `Reconcile` bleiben gesperrt.
- FA-194 bestaetigt den gescopten Preview-Pfad: Nur der exakte `Preview Posting`-Menuepunkt wurde geklickt; danach oeffnete BC die Seite `Error Messages`. Das ist ein Preview-Fehlerbild und kein Buchungsnachweis. Der naechste Schritt muss die Fehlermeldung fachlich lesen, bevor Setup geaendert oder ein weiterer Preview-/Posting-Lauf geplant wird.
- FA-195 Review-Regel: Eine erreichte `Error Messages`-Seite ist noch keine Fehlerursache. Vor Setup-Fit, Retry oder Posting muss ein eigener read-only Lauf die konkrete Fehlermeldung, betroffene Felder und moegliche Ursache sichern.
- FA-196 zeigt die Grenze gespeicherter Error-Messages-URLs: Die Seite `Error Messages` kann spaeter read-only erreichbar sein, aber eine leere Liste anzeigen. Das beweist den Fehlerkontext nicht erneut. Wenn die Detailzeile leer ist, muss der naechste sichere Plan entscheiden, ob ein neuer Preview-only-Lauf die Error-Messages-Details unmittelbar nach dem Klick erfassen darf; keine Setup-Aenderung aus einer leeren Error-Messages-Liste ableiten.
- FA-197 entscheidet daraus: Eine leere spaeter geoeffnete `Error Messages`-Liste ist kein Setup-Beweis. Erlaubt ist genau ein neuer, eng bewachter Preview-only-Lauf, der unmittelbar nach dem exakten `Preview Posting`-Klick Error-Messages-Zeilen, Details/Feldwerte und Seitentext sichert. `Post`, `Post and Print`, `OK`/`Yes`, Journal-Edit und Setup bleiben gesperrt.
- FA-198 bestaetigt dieses Sofort-Capture-Muster: Der exakte `Preview Posting`-Menuepunkt oeffnete `Error Messages`; der Kernbefund ist `Gen. Journal Line ASSETS / DEFAULT / 10000`, ein UI-Hinweis `Waehlen Sie einen Wert fuer Batch Name` und die Meldung, dass `FA Posting Type` in `Gen. Journal Line` leer ist. Das ist ein konkreter Preview-Fehler, aber noch keine Setup-Ursache und keine Posting-Freigabe. Lange Spalten-/Aktionslisten duerfen nur Kontext sein; die Evidence muss den fachlichen Fehlerkern separat extrahieren.
- FA-199 entscheidet den naechsten Schritt: Fuer manuellen Anlagenzugang im Fixed Asset G/L Journal muss laut Microsoft Learn `FA Posting Type = Acquisition Cost` gesetzt werden. Die naechste Live-Aktion darf deshalb nur dieses vorhandene Journalzeilenfeld auf `Acquisition Cost` setzen und nach Reopen beweisen. Kein `Preview Posting`, kein `Post`, kein Setup-Fit und keine neue Journalzeile in diesem Schritt.

## Screenshots

- Screenshot-Typen: Navigation, Setup Before/After, Preflight, Posting Dialog, Posted Document, Ledger Trace, Report, Error, Rejected Path, Book Candidate.
- Buchkandidat nur, wenn das sichtbare Lernziel im Bild erkennbar ist.
- Wenn ein Screenshot mehrere fachliche Aussagen stuetzen soll, muessen alle behaupteten Codes/Werte sichtbar sein. Sonst ist das Bild nur partieller Kandidat und die nicht sichtbaren Werte bleiben offen.
- `FIXEDASSETS-041` ist ein aktuelles Negativbeispiel fuer Buchbilder: Die K30000-Bilder zeigen den Kreditor und Payment-Werte, aber nicht die gesuchten Posting-/Currency-/Tax-Codes. Sie bleiben Evidence, duerfen aber nicht als Field-Proof fuer die fehlenden Codes verwendet werden.
- `FIXEDASSETS-042` markiert deshalb den naechsten Screenshot-Typ als FastTab Visibility Proof: Das Bild muss den Kreditor plus den geoeffneten FastTab und die behaupteten Default-Felder oder Werte zeigen. Ein Bild ohne diese Codes bleibt Diagnose, nicht Buchkandidat.
- `FIXEDASSETS-043` ist ein partieller positiver FastTab-Proof: Das Invoicing-Bild zeigt den geoeffneten Bereich und Tax-Felder, aber nicht die Buchungsgruppen oder Waehrung. Es ist daher ein Buchkandidat fuer "Sichtbare Invoicing-/Tax-Felder lesen", nicht fuer "Kaufbeleg ist bereit".
- `FIXEDASSETS-044` ergaenzt die Gate-Regel: Ein partieller FastTab-Proof darf keinen Kaufbeleg-Preflight ausloesen, wenn die Felder fuer Kontenfindung, Waehrung oder VAT-Kontext nicht sichtbar sind. Der naechste Schritt ist dann UI-Verfuegbarkeit ueber Personalisieren und technische Page-/Feldwahrheit ueber Page Inspection, weiterhin read-only.
- `FIXEDASSETS-045` zeigt die Grenze der Diagnose: `Personalisieren` war sichtbar, aber nicht ausgefuehrt/gespeichert; Page Inspection per `Ctrl+Alt+F1` war im Playwright-/Browserkontext nicht stabil verfuegbar. Deshalb bleiben fehlende Vendor-Defaults offen. Ein Diagnosebild darf nicht als Beweis fuer unsichtbare Codes verwendet werden.
- `FIXEDASSETS-049` ist ein weiteres Negativbeispiel fuer Buchbilder: Der Personalisieren-Modus ist sichtbar, aber die Feldliste und die kritischen Captions sind nicht sichtbar. Das Bild ist ein guter Debugging-Screenshot fuer "Personalisieren wurde geoeffnet", aber kein Screenshot fuer "diese Defaults sind verfuegbar oder gesetzt".
- `FIXEDASSETS-016` ist ein positives Beispiel: Das Nachherbild zeigt nicht nur den Seitenkontext, sondern `MACHINES` plus `12210`/`82000` im sichtbaren Kartenbereich.
- `FIXEDASSETS-020` ist ein gutes Zwischenbild: Es beweist sichtbare Anlagenkartenfelder nach `Mehr anzeigen`, aber nicht die fertige Anlage. Solche Bilder muessen als Feldmapping-/Preflight-Kandidaten markiert werden.
- `FIXEDASSETS-022` ist ein Rejected-QA-Beispiel: Ein Bild, das nicht den behaupteten Code oder sogar den falschen Dialog zeigt, muss geloescht oder als `rejected/do-not-use` dokumentiert werden. Seitentext-Treffer allein reichen nicht, wenn das sichtbare Bild den Wert nicht zeigt.
- `FIXEDASSETS-023` ist ein Debug-Bild-Beispiel: Die Page-Inspection-Aufnahme darf fuer technische Nachweisfuehrung und Bugfixing genutzt werden, aber nicht als finales Anwenderbild fuer die Anlage.
- `FIXEDASSETS-029-EXISTING` ist ein Error-/Learning-Bild: Die Karte zeigt `FA-CNC-01`, aber die relevanten Anlagenfelder sind leer. Es ist deshalb kein Zielbild fuer Kapitel 21, sondern ein gutes Bild fuer "Code vorhanden, Stammdatensatz fachlich unvollstaendig".
- `FIXEDASSETS-032` ist ein gutes Teilfit-/Debugging-Bild: Das Bild zeigt die relevanten AfA-Werte wirklich, aber auch die weiter leeren Stammdatenfelder im Kopf. Es darf fuer "teilweise korrigiert, noch nicht buchungsreif" genutzt werden, nicht als finales Anlagenbild.
- `FIXEDASSETS-034` ist ein Composite-Proof-Beispiel: Das Finalbild zeigt `FA Subclass Code = EQUIPMENT` und AfA-Zeilen, aber nicht gleichzeitig `HGB` und `MACHINES`. Buch- oder Evidence-Text darf deshalb nur sagen: 034 belegt die Unterklasse, 033+034 zusammen belegen den Labor-Stammdatenfit.
- `FIXEDASSETS-039` ist ein partieller Vendor-Card-Proof: Die Bilder duerfen `K30000`, Name, `1M(8D)` und `BANK` belegen, aber nicht die nicht sichtbaren Buchungsgruppen, Waehrung oder Tax/VAT-Defaults.
- Metadaten muessen Status, Zweck, sichtbares Ziel, Labor/final-Grenze, Buchwirkung und Limitationen nennen.
- `pageText()` kann Screenshot-Kontext absichern, ersetzt aber keinen visuellen Beweis.

## Evidence

- Evidence muss kompakt und verwertbar bleiben: Markdown-Zusammenfassung, JSON-Ergebnis, wenige wichtige Screenshots.
- Jede Evidence beantwortet: Was wurde geprueft, wo, mit welchen Objekten, welches Ergebnis, welche Grenze, welche Buchwirkung, naechster Schritt.
- `compactPageText()` ist fuer fokussierte Seitentexte besser als lange Rohdumps.
- Keine `console-*.log`, `page-*.yml`, Traces, Reports, Auth-Dateien oder Secrets committen.

## Rejected Paths

- Ein gescheiterter UI-Pfad ist wertvoll, wenn Situation, Symptom, Ursache, BC-Logik, Loesung/Grenze und Buchwirkung dokumentiert sind.
- Rejected Path nicht wiederholen, solange kein neuer Hebel oder eine neue Freigabe existiert.
- Belegte Beispiele: Page `371` fuer Bankposten rejected, Analysis View `New/Neu` rejected, Financial-Reports-Dimensionswirkung fuer `PRODUCTLINE`/`CHANNEL` nicht belegt.

## Anti-Patterns

Stand Audit 16.06.2026:

- `waitForTimeout`: 419 Vorkommen in `playwright/core` und `playwright/projects/fibu-book5/tests`.
- `mouse.click`: 17 Vorkommen.
- `force: true`: 18 Vorkommen.
- `keyboard.press('Enter')`: 3 Vorkommen.
- `first/last/nth`: 182 Vorkommen.

Regeln:

- Feste Waits schrittweise durch `expect`, `expect.poll`, `waitForPageText()` oder sichtbare Dialog-/Grid-Ziele ersetzen.
- Koordinatenklicks nur als letzter Fallback und mit anschliessendem Zielnachweis.
- `force: true` nur mit Kommentar/Begruendung oder nach sichtbarer Elementdiagnose.
- `first/last/nth` nur mit fachlicher Trefferbegrenzung oder anschliessender Kontextpruefung.
- Kein ungescopter `New/Neu`, kein blinder Tell-Me-Enter, kein ungeprueftes `Post`.

## Wann API erlaubt ist

- Diagnose, kompakte Zustandspruefung, historische Laborfit-Klaerung oder maschinenlesbare Cross-Checks.
- Nur wenn klar markiert ist: API ist nicht Buch-Klickpfad.
- Keine API nutzen, um Setup fuer eine Klickanleitung "unsichtbar" herzustellen, ohne spaeteren UI-Nachziehpfad zu dokumentieren.

## Wann UI-first Pflicht ist

- Alles, was ein Anwender im Buch nachklicken soll.
- Stammdaten- und Setup-Fits, sobald daraus Screenshots, Lernwert oder Buchanleitung entstehen.
- Buchungen, Zahlungen, Ausgleich, Journale, Preview Posting, Posting Dialoge, Apply Entries und Fehlerbehebung.
- Jeder Schritt, bei dem ein Anfaenger verstehen soll: Was sehe ich, was tue ich, warum reagiert BC so, woran erkenne ich den Erfolg?

## Journal Grid Candidate Blocking

- FA-177 belegt eine wichtige Sicherheitsgrenze fuer virtualisierte BC-Journalraster: Ein sichtbarer Zielkopf wie `Bal. Account No.` reicht nicht, um eine Werteingabe freizugeben.
- Wenn Control-Text nicht eindeutig an einer fachlichen Zeile haengt oder durch Optionslisten-Text verunreinigt ist, muss der Helper mit `missing-row-anchor` blockieren, auch wenn zielartig wirkende Controls sichtbar sind.
- Die naechste Helper-Verbesserung sollte einen stabilen Feldkandidaten aus Grid-Geometrie/Header-Reihenfolge plus Zeilenkontext ableiten und danach erneut read-only laufen, bevor irgendein Journalwert eingetragen wird.

## Journal Grid Geometry Candidates

- FA-179 erweitert die Journalzellen-Kandidatenlogik um Header-/Control-Geometrie: Ein Control darf nur dann als Zielzelle gelten, wenn es im x-Bereich des Ziel-Headers liegt und auf derselben y-Zeile Controls mit den geforderten fachlichen Row-Signalen liegen.
- Diese Logik ist nur ein lokaler Helper-Beweis. Sie ersetzt keinen Live-Nachweis und unlockt keine Werteingabe ohne anschliessende read-only Probe.
- Bei virtualisierten BC-Journalrastern ist `rowText` allein zu schwach, weil Optionslisten und Fokuszustand den Text verunreinigen koennen.
- FA-181 bewertet die FA-180-Live-Probe: Ein einzelner editierbarer Geometrie-Kandidat darf nur einen eng bewachten Werteingabe-Preflight freigeben. Vor der Eingabe muss der Kandidat erneut gefunden werden; danach muss der sichtbare Wert im selben Kandidaten bewiesen werden. Preview Posting oder Post werden daraus nicht freigegeben.
- FA-182 belegt den Werteingabe-Preflight praktisch: Der erneut gefundene Kandidat `14` wurde per UI mit `82000` gefuellt und danach als `already-visible` im selben Kandidaten nachgewiesen. Das ist Journal-Feldwert-Evidence, aber noch kein Preview-/Posting-Nachweis.
- FA-183 zieht die Gate-Grenze: Ein sichtbarer Journal-Gegenkonto-Wert darf einen Preview-only-Lauf freigeben. Eine echte Buchung bleibt gesperrt, bis Preview-Postenzeilen oder ein Preview-Fehler separat belegt und reviewed sind.
- FA-185: Ein Journalwert direkt nach `fill()` ist nur ein UI-Control-Nachweis. Vor Preview/Post braucht der Lauf einen Persistenzbeweis nach sicherem Commit plus Refresh/Reopen; sonst kann BC beim erneuten Oeffnen wieder den leeren gespeicherten Journalwert zeigen.
- FA-186 belegt die Persistenz fuer `Bal. Account No. = 82000` nach Reopen per Grid-Input-Helper. Screenshot-QA ueber `pageText()` ist dafuer ungeeignet, wenn der relevante Wert nur als Input-Value im virtualisierten Grid vorliegt; in diesem Fall ist JSON-/Helper-Evidence massgeblich und das Bild bleibt kein Buchkandidat.

## Number Series Lines Grid Blocking

- TARGET-016C korrigiert ein Locator-Problem: sichtbare Spaltenkoepfe wie `Startnr.` duerfen nicht durch zusammengesetzte Texte mit Leerzeichen aus der Trefferlogik fallen. Header-Matching muss Caption und `aria-label` getrennt normalisieren.
- TARGET-016C zeigt ausserdem die Grenze von Zellselektion in Business-Central-Listen: `Liste bearbeiten` kann eine leere Zeile oeffnen und eine Zelle visuell markieren, ohne dass ein klassisches editierbares DOM-Element aktiv ist.
- Ausgewaehlte Zellen sind kein ausreichender Schreibbeweis. Der Screenshot `target-016c-040-after-unique-editor-write-attempt.png` zeigt nach dem Tippversuch markierte Seitentexte/Labels statt sichtbarer `Startnr.`-/`Endnr.`-Werte.
- Wiederholte Koordinaten-, Header- oder Selected-Cell-Typing-Routen sind fuer Nummernserienzeilen gesperrt, bis Page Inspection, Personalisieren oder eine andere Standard-BC-Route den echten Feld-/Editorpfad zeigt.
- Checkbox-Spalten wie `Offen` und `Luecken in Nummern zulassen` gehoeren zur Nummernserien-Wahrheit. Sie duerfen nicht als Randdetail ignoriert und nicht ohne eigenes Vorher/Nachher-Gate geaendert werden.
- TARGET-016D belegt, dass `Ctrl+Alt+F1` in diesem Kontext die Seitenueberpruefung oeffnet. Fuer die Nr.-Serienzeilen ist die technische Seite `No. Series Lines (457, List)` und die Tabelle `No. Series Line (309)` sichtbar.
- TARGET-016E trennt `setupWriteAttempted` von `setupChanged`: Ein Tastatur-/F2-Versuch ist erst dann eine Setup-Aenderung, wenn Werte nach Reopen sichtbar sind. Bei U-CUST waren `U-CUST00001`/`U-CUST99999` nach Reopen nicht sichtbar.
- `F2` ist in diesem Browser-/Shell-Kontext fuer Nummernserienzeilen kein sicherer Zell-Editor-Hebel: Das Bild `target-016e-025-after-f2-field-flow-fallback.png` zeigt den Microsoft-365-App-Launcher. Diese Route ist fuer weitere Nummernserien gesperrt.
- TARGET-016F belegt, dass `Personalisieren` auf `Nr.-Serienzeilen` ueber das Einstellungen-Pane als sichtbarer Textlink erreichbar ist, nicht zwingend als Role-Menuitem. Erfolgsbild ist der Balken `Wird personalisiert: Nr.-Serienzeilen` mit `Fertig`; nach reiner Inspektion ohne Feld-/Aktionsaenderung muss `Fertig` zum Verlassen geklickt werden.
- TARGET-016F zeigt auch einen falschen Menuepfad: Ein Klick auf scheinbar naheliegende `Weitere Optionen` kann im Nummernserienkontext ein Copilot/Generieren-Flyout statt eines fachlichen Action-Menues oeffnen. Screenshot-QA muss deshalb pruefen, was das Menue wirklich zeigt.
- TARGET-016G verschaerft die Personalisieren-Grenze fuer `Nr.-Serienzeilen`: Der Modus zeigt die Spalten `Startnr.`, `Endnr.`, `Luecken in Nummern zulassen` und `Offen` sowie einen roten Fokus-/Pflichtmarker bei `Startnr.`, aber kein sicher klickbares `Feld hinzufuegen`-/Add-field-Panel. Das ist wertvolle Feld-/Aktionskarten-Evidence, aber kein Schreibhebel. Der naechste Schritt muss daher eine source-backed oder Assisted-Setup-Route pruefen, nicht dieselben Grid-/F2-/Personalize-Add-field-Versuche wiederholen.
- TARGET-016H entscheidet source-backed: Fuer Nummernserienzeilen ist der naechste Schreibversuch nur ueber den offiziellen Standardpfad erlaubt: Nummernserie waehlen, `Zeilen`, `Neu`, dann `Startdatum`, `Startnr.` und `Endnr.` befuellen und per Reopen-Proof pruefen. `Personalisieren`, F2, Header-Koordinaten und Selected-Cell-Typing bleiben fuer diesen Block rejected, bis neue sichtbare UI-Evidence einen anderen Mechanismus zeigt. Checkboxen wie `Luecken in Nummern zulassen` und `Offen` bleiben default-locked.
- TARGET-016I zeigt die naechste Grenze: `Nr.-Serienzeilen` fuer `U-CUST` zeigt bereits eine sichtbare Sternzeile mit leerem `Startdatum`, rotem Pflichtmarker bei `Startnr.` und `Offen`-Checkbox. Ein Klick auf `Neu` im Lines-Kontext ist also kein ausreichender Fortschritt, wenn danach kein echter aktiver Editor bewiesen ist. Der naechste Helper muss zuerst den aktiven Grid-Editor technisch/sichtbar nachweisen und darf nicht erneut nur unter Spaltenkoepfe klicken.
- TARGET-016J korrigiert die technische Diagnose: In `Nr.-Serienzeilen` liegen die editierbaren Controls im Business-Central-Frame; Zellkandidaten muessen deshalb frame-aware Koordinaten und die Header-Band-Position nutzen. `Startdatum` und `Startnr.` wurden als echte aktive Inputs erkannt, `Endnr.` aber nicht ausreichend bewiesen. Ohne alle Pflichteditoren darf keine Werteingabe fuer Start-/Endnummern laufen.
- TARGET-016J zeigt auch eine neue Sicherheitsfalle: Ein harmlos wirkender Editor-Probe mit Fokus/Enter kann eine date-only Zeile speichern. Bei `U-CUST` blieb nach Reopen eine unvollstaendige Zeile mit `Startdatum 30.06.2026`, leerer `Startnr.` und leerer `Endnr.` sichtbar. Grid-Probes brauchen deshalb vorab einen Recovery-Plan: entweder die Zeile vollstaendig und korrekt befuellen oder sie sichtbar entfernen.
- TARGET-016K blockiert den einfachen Cleanup-Pfad: Ein toolbar-naher `Loeschen`-/Delete-Versuch ohne sichtbare Bestaetigungsdialog-Evidence entfernte die unvollstaendige `U-CUST`-Zeile nicht. Diese Route darf nicht wiederholt werden. TARGET-016L muss entweder eine row-scoped Bestaetigung/Context-Menu-Route beweisen oder die bestehende Zeile kontrolliert vervollstaendigen.
- TARGET-016L proves the safer recovery route: Instead of repeating Delete, focus the existing incomplete row, use the real active editor sequence `Startdatum -> Tab -> Startnr. -> Tab -> Endnr.`, then reopen. For `U-CUST`, `U-CUST00001` and `U-CUST99999` were visible after reopen. The screenshot still needs visual QA for the full date if a date claim matters; the strong claim is Start-/Endnummern persistence. This route can be reused for remaining U-* lines only with per-code before/reopen proof and without changing gap/open checkboxes.
- TARGET-016M scales the TARGET-016L active-editor route to `U-VEND`, `U-ITEM`, `U-SO`, `U-SINV`, `U-PO` and `U-PINV`. Each code needs its own before/after/reopen proof; success is not just substring-matching in JSON, but visible `Startnr.` and `Endnr.` in the correct `Nr.-Serienzeilen` context. Screenshot-QA for `U-VEND` showed the value area is visually tight, so companion text snapshots remain mandatory for exact number verification. This proves line ranges only; setup-page assignment, master data, Preview Posting and posting remain separate gates.
- TARGET-017 adds a setup-card rule: Sales, Purchase and Inventory setup pages can render target fields in a two-column card layout. A control on the same y-position in the right column is not automatically the same field as the left-column label; helpers must prefer the nearest label/control pair in the target column and keep diagnostics for rejected candidates. After reopening a setup page, assigned number-series values can render as visible text/buttons/links with `Details oeffnen` behavior rather than editable inputs. Reopen proof may therefore combine screenshot text and diagnostics, not only editable input values. TARGET-017 proved `U-SO`, `U-SINV`, `U-VEND` and `U-PINV`; `U-CUST`, `U-PO` and `U-ITEM` require Page Inspection/Personalize/FastTab discovery before any repeat attempt.
- TARGET-017B adds the setup-field-discovery boundary: If a setup-card field label is not visible after entering edit mode and expanding the relevant visible sections, Playwright must not invent an assignment route. `Purchase Order Nos.` produced visible field/control candidates in `Purchases & Payables Setup`, but `Customer Nos.` and `Item Nos.` were not visible in the tested card views. The next step is a conscious park/follow-up decision, not another blind typing loop. Field discovery evidence should include the setup page, intended field, candidate count, nearby labels/buttons and a screenshot; it proves route visibility only, not value assignment.
- TARGET-036D2G adds the current `No. Series` checkbox boundary for `U-VEND`: Microsoft source basis confirms `Manual Nos.` is the real field for manual numbers, but the visible Business Central list and DOM diagnostics did not expose a unique row-scoped `U-VEND` / `Manual Nos.` checkbox candidate. Do not repeat broad checkbox, header-only, cell-coordinate or filtered-list write routes. The next route must either use Page Inspection/field truth to prove the exact value route or consciously park manual vendor numbering.
- BC command bars can include hover/help text inside action inventory, for example `Neu Erstellen Sie einen neuen Eintrag` or `Liste bearbeiten Änderungen auf der Seite vornehmen`. Keep that text because it is useful for beginner explanation, but do not treat action hover text as proof that a row value changed.

## Known Setup Page Direct Routes

- TARGET-019B shows the preferred route for known setup pages: open direct `page=` URLs with the expected company parameter instead of opening Tell-Me/search. This avoids accidental search overlays and keeps screenshot evidence focused on the page itself.
- Direct page visibility is still not setup correctness. General Posting Setup, Customer Posting Groups, Vendor Posting Groups, Inventory Posting Setup and VAT Posting Setup can be captured read-only as context, but account correctness, VAT correctness and posting readiness require later setup/value, Preview Posting and entry-trace gates.
- TARGET-028 tightens the Posting Groups page route: Vendor Posting Groups in the visible German Universaarl UI is Page 111 (`Kreditorenbuchungsgruppen`, `Verbindlichkeiten-Konto`). The old Page 93 assumption opened a wrong `Projektposten-Vorschau` surface and must be treated as rejected evidence. For setup pages, require both the real visible page title and at least one page-specific field signal; global navigation text such as `Payables Agent` is not enough.
- If a direct setup page opens with visible `Neu`, `Bearbeiten`, `Buchen` or `Buchungsvorschau` text, record it as visible-but-not-clicked. Do not click command-bar actions during read-only preflight.
- TARGET-020 extends the route list for USt/VAT setup: VAT Business Posting Groups (470), VAT Product Posting Groups (471) and VAT Posting Setup (472).
- Compact page-text snapshots must filter Business Central shell/bootstrap lines such as `allowedEndpoints`, `allowedResources`, `clientId`, `authority`, `parentPageOrigin` and `upn` before writing Evidence. Screenshots and text snapshots should prove the setup context, not expose browser/session metadata.
- TARGET-027R tightens the screenshot truth gate for setup pages: hidden DOM/page text is not enough. A page counts only when visible DOM text and the PNG show the claimed page. Blank pages and Tell-Me/search overlays are rejected even if `pageText()` contains the target caption.
- TARGET-027R also separates overlay contexts: a target list/page may appear as a side page over a dimmed Role Center, but the proof must show the target title, relevant columns and no active search overlay. Role Center background alone is not the problem; missing target page content is.
- For VAT page recovery, Page `471` (`MwSt.-Produktbuchungsgruppen`) and Page `472` (`MwSt.-Buchungsmatrix Einr.`) were recovered read-only. `TARGET-027C4` then recovered Page `470` (`MwSt.-Geschaeftsbuchungsgruppen`) by clicking the smallest normalized visible Tell-Me result row. This unlocks only the next bounded INLAND/VAT19 group-write retry; it is not VAT setup, matrix, Preview or posting proof.
- TARGET-027S adds a route-parity warning for Page `470`: the older TARGET-020 direct-page route still has a visible Page-470 screenshot, but the newer custom recovery test can fall back to Role Center/Tell-Me and must not be accepted as proof. For the next VAT write-gate, reuse the TARGET-020 direct-page pattern for Page 470 and avoid treating search overlay text or hidden DOM text as a page proof.
- TARGET-027C tightens the write-gate rule for setup pages: before clicking `Neu` or `Liste bearbeiten`, the helper must locate a scoped target form/grid surface whose screenshot itself shows the setup list. Generic shell text such as `Neu`, `Neue` or `Beschreibung` is not enough. If direct page and exact search-result route still leave the visible UI on Role Center, block before write and capture rejected screenshots.
- TARGET-027C retry adds two important BC grid lessons. First, do not press `Escape` immediately after opening a target page from Tell-Me; in side-page/pane contexts it can close the newly opened target surface and send the run back to the Role Center. Second, active grid cell values can be visible in the PNG while missing from `pageText()`/`innerText`; screenshot QA and value-aware DOM candidates must be used before declaring a persisted setup row missing.
- TARGET-027C also shows why repeated setup writes need a duplicate guard. A second attempt to create the same VAT group can show `Nicht gespeichert` and a page error even when the first row has already persisted. Reopen proof is the deciding check. For INLAND/VAT19, the accepted proof is the final reopen screenshots, not the intermediate duplicate-error screenshot.
- TARGET-027D6 adds a Page-472 route-discovery boundary: `Settings -> Personalize` can open Role Center personalization instead of the active setup page. Treat that as wrong-target evidence, leave personalization, reopen the intended `page=472`, and only then continue with Page Inspection or route decisions. Page Inspection on the reopened target page can prove `VAT Posting Setup (472, List)` and table `VAT Posting Setup (325)` plus the INLAND/VAT19 row, but it still does not create a true editor for account values. Do not repeat D3/D5 cell typing for `3806`/`1406`.
- TARGET-044C uebertraegt diese Grenze auf Debitoren-/Artikelkarten: Eine Kartenroute aus einem frueheren Lauf beweist nicht, dass ein separater `Personalisieren`-Probe-Lauf automatisch im Kartenkontext bleibt. Wenn das sichtbare Bild wieder Role Center zeigt, ist der Lauf `blocked`, auch wenn URL- oder Frame-Textfragmente vorhanden sind. Fuer Listen gilt zusaetzlich: Zeile selektieren oder doppelklicken ist kein sicherer Kartenoeffner; bevorzugt wird der sichtbare Nummern-Link (`No.`/`Nr.`) oder eine eigene alternative Page-Route. Fehlermeldungen duerfen keine rohen Business-Central-Seitentexte mit Auth-/Shell-Metadaten in Evidence schreiben.
- TARGET-044D korrigiert einen konkreten False-Positive-Pfad: Ein breiter Texttreffer wie `Lieferung` kann auf FactBox-/gebuchte-Beleg-Links zeigen und von der Debitorenkarte weg navigieren. FastTab-Helper muessen deshalb echte Header-/Chevron-Controls mit `aria-expanded` bevorzugen, breite FactBox-Links ausschliessen und nach jedem Klick die erwartete Page-ID erneut pruefen. Sichtbare Begriffe wie `Gesperrt`, `Basiseinheit` oder `Lagerbestand` sind UI-Discovery, aber kein Wert-/Posting-Readiness-Beweis.
- TARGET-045B verschaerft die Screenshot-QA fuer Karten-FastTabs: Wenn `Fakturierung`/`Zahlungen` oder `Einstandspreise und Buchung` sichtbar sind, aber die Screenshot-Wahrheit zeigt, dass sie nicht zuverlaessig geoeffnet wurden, darf der Lauf nicht behaupten, die darunterliegenden Felder seien nicht vorhanden. Dokumentiere dann `visible-fasttab-but-route-insufficient` und waehle entweder Source-Mapping, Page Inspection oder einen gezielteren FastTab-Helper als naechsten Case. Keine Customer-/Item-Posting-Felder schreiben, solange nur Kartenkontext und keine genaue Feld-/Wertbindung bewiesen ist.
- TARGET-027D7 turns the D6 discovery into a route rule: the next Page-472 write attempt must visibly use `Weitere Optionen` / `Liste bearbeiten` before any editor probe. Only after true active editors are proven for `MwSt. %`, `Umsatzsteuerkonto` and `Vorsteuerkonto` may the test write `19`, `3806` and `1406`. If one required editor is missing, stop without writing. Do not delete the partial row in the same run; cleanup needs its own row-scoped confirmation proof.
- TARGET-027D8 proves the visible Page-472 `Weitere Optionen` / `Liste bearbeiten` route is still not enough for VAT account writes: `MwSt. %` exposed a textbox-like editor, but `Umsatzsteuerkonto` and `Vorsteuerkonto` did not expose true active editors. Do not run another direct list-edit/cell route for this row. The next case must decide a row-scoped cleanup/recreate route or a different standard UI action such as a safe copy/new-row path with confirmation evidence.
- TARGET-027D10 proves the safer next step after D8: inventory row-scoped actions before touching destructive actions. The focused `INLAND`/`VAT19` row exposes `Loeschen` candidates in the command/menu surface, but visible delete actions are not cleanup proof. Helper code must normalize German umlauts and mojibake variants when classifying dangerous actions. The only allowed follow-up is a separate row-scoped delete-dialog probe that captures the confirmation and cancels/closes it; no `Ja`/`Yes`/`OK` and no setup deletion without a later explicit cleanup case.
- TARGET-027D11 proves the row-scoped delete dialog behavior on Page 472: after row focus and More Options, the visible delete action opens `Fortfahren und loeschen?` with `Ja` and `Nein`. Clicking `Nein` leaves the `INLAND`/`VAT19` row visible after reopen. This unlocks only a cleanup decision case, not actual deletion. A future cleanup case must still be separate, must click `Ja` only after a fresh Smart Decision, and must immediately prove either the row is gone or stop on any unexpected state.
- TARGET-027D12 adds the cleanup/recreate separation rule: decide cleanup locally, then execute cleanup in one narrow case, and recreate/complete the correct row only in a later case after row-absence proof. Do not combine destructive cleanup and new setup creation in the same Playwright run, because screenshots and state traces become harder to trust.
- TARGET-027D13 adds a stronger destructive-action truth gate: even after the expected `Fortfahren und loeschen?` dialog and a controlled `Ja`, the row may still be visible after reopen. Therefore helpers must not treat dialog confirmation as setup-change proof. A cleanup is proven only by fresh reopen evidence that the exact target row is absent. If the row remains visible, mark the clicked route blocked and switch to a genuinely new UI hypothesis such as the row-inline ellipsis/menu, not another toolbar-/More-Options delete retry.
- TARGET-027D13C proves the useful recovery: the row-inline ellipsis (`Weitere Optionen anzeigen`) directly beside the target row can behave differently from the toolbar/menu delete route. When a row-level destructive action is needed, first find the row by visible text, then locate a button on the same y-band with row-menu labels/title, capture the menu state, click only the exact row-level delete action, confirm only the expected dialog, and finally reopen the page. The success criterion is row absence after reopen, not the `Ja` click.
- TARGET-027D14 proves the Page-472 recreate boundary after cleanup: `Liste bearbeiten` plus `Neu` can expose a transient blank matrix row, but the rowheader and header cells must not be mistaken for editable business fields. The screenshot may show default-looking cells such as `Normale MwSt.` and `0`, but that is not persistence. A correct matrix row is only proven by a reopen screenshot with `INLAND`, `VAT19`, `19`, `3806` and `1406` bound to the same row. If the first mandatory cell does not expose a true active editor, stop before typing and switch to card/Page-Inspection/field-route diagnosis.
- TARGET-027D15 adds the no-write diagnosis rule for Page 472: after `Neu`, the transient row can be inspected by Page Inspection as `VAT Posting Setup (472, List)` / table `VAT Posting Setup (325)` with empty field values. `Bearbeiten` on this surface remains list/grid behavior; it does not prove a safe card route. Active-element evidence must filter BC shell/bootstrap text before writing JSON. The next step is a local route decision, not another value-typing attempt.
- TARGET-027D16 turns that diagnosis into the next route rule: do not repeat blind Page-472 grid typing and do not treat Page Inspection as a write route. Before any future `INLAND`/`VAT19` write, run a no-write lookup/detail/select discovery for the first two fields. The helper should capture tooltip/detail-button state, whether a lookup opens, whether it navigates away, and whether the page reopens with no accidental target row. Only a later case may select values, and only if D17 proves exact controls and stop conditions.
- TARGET-027D17 closes that hypothesis for the current Page-472 surface: hover/click/Alt+ArrowDown on the first candidate field after `Liste bearbeiten` + `Neu` did not expose a safe lookup/listbox/dialog/select affordance, and the transient new row disappeared before a second candidate could be probed. After reopen, no `INLAND`/`VAT19` row persisted. Do not repeat Page-472 grid typing, `Alt+ArrowDown`, or first-field lookup probes for the VAT matrix. The next route must be a local/source decision for a non-grid standard setup path, with BC shell/bootstrap text sanitized before any evidence JSON is written.
- TARGET-027D18 selects the next non-grid route: read-only Configuration Packages discovery before any write. For this route, Playwright must treat `Import`, `Export`, `Validate`, `Apply`, `Edit in Excel` and publish-like actions as dangerous controls. D19 may inventory them and screenshot them, but must not click them. A configuration package is a standard UI/setup-data mechanism, not an API shortcut, but it is advanced and can become effective, so the first pass must be read-only and screenshot-truth gated.
- TARGET-027D19 proves direct Page `8615` (`Konfigurationspakete`) works in `playthru` / `UNIVERSAARL-DE` without opening Tell-Me/search. The bottom-left information tile can contain words such as `Importieren` and `Exportieren`; treat it as explanatory page text, not as a dangerous dialog, unless an actual modal/confirmation surface with effective controls is visible. Action inventory must filter huge page containers and keep compact controls only. The visible `Neu`, `Paket importieren...`, `Paket exportieren...` and `Tabellen abrufen...` actions are read-only inventory in D19 and must not be clicked. D19 did not expose Table `325` / `VAT Posting Setup` without creating package metadata, so the next step is a local D20 route decision before any package/table creation.
- TARGET-027D20 adds the metadata-before-values rule for Configuration Packages: a temporary package header or package-table row may be a valid discovery surface only in a dedicated case with before/after/reopen evidence and cleanup/keep reasoning. Do not treat package metadata as VAT setup. `Import`, `Export`, `Validate`, `Apply`, `Edit in Excel`, publish-like actions and direct VAT matrix values remain hard stops unless a later case explicitly unlocks them.
- TARGET-027D21 adds a duplicate/new-card guard for Configuration Packages: after `Neu`, BC can open Page `8614` (`Paketkarte konfigurieren`) while an existing package row is visible behind the card. Do not mistake the package code `U-VAT325-DISC` for a Table-325 proof. Also do not keep clicking `Neu` or blind-editing `Tabellen-ID 0` when the active card is a blank duplicate context. The next step must be a local cleanup/keep/park decision or a more specific existing-record-open route.
- TARGET-027D23 adds two cleanup/dialog lessons for Configuration Packages. First, German BC action labels may expose native umlauts in accessible names (`Löschen`), so destructive-action locators must cover both native and normalized forms. Second, teaching tips can use `role=dialog`; dialog gates must dismiss or ignore explanatory tips before treating a surface as a destructive confirmation. In D23, toolbar `Löschen` and a row-inline delete attempt did not expose a trusted confirmation dialog and the exact `U-VAT325-DISC` row remained visible after reopen. Do not repeat the same cleanup clicks; run a local blocker review and choose a different route or explicit park decision.
- TARGET-021 extends the route list for Dimensions: Dimensions context (560) and General Ledger Setup global dimension fields (118). An empty Dimensions list is a useful finding, not a setup success.
- TARGET-023 corrects the writable dimension route: Dimension codes are handled on page `536` (`Dimensionen`), while Dimension Values can be opened directly on page `537` with a Dimension Code filter. Do not treat page `560` as the setup-write surface.
- TARGET-023 also shows a current BC grid limitation: page `537` makes `Neu` and `Liste bearbeiten` visible, but the current helper only proves the first value per dimension (`SOFTWARE`, `ADMIN`, `DIRECT`). Additional values need a control-level row-entry recovery before master data; do not repeat blind `Neu`/keyboard routes.
- TARGET-023B replaces that blocked route: for additional Dimension Values, open page `536` (`Dimensionen`), select the dimension row, use the `Dimension` action group, then open `Dimensionswerte`. In that related-list context, the last row exposes usable textboxes for `Code` and `Name`; after typing, reopen the same related list and verify grid values. Direct filtered page `537` remains good for visibility/reopen checks, but not as the first write surface for extra rows.
- TARGET-023B also adds a screenshot-QA lesson: if a row is visibly present but `getByRole('row', { name })` misses it, close teaching tips and fall back to row text/textbox selection before declaring the row missing.
- TARGET-024B shows that General Ledger Setup global dimension fields need a narrower inspection route. Page `118` shows `Globaler Dimensionscode 1/2`; direct card edit did not persist values. The action `Globale Dimensionen aendern...` opens an action page with left and right input columns plus `Fortlaufend`/`Parallel`. Filling the visible left column and clicking the sequential route returned to Page `118`, but the fields were still empty after reopen. Do not repeat this route blindly; TARGET-024C must distinguish current/new columns, true execution actions and Page Inspection field names before another write attempt.
- TARGET-024C adds the concrete UI learning for `Globale Dimensionen aendern...`: the action label uses the real German umlaut form `aendern/ändern`, so locator patterns must cover both normalized and native UI text. After opening the action page, a second edit-mode activation exposes one left editable combobox per `Globaler Dimensionscode` row. The right-side textbox and the check/update control are disabled/read-only. `Fortlaufend` and `Parallel` are run actions, not discovery controls; do not click them unless the active case explicitly unlocks a controlled assignment route and requires Page-118 reopen proof.
- TARGET-024D corrects the action semantics: `Fortlaufend` is a tab/processing mode, and the actual execution control below it is `Starten`. Screenshot-QA must catch this two-step pattern. In the controlled route, `PRODUCTLINE` and `COSTCENTER` were entered into the left editable comboboxes and `Starten` was clicked, but Page `118` still showed empty `Globaler Dimensionscode 1/2` fields after reopen. Do not repeat this same route; the next step needs source/Page-Inspection/alternative-route evidence or a conscious park decision before master data.
- TARGET-024E adds the source-backed routing rule for global dimensions: Microsoft Learn anchors global dimensions in `General Ledger Setup` / the Dimensions FastTab. After TARGET-024D failed, the next hypothesis must target Page `118` fields directly with tooltip/control/Page-Inspection proof and a Page-118 reopen check. Page `577` (`Globale Dimensionen aendern...`) must not be the primary repeated route unless a new source-backed log/session hypothesis exists.
- TARGET-024F adds the Page-118 edit-mode lesson: the visible Business Central card lives inside an iframe, so toolbar actions such as the German pencil button `Aenderungen auf der Seite vornehmen` must be searched frame-aware. A visible command-bar word such as `Buchen` is a risk signal to record, not a blocker when no action is clicked. In edit mode, the Page `118` `Globaler Dimensionscode 1/2` fields did not expose direct textboxes or comboboxes; they rendered as `A role=button` controls with `Details oeffnen...` titles. Do not retry direct textbox typing. The next route must test those exact lookup/detail buttons, stop on related-record navigation, or park global-dimension assignment for Page Inspection/source review.
- TARGET-024G/TARGET-024H close the current Global Dimension assignment loop: clicking the Page `118` `Globaler Dimensionscode 1` detail button did not expose a selectable `PRODUCTLINE` lookup, and Page `118` still did not show `PRODUCTLINE`/`COSTCENTER` after reopen. After Page `577`, direct Page `118` textbox, and Page `118` button routes all failed to prove persistence, the next efficient rule is to park Global Dimension Code assignment and continue core master-data planning with a clear no-claim boundary. Do not block all master data just because global dimensions are parked; do block any book/coverage claim that `PRODUCTLINE` or `COSTCENTER` are global dimensions.
- If compact BC text contains mojibake, normalize it before writing result/text evidence. The screenshot remains the visual source; JSON and `.txt` evidence must stay readable and token-light.

## Master Data List Preflight

- TARGET-025 proves a safe read-only route for the core master-data lists in `playthru` / `UNIVERSAARL-DE`: Debitoren (Page 22), Kreditoren (Page 27), Artikel (Page 31) and Lagerorte (Page 15) can be opened directly without Tell-Me/search and without clicking `Neu`.
- Empty lists are useful evidence, but only for current list state and page readiness. They do not prove templates, mandatory fields, posting groups, VAT defaults, number-series readiness or future card creation.
- Teaching tips at the lower left are a double-edged BC pattern. They can explain import/template affordances for beginner screenshots, but they also obscure page content. Each screenshot must decide whether the tip is part of the learning target or should be closed before field-level evidence.
- FactBoxes on the right are useful when they explain list context, statistics or card-side details. They must not be allowed to hide grid columns needed for the claim; for list/grid proof, prefer a wider viewport, focus mode or a collapsed FactBox when the table itself is the target.
- A visible `Neu` action on a master-data list is not a creation proof and not a permission proof. It must be recorded as visible-but-not-clicked unless the active case explicitly unlocks card creation and the result contains a cleanup/trace plan.

## Chart of Accounts Preflight

- TARGET-026B proves the preferred read-only route for the Universaarl Kontenplan: open direct Page `16` in `playthru` / `UNIVERSAARL-DE`, capture page text, candidate-signal JSON and at least one screenshot after moving mouse focus away from hover tooltips.
- TARGET-026F adds the controlled setup route for the first Universaarl G/L account: Page `16` list edit can create/confirm `Nr.` and `Name`, but `GuV/Bilanz` may remain wrong if treated as a plain list cell. Screenshot-QA caught `1200 Bank Saarland` as `GuV`; the successful correction opened the `Sachkontokarte` / G/L Account Card, focused `Name`, tabbed into `GuV/Bilanz`, selected `Bilanz` with keyboard, and reopened Page `16` for proof. For remaining accounts, success requires both account visibility and correct `GuV/Bilanz` after reopen.
- TARGET-026J shows a safer account-creation route after a list-edit false start: `Liste bearbeiten` + `Neu` can leave no editable `Nr.`/`Name` controls even when the page says the list has more items. Reset to the normal list page, click `Neu` without the active edit-list state, then fill the newly exposed row/card fields. Screenshot-QA must verify the account number near the name, because `1200 Bank Saarland` and `1800 Bank Saarland` can both be visible.
- TARGET-026M recovery adds a strict screenshot-QA rule for option fields: `GuV/Bilanz` as a field label is not the same as field value `GuV`. A helper must read or prove the value itself and then reopen the list/card. In this run the filtered `Sachkontokarte` route reached an editable-looking field, but the card-after-attempt and chart-reopen screenshots still showed `Bilanz`; therefore `4400` and `5400` remain blocked.
- TARGET-026M Page Inspection follow-up adds the German Page Inspection pattern: `Ctrl+Alt+F1` can open `Seitenueberpruefung` on the right pane. For the `Sachkontokarte` it exposed Page `G/L Account Card (17, Card)`, Table `G/L Account (15)` and field `Income/Balance (9, Option)`. This is stronger than raw page text, but it still showed value `Bilanz`, not `GuV`; the next write route must use this field truth and then prove by reopen.
- TARGET-026M safe-write follow-up proves the bounded correction pattern for BC card option fields: first use Page Inspection or screenshot QA to identify the real field; then enter real card edit mode; then change only the targeted option field; finally reopen the card and the list. The edit icon coordinate must be derived from the screenshot if labeled locators fail. For the GuV/Bilanz field, the only accepted success state is visible value `GuV` plus `Kontoart` = `Buchung` after reopen. Do not unlock VAT setup, posting groups or master data from the in-edit screenshot alone.
- A blank Chart of Accounts grid is strong foundation evidence. It proves page reachability and missing visible account rows, but it does not prove account correctness, VAT account mappings, posting readiness or a final German account plan.
- Hover tooltips on column headers can be useful beginner explanations, but they are hover-state truth. For book or setup-gate screenshots, capture a second image after `Escape`/mouse-move so the claim is about the page, not the hover.
- Visible actions such as `Neu`, `Liste bearbeiten`, `Buchen` or `Bericht` must be recorded as visible-but-not-clicked during read-only preflight. They do not unlock account creation, VAT setup or posting.

## VAT Configuration Packages

- TARGET-027D22 parks the configuration-package route after `U-VAT325-DISC` was left as temporary metadata. A package code that contains `325` is not proof of Table `325` or VAT Posting Setup. Cleanup must target the exact package code only, and it must stop if the row or confirmation target is ambiguous.
- Cleanup evidence is only admin-metadata evidence. It does not prove VAT correctness, Posting Groups readiness, Preview Posting readiness, VAT Entries or G/L Entries.
