# TARGET-023B Dimension Values Route Recovery

Instanz: playthru
Company: UNIVERSAARL-DE

## Ergebnis

TARGET-023B recovered all missing Dimension Values with reopen proof.

## Werte

- PRODUCTLINE.SERVICE: already-exists - PRODUCTLINE.SERVICE ist bereits sichtbar.
- PRODUCTLINE.TRAINING: already-exists - PRODUCTLINE.TRAINING ist bereits sichtbar.
- COSTCENTER.SALES: already-exists - COSTCENTER.SALES ist bereits sichtbar.
- COSTCENTER.OPERATIONS: already-exists - COSTCENTER.OPERATIONS ist bereits sichtbar.
- CHANNEL.PARTNER: created - CHANNEL.PARTNER wurde ueber Dimensionsliste -> Dimension -> Dimensionswerte angelegt und nach Reopen sichtbar.

## UI-Learning

- Die gefilterte Dimensionswerte-Seite muss vor dem Schreiben als richtiger Dimensionskontext sichtbar sein.
- `Liste bearbeiten`, `Neu`, `Code` und `Name` werden als sichtbare Controls/Spalten dokumentiert.
- Ein Wert zaehlt erst, wenn er nach erneutem Oeffnen der gefilterten Seite sichtbar ist.

## Grenzen

- Keine Stammdaten.
- Keine Global-Dimension-Zuweisung.
- Keine Preview und keine Buchung.
