# Setup-Readiness-Matrix

Update nach `FIXEDASSETS-064-PURCHASE-INVOICE-LINE-TYPE-UI-PROBE-NO-TARGET`: Fixed Assets steht jetzt auf `purchase-invoice-line-type-helper-diagnosis-needed`. Der praktische Lauf war kein Setup-Fehler und kein Stammdatenfit, sondern ein UI-/Grid-Bedienungsproblem: Zeile blieb `Type = Item`, Eingabe `Fixed Asset` erzeugte Vendor-Registrierungsdialog; Draft `107209` wurde UI-first geloescht. Keine Preview, kein `Post`, kein Zugang/AfA. Naechster Schritt: Dropdown-/Lookup-Auswahl fuer Zeilentyp diagnostizieren, weiter ohne `FA-CNC-01`.

Update nach `FIXEDASSETS-063-PURCHASE-INVOICE-LINE-TYPE-STRICTNESS`: Fixed Assets steht jetzt auf `purchase-invoice-line-type-ui-probe-needed`. Kein BC-Lauf und keine Setup-Aenderung. Aus 062 ist maschinenlesbar belegt: Zeile blieb `Type = Item`, `FA-CNC-01` oeffnete falschen Vendor-Card-Kontext, Cleanup fuer `107223`, `107224` und `V00060` ist erledigt. Das ist kein fehlender Stammdaten-/Posting-Setup-Fit, sondern ein UI-/Zeilentyp-/Lookup-Problem. Naechster praktischer Schritt darf nur den Zeilentyp `Fixed Asset` in der Einkaufsrechnungszeile sichtbar setzen/pruefen, noch ohne `FA-CNC-01`.

Update nach `FIXEDASSETS-062-K30000-FA-CNC-01-TARGET-FIELD-MAPPING-NO-POSTING`: Fixed Assets steht jetzt auf `purchase-invoice-line-type-strictness-needed`. Der Card-/Lines-Kontext nach `Neu` ist tragfaehig, aber der Zielzeilenpfad ist rejected: `FA-CNC-01` oeffnete eine `Vendor Card`, waehrend die Zeile `Type = Item` blieb. Das ist kein Setup-Fit-Fehler im Sinne fehlender Stammdaten, sondern ein UI-/Lookup-/Zeilentyp-Sicherheitsproblem. Preview, `Post`, Zugang, AfA und deutscher Finalnachweis bleiben gesperrt; naechster Schritt ist ein strenger Zeilentyp-/Lookup-Helper.

Update nach `FIXEDASSETS-061-K30000-FA-CNC-01-TARGET-FIELD-MAPPING-GATE`: Fixed Assets steht jetzt auf `target-field-mapping-approved-no-posting`. Kein BC-Lauf und keine Setup-Aenderung; nach dem belegten Purchase-Invoice-Card-/Lines-Kontext aus 060 ist genau ein enger 062-Zielwerte-Preflight erlaubt. Erfolg zaehlt nur, wenn `K30000`, `Vendor Invoice No.`, `Type = Fixed Asset` und `FA-CNC-01` im selben sichtbaren Purchase-Invoice-/Lines-Kontext stehen. Preview, `Post`, Zugang, AfA und deutscher Finalnachweis bleiben gesperrt.

Update nach `FIXEDASSETS-060-PURCHASE-INVOICE-CARD-CONTEXT-PREFLIGHT-NO-TARGET-ENTRY`: Fixed Assets steht jetzt auf `purchase-invoice-card-lines-context-proven-no-target-entry`. Der UI-first Lauf beweist nach `Neu` den aktiven Belegkarten-/Lines-Kontext, aber keinen Zielbeleg und keine Anlagenzeile. Nicht freigegeben sind weiter Zielwerteingabe, Preview, `Post`, Zugang, AfA und deutscher Finalnachweis. Naechster Schritt ist ein 061-Gate fuer den moeglichen Zielwerte-Preflight.

Update nach `FIXEDASSETS-059-PURCHASE-INVOICE-FIELD-MAPPING-HELPER-REFINEMENT-OR-MANUAL-PATH`: Fixed Assets steht jetzt auf `purchase-invoice-context-guard-refined-no-target-entry`. Kein BC-Lauf; der Guard erkennt 058-Listen-/Inline-Signale nach `Neu` als `blocked-list-or-inline-row-context`. Naechster Schritt ist nur ein Card-/Lines-Kontext-Preflight ohne `K30000`, ohne `FA-CNC-01`, ohne Preview und ohne Buchung.

Update nach `FIXEDASSETS-058-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-RETRY-NO-POSTING`: Fixed Assets steht jetzt auf `purchase-invoice-field-mapping-blocked-before-entry`. Der erlaubte guarded Retry wurde ausgefuehrt, stoppte aber vor jeder Zielwerteingabe, weil nach `Neu` kein stabiler Purchase-Invoice-Card-/Zeilenkontext nachgewiesen wurde. Keine Preview, kein `Post`, kein Zugang, keine AfA, keine Setup-Aenderung, kein Draft. Naechster Schritt ist Helper-/Manual-Path-Refinement, nicht Preview oder Buchung.

Update nach `FIXEDASSETS-057-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-RETRY-GATE`: Fixed Assets steht jetzt auf `purchase-invoice-field-mapping-retry-approved-no-posting`. Freigegeben ist genau ein neuer enger 058-Retry mit Guard, nicht die Wiederholung von 055. Nicht freigegeben sind Preview, `Post`, Zugang, AfA, Setup-Aenderung, API-Shortcut oder Company-Wechsel.

Update nach `FIXEDASSETS-056-PURCHASE-INVOICE-FIELD-MAPPING-BLOCKER-DIAGNOSIS`: Fixed Assets steht jetzt auf `purchase-invoice-field-mapping-guard-ready-gate-needed`. Der 055-Blocker wurde ohne neuen BC-Lauf in einen Playwright-Guard und Evidence-Index ueberfuehrt. Stop-Kriterien sind Vendor-Registrierungsdialog, `Vendor Card`, versehentliche Vendor-Nummer und `FA-CNC-01` ohne sichtbaren Zeilentyp `Fixed Asset`. Keine Preview, kein `Post`, kein Zugang, keine AfA, keine Setup-Aenderung. Naechster Schritt ist `FIXEDASSETS-057`: Retry-Gate entscheiden.

Update nach `FIXEDASSETS-055-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-NO-POSTING`: Fixed Assets steht jetzt auf `purchase-invoice-field-mapping-blocked-wrong-context-cleaned-up`. Der UI-first Lauf ist rejected als Erfolg, aber wertvoll als Lernfall: Zielcodes im falschen Kontext zaehlen nicht. `V00040` und `107222` wurden per UI bereinigt. Kein Preview Posting, kein `Post`, kein Zugang, keine AfA, keine Setup-Aenderung und keine deutsche Finalaussage. Naechster Schritt ist `FIXEDASSETS-056`: sicherer Zeilenkontext-Helper und harte Stop-Kriterien fuer Vendor-Registrierungsdialoge/Vendor-Card-Popups.

Update nach `FIXEDASSETS-054-K30000-FA-CNC-01-PURCHASE-INVOICE-FIELD-MAPPING-GATE`: Fixed Assets steht jetzt auf `purchase-invoice-target-field-mapping-approved-no-posting`. Freigegeben ist nur ein enger UI-first Lauf fuer `K30000` im Kopf und `FA-CNC-01` in der Zeile mit zwingender Draft-Bereinigung. Nicht freigegeben sind Preview Posting, `Post`, Anlagenzugang, AfA, Setup-Aenderung oder deutsche Finalaussagen.

Update nach `FIXEDASSETS-053-K30000-PURCHASE-INVOICE-PREFLIGHT-NO-POSTING`: Fixed Assets steht jetzt auf `purchase-invoice-page-preflight-proven-no-target-entry`. Der UI-first Lauf belegt `Purchase Invoices` Page `9308`, `Purchase Invoice` Page `51`, Pflichtfelder `Vendor Name` und `Vendor Invoice No.`, Zeilenbereich `Type`/`No.` und `Post` als Gefahrengrenze. Kein `K30000`-Belegkopf, keine `FA-CNC-01`-Zeile, keine Preview, kein Anlagenzugang, keine AfA und keine Buchung sind freigegeben.

Update nach `FIXEDASSETS-052-K30000-VENDOR-PURCHASE-INVOICE-PREFLIGHT-GATE-DECISION`: Fixed Assets steht jetzt auf `purchase-invoice-preflight-approved-no-posting`. Der K30000-Techniknachweis aus `FIXEDASSETS-051` reicht fuer einen engen UI-first Purchase-Invoice-Preflight, um Belegseite, Pflichtfelder, Draft-/Cleanup-Verhalten, `K30000`, `FA-CNC-01` und gefaehrliche Posting-Aktionen sichtbar zu pruefen. Kein Anlagenzugang, keine AfA, keine Buchung und kein Setup-Fit sind freigegeben.

Update nach `FIXEDASSETS-051-K30000-VENDOR-PAGEINSPECTION-READONLY`: Fixed Assets steht vor dem Anlagenkauf jetzt auf `vendor-technical-defaults-proven-gate-needed`. Page Inspection beweist Page `Vendor Card (26)`, Source Table `Vendor (23)` und mehrere K30000-Feldwerte technisch: `DOMESTIC`, leere Currency/VAT/Tax-Felder, `Tax Liable = Nein`, `1M(8D)` und `BANK`. Das ist kein Anwender-Screenshot-Feldwertebild und keine Kaufbelegfreigabe. Naechster Schritt ist ein Gate fuer Purchase-Invoice-Preflight versus UI-sichtbaren Default-/Setup-Fit; kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung.

Update nach `FIXEDASSETS-050-K30000-VENDOR-DEFAULTS-PAGEINSPECTION-OR-SETUP-GATE-DECISION`: Fixed Assets bleibt vor dem Anlagenkauf auf `vendor-pageinspection-readonly-needed`. Kein Default-/Setup-Fit ist freigegeben, weil die fehlenden K30000-Defaults weder normal sichtbar noch per Personalisieren-Feldliste belegt sind. Naechster Schritt ist Page Inspection/Seitenpruefung read-only; kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung.

Update nach `FIXEDASSETS-049-K30000-VENDOR-PERSONALIZE-FIELD-AVAILABILITY-READONLY`: Fixed Assets bleibt vor dem Anlagenkauf auf `vendor-defaults-pageinspection-or-setup-gate-decision-needed`. `Personalisieren` oeffnet zwar sichtbar die `Vendor Card`, aber der Feldlisten-/`Add field`-Pfad ist nicht stabil offen und die vier kritischen Defaults sind dort nicht sichtbar. Kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung.

Update nach `FIXEDASSETS-048-K30000-VENDOR-DEFAULTS-MANUAL-PERSONALIZE-OR-SETUP-GATE-DECISION`: Fixed Assets bleibt vor dem Anlagenkauf auf `vendor-defaults-personalize-diagnosis-needed`. Ein Default-/Setup-Fit ist nicht freigegeben. Naechster Schritt ist nur `FIXEDASSETS-049`: per `Personalisieren` read-only/diagnostisch pruefen, ob die fehlenden K30000-Felder als Page-Felder verfuegbar sind. Kein Kaufbeleg, kein Zugang, keine AfA, keine Buchung.

Update nach `FIXEDASSETS-047-K30000-VENDOR-DEFAULTS-VISIBILITY-VALUE-DISCOVERY-READONLY`: Fixed Assets bleibt vor dem Anlagenkauf auf `vendor-defaults-gate-decision-needed`. Der read-only UI-Lauf zeigt `Tax Area Code`, `Tax Liable`, `Payment Terms Code = 1M(8D)` und `Payment Method Code = BANK`, aber weiter keine sichtbaren `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` oder `VAT Bus. Posting Group`. Naechster Schritt ist eine no-posting Gate-Entscheidung: manuelle Personalisierung als Sichtbarkeitspfad oder enger UI-first Default-/Setup-Fit; kein Kaufbeleg und keine Buchung.

Update nach `FIXEDASSETS-046-K30000-VENDOR-DEFAULTS-GATE-DECISION`: Fixed Assets bleibt vor dem Anlagenkauf auf `vendor-defaults-visibility-value-discovery-needed`. Kein Setup-/Default-Fit wurde freigegeben: `Personalisieren` als sichtbarer Diagnoseeinstieg beweist keine Werte fuer `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` oder `VAT Bus. Posting Group`. Naechster Schritt ist read-only Werte-/Sichtbarkeits-Discovery auf der `K30000`-Kreditorenkarte; kein Kaufbeleg, kein Setup-Fit und keine Buchung.

Update nach `FIXEDASSETS-045-K30000-VENDOR-PERSONALIZE-PAGEINSPECTION-DIAGNOSIS-READONLY`: Fixed Assets bleibt vor dem Anlagenkauf auf `vendor-defaults-gate-decision-needed`. `Personalisieren` ist als Settings-Einstieg sichtbar, aber keine Personalisierung wurde gespeichert und keine fehlenden Default-Felder wurden dadurch sichtbar belegt. Page Inspection per `Ctrl+Alt+F1` war im Browserkontext nicht stabil verfuegbar. `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group` bleiben offen; kein Kaufbeleg, kein Setup-Fit und keine Buchung.

Update nach `FIXEDASSETS-044-K30000-VENDOR-PURCHASE-INVOICE-GATE-DECISION`: Kaufbeleg-Readiness fuer `K30000` bleibt nicht erfuellt. Sichtbare Payments-/Tax-Teilfelder reichen nicht, solange `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group` nicht sichtbar oder technisch diagnostiziert sind. Naechster Schritt: `FIXEDASSETS-045` read-only Personalisieren/Page-Inspection-Diagnose, kein Setup-Fit und keine Buchung.

Update nach `FIXEDASSETS-043-K30000-VENDOR-INVOICING-FASTTAB-VISIBILITY-READONLY`: Fixed Assets bleibt vor dem Anlagenkauf auf `purchase-invoice-gate-decision-needed`. `Invoicing` wurde read-only per kleinem `aria-expanded=false`-Control geoeffnet; sichtbar sind `Tax Liable`, `Tax Area Code`, `Posting Details` und Withholding-Tax-Felder. `1M(8D)` und `BANK` sind weiter sichtbar. Nicht sichtbar belegt bleiben `Vendor Posting Group`, `Gen. Bus. Posting Group`, `Currency Code` und `VAT Bus. Posting Group`. Kein Kaufbeleg, kein Setup-Fit und keine Buchung.

Update nach `FIXEDASSETS-042-K30000-VENDOR-DEFAULTS-VISIBILITY-DECISION`: Fixed Assets bleibt vor dem Anlagenkauf auf `fasttab-visibility-proof-needed`. `K30000`, `1M(8D)` und `BANK` sind sichtbar; Posting-/Currency-/Tax-Defaults sind nicht sichtbar belegt. Entscheidung: kein Setup-Fit und kein Kaufbeleg, sondern `FIXEDASSETS-043` als read-only FastTab-Sichtbarkeitslauf mit Zielcaption-/Zielwert-Nachbedingung.

Update nach `FIXEDASSETS-041-K30000-VENDOR-DEFAULTS-FIELD-DIAGNOSIS-READONLY`: Fixed Assets bleibt vor dem Anlagenkauf auf `visibility-decision-needed`. `K30000`, `1M(8D)` und `BANK` sind sichtbar; Posting-/Currency-/Tax-Defaults sind im Bild nicht sichtbar und die FastTab-Expansion ist nicht ausreichend bewiesen. Naechster Schritt: `FIXEDASSETS-042`, keine Einkaufsrechnung und keine Buchung.

Update nach `FIXEDASSETS-040-K30000-VENDOR-DEFAULTS-DECISION`: Fixed Assets bleibt vor dem Anlagenkauf auf `field-diagnosis-needed`. `K30000` ist sichtbar und Zahlungsdefaults sind belegt, aber Posting-/Currency-/Tax-Defaults sind nicht sichtbar. Naechster Schritt: `FIXEDASSETS-041` read-only UI-Diagnose mit breiter Layoutansicht, `Mehr anzeigen`, Personalisieren/Page Inspection nur als Diagnose.

Stand: 16.06.2026

Diese Matrix zeigt, ob ein Prozess in `RM-DEMO` praktisch tragfaehig ist. Sie ist keine Freigabe fuer Buchungen oder Setup-Aenderungen. Gates aus `POSTING-AND-SETUP-GATES.md` bleiben vorrangig.

Company-Kontext: Alle Prozesszeilen gelten aktuell fuer `RM-DEMO`, solange keine andere Company explizit genannt ist. Seit `GOVERNANCE-012` ist `COMPANY-REGISTRY.md/json` die Pflichtquelle fuer Multi-Company-Arbeit in `MCP_1_20260210`. Neue Prozessfaelle muessen ihren Company-Kontext in Evidence und `PROCESS-CASE-REGISTRY.json` fuehren.

| Prozess | Stammdaten | Setup | Preflight | Buchung erlaubt? | Luecken | Naechster Schritt |
|---|---|---|---|---|---|---|
| Company Registry | `CRONUS USA, Inc.`, `My Company` und `RM-DEMO` sichtbar; `RM-PROD`, `RM-SALES`, `RM-SERVICE`, `RM-SHARED`, `RM-AT` geplant/nicht sichtbar | Registry angelegt und live read-only enumeriert | `GOVERNANCE-013` belegt Companies-Liste; keine Company-Aktion | nein | Zielcompanies noch nicht angelegt; `My Company` nicht profiliert; keine DE-Finalcompany | nur mit neuem Company-Aktionsplan weiter; sonst `FIXEDASSETS-031` |
| O2C | `D10000`, `RM-M100`, `FRA-ZL`, `EUR`, `PRODUCTLINE=MACHINE`, `CHANNEL=B2B` belegt | CRONUS-Laborfit inkl. `FRA-ZL` + `RESALE` -> `14140` | Preview Posting und Postenspur belegt | nein, Referenzbuchung `PS-INV103297` nicht wiederholen | deutsche 19-%-USt, deutscher Kontenplan, finaler DE-Screenshot | nur read-only oder Buch-Sync; keine zweite O2C-Buchung |
| P2P | `K10000`, `RAW-STEEL`, `FRA-ZL` belegt | Vendor Template und CRONUS-Posting tragen im Labor | Preview/Buchung/Postenspur belegt | nein, Rechnung `108219` nicht wiederholen | deutsche Vorsteuer, deutscher Kontenplan, Kreditorenzahlung | nur Folge-Evidence oder Payment-Gate |
| Inventory | `RM-M100`, `RAW-STEEL`, `FRA-ZL` belegt | Inventory Posting Setup traegt im Labor | Journal Check und Inventory Valuation belegt | nein, `INV008-899959` nicht wiederholen | keine finalen DE-Lagerwerte, Warehouse nicht aktiv | nur read-only Trace oder neues Ziel mit Gate/Zweck |
| Payments / OP | `D10000`, `PS-INV103297`, `BANK-RM-01` belegt | Bankkonto-Fit CRONUS-Labor, Page `372` Bankposten sichtbar | Journal Check, Apply Entries, Zahlung `PAY011-PS103297` belegt | nein, Zahlung nicht wiederholen | Bankabstimmung offen, Kreditorenzahlung offen, DE-Bank/Compliance offen | nur mit neuem Gate oder neuem Evidence-Zweck |
| Reporting | gebuchte Posten und Artikelposten-Dimensionen belegt | Analysis View `REVENUE` passt nicht zu `PRODUCTLINE`/`CHANNEL`; `RM-PLCH` nicht angelegt | mehrere read-only Negativpfade belegt | nein | Financial-Reports-Summenwirkung offen; Analysis-View-Setup locked | neues Gate mit gescoptem New-/Kartenmuster oder alternativer offizieller Pfad |
| Fixed Assets | `HGB`, `MACHINES`, `FA-CNC-01` und `K30000` sind als CRONUS-USA-Laborstammdaten belegt; `K30000` ist technisch auf `Vendor Card (26)` / `Vendor (23)` nachgewiesen | Anlagen-/AfA-/Posting-Setup ist fuer Stammdatenfit laborseitig belegt; K30000-Page-Inspection zeigt `Vendor Posting Group = DOMESTIC`, `Gen. Bus. Posting Group = DOMESTIC`, `Currency Code = Leer`, `VAT Bus. Posting Group = Leer`, `Tax Area Code = Leer`, `Tax Liable = Nein`, `1M(8D)`, `BANK` | `FIXEDASSETS-053` belegt die leere Purchase-Invoice-Seite; `FIXEDASSETS-060` beweist nach `Neu` den aktiven Purchase-Invoice-Card-/Lines-Kontext; `FIXEDASSETS-062` und `FIXEDASSETS-064` sind rejected, weil Zeilentypwechsel nicht sichtbar gelang und Vendor-/Registrierungslogik aufging | nein | kein Zielbeleg mit gueltiger Anlagenzeile, kein sichtbar stabil gesetzter Zeilentyp `Fixed Asset`, keine Preview, kein Zugang/AfA, keine Anlagenposten, kein deutscher HGB-/Kontenplan-Endstand | `FIXEDASSETS-065-PURCHASE-INVOICE-LINE-TYPE-HELPER-DIAGNOSIS`; Dropdown-/Lookup-Auswahl fuer Zeilentyp diagnostizieren, weiter ohne `FA-CNC-01` |
| Warehouse | `FRA-ZL` sichtbar | keine aktivierte Warehouse-/Bin-Logik belegt | Einstiegspfade read-only | nein | Bins, directed warehouse, Receipts/Picks nicht praktisch | nur mit Warehouse-Gate aktivieren |
| Manufacturing / Assembly | `RM-M100`, `RAW-STEEL` sichtbar; `COMP-CTRL`, `KIT-MAINT` fehlen | BOM/Routing/Assembly-Struktur nicht belegt | Tell-Me-Readiness | nein | Komponenten, BOM/Routing, Auftrag, Verbrauch, Output | Setup-Gate vor praktischem Lauf |
| Service | `D10000` sichtbar; Servicezielobjekte fehlen | Service Management Setup nur read-only | Einstiegspfade read-only | nein | Service Item, Ressource, Ersatzteil, Technikerlager | UI-first Setup-Gate vor Serviceauftrag |
| Projects | `D10000` sichtbar; Projektzielobjekte fehlen | Projekt-/WIP-Setup nicht belegt | Einstiegspfade read-only | nein | Projekt, Aufgaben, Ressource, Material, Lager | UI-first Setup-Gate vor Projektprozess |
| Dropshipping / Sonderverkauf | `D11000`, `K20000`, `SP-PUMP-01` fehlen | Purchasing-Code-/Drop-Shipment-Logik nicht belegt | Einstiegspfade read-only; Shopify gestrichen | nein | Zielstammdaten und Drop-Shipment-Prozess | nur mit Gate, ohne Shopify-Scope |
| Intercompany / Ausland | Zieldebitoren/Companies fehlen | IC Partner, VAT/Waehrung nicht eingerichtet | Einstiegspfade read-only | nein | neue Company, IC-Partner, Ausland/VAT/Waehrung | Gate `NEW-COMPANY-001`/IC noetig |
| Tax / VAT | CRONUS-USA Sales Tax belegt | deutsche 19-%-VAT-Matrix nicht eingerichtet | Gate-Readiness `TAX-002` | nein | DE-VAT Business/Product Groups, VAT Posting Setup, VAT Entries | praktischer DE-VAT-Fit nur mit Freigabe |
| Compliance / E-Rechnung | keine finalen deutschen Compliance-Stammdaten | E-Documents, Versandprofil, Change Log nicht eingerichtet | Readiness sichtbar | nein | E-Rechnung, Peppol/Provider, Archiv, GoBD, Change Log | eigenes Gate erforderlich |
| Security / Rollen | keine User-/Permission-Aenderung | Admin-Kontexte nur read-only | Readiness sichtbar | nein | SoD, Profile, Permission Sets, Security Groups | eigenes Gate erforderlich |
| Migration / Cutover | Zielstruktur beschrieben | kein Import/Opening Balance | Buch-Sync | nein | Configuration Packages, Salden, Abstimmung | eigenes Gate erforderlich |
| Integrationen / Operations | Zielbild beschrieben | keine Extension/API/Job Queue/Monitoring-Aenderung | Buch-Sync | nein | Connector, Power BI, Power Platform, Job Queue, Telemetry | eigenes Gate erforderlich |
