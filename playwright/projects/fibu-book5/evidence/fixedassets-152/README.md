# fixedassets-152 Evidence Index

| Datei | Typ | Beweist | Beweist nicht | Status |
|---|---|---|---|---|
| `FIXEDASSETS-152-review.md` | Markdown | lokale Bewertung der FA-151-Readiness-Evidence | keine BC-Ausfuehrung, keinen Anlagenzugang | `review`, `labor` |
| `FIXEDASSETS-152-result.json` | JSON | Gate-Entscheidung und naechster Case | keine Ursache im BC-Code, keine Buchung | `decision` |

Aktuelle Wahrheit: `FA-CNC-01` zeigt `Posting Group = MACHINES` und `Book Value = 0,00`; `Acquire` ist sichtbar, aber deaktiviert. Der Anlagenzugang bleibt gesperrt.
