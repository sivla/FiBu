# Skill: bc-navigation

Use this skill for Business Central UI navigation.

## Rules

- Stay inside `MCP_1_20260210`.
- Confirm the target company before changing data.
- Prefer Page ID or proven page entry paths over ambiguous Tell-Me first hits.
- Do not press Enter blindly in Tell-Me when results are ambiguous.
- Use wide layout when table columns or line details matter.
- Hide FactBox if it blocks the grid or screenshot purpose.
- Use Personalisieren only as a documented diagnostic tool unless a profile/user-personalization change is explicitly in scope.
- Use Page Inspection as a diagnostic proof for Page, Table and field values when visual UI fields are missing.

## Screenshot rule

A screenshot must show the thing it claims to prove. If the target code, field, button or posting trace is not visible, mark the image as rejected/debugging or candidate-only.
